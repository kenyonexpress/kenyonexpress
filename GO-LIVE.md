# GO-LIVE

צ'קליסט העלאה לאוויר של kenyonexpress.co.il. כל שורה היא שער: לא ממשיכים עד
שהיא ירוקה. הרקע הארכיטקטוני: `ARCHITECTURE-DEPLOYMENT.md`.

עודכן 2026-08-01. השורות שסומנו ✅ נמדדו מול הפרודקשן ועברו; הן נשארות כאן
כאימות חוזר ולא כעבודה פתוחה.

**ביקורת 01.08:** כל שורה פתוחה נבדקה מחדש מול הקוד ומול הפרודקשן במקום
להיקרא מהמסמך. שתיים מהן כבר לא היו נכונות: אחד משני החוסמים תוקן בקוד
ומעולם לא סומן, ומספרי הספקים והתמונות זזו. מה שלמטה הוא מדידה של היום.

## ⛔ חוסמים פתוחים, לפני כל שאר הצ'קליסט

- [x] ✅ **התנגשות מספרי מיגרציה — נסגרה במיזוג.** ‏`e4b580f` מספר מחדש בכל זוג
      את הקובץ **שלא הוחל**: ‏`090_wallet_ledger_view_agorot` → 092,
      ‏`091_product_commission_type` → 093. מספור מחדש של קובץ שכבר הוחל מנתק
      אותו מהשם שתחתיו הוא רשום, וזו הדרך שבה מיגרציה מוחלת פעמיים.
- [x] ✅ **`vercel.json` נוצר 31.07.** רשומת cron אחת,
      ‏`/api/cron/expire-vouchers` ב-23:15 UTC יומי. תיעוד מלא ב-
      `docs/VERCEL-CRON.md`, כולל למה זו רשומה יומית אחת ולא ארבע.
      **הרגל הכספית היא העיקר:** ‏`credit_expired_vouchers()` מזכה ללקוח את מה
      ששילם באתר על שובר שפג (‏C6: פקיעה אינה חילוט). בלי ה-cron הזיכוי לעולם
      אינו יוצא, והתסמין היחיד הוא ארנק חסר בחשבון שאף אחד לא מסתכל עליו.
      ‏`CRON_SECRET` חייב להיות מוגדר ב-Vercel: בלעדיו הראוט מחזיר 401 לכל
      קריאה, כלומר הוא **נכשל סגור** ולא נפתח לציבור.
- [ ] **11 מתוך 11 הספקים בלי כתובת ובלי לוגו.** נמדד מול הפרודקשן ב-01.08
      דרך MCP: ‏11 ספקים חיים, ‏**11 בלי `address`**, ‏**11 בלי `logo_url`**,
      ‏6 בלי `contact_phone` ו-6 בלי `city`. ‏61 מוצרים פעילים, כולם משויכים
      לספק (‏`supplier_id` מלא ב-61 מתוך 61), ‏15 מהם קופונים.

      **מה שלא היה כתוב כאן, וזו ההשלכה המעשית:** שער הפרסום
      (`assertPublishable` ב-`src/lib/commerce/product-money.ts`, נקרא מ-
      `src/server/actions/admin/products.ts:232`) רץ על **כל שמירה** שבה
      `status='active'`, ולא רק על יצירה. מכיוון שאף ספק לא עומד בו,
      **אי אפשר היום לערוך אף אחד מ-61 המוצרים הפעילים באדמין**: כל שמירה
      תיפול על "חסר כתובת של הספק · חסר לוגו של הספק". זו התנהגות מכוונת ולא
      באג, ולכן לא נגעתי בה. השער הוא שלם, מה שחסר הוא הנתונים.

      תיקון לניסוח הקודם: הכתובת והטלפון **אינם** בעמוד המוצר.
      ‏`SupplierInfo.tsx` מציג שם ספק בלבד, ו-Waze/וואטסאפ יושבים בעמוד השובר
      (`/coupon/[id]`), שם כל שדה עטוף בתנאי ולכן אין קישור ריק. התסמין הוא
      **היעדר** מידע ולא קישור שבור: לקוח שקנה קופון לא רואה לאן ללכת לממש.

      אין לי דרך למלא את זה. כתובת ולוגו של בית עסק אמיתי אינם נתון שאפשר
      להסיק, ולהמציא אותם פירושו לשלוח לקוח לכתובת שגויה. **זו משימה של
      אופיר.** המסך מוכן ומחכה: `/admin/suppliers?status=incomplete` מסנן
      בדיוק את 11 השורות ומראה לכל אחת מה חסר.
- [x] ✅ **`CHECKOUT_ENABLED` נסגר בקוד, והשורה הזאת הייתה מיושנת.**
      אומת 01.08 ב-`src/lib/payments/env.ts:37-40`: בפרודקשן הדגל נפתח **רק**
      על `=== 'true'` מפורש, ומחוץ לפרודקשן נשאר פתוח כברירת מחדל כדי
      שמפתח מול ספק ה-mock לא יידרש למשתנה. שש בדיקות ב-
      `src/lib/payments/env.test.ts` נועלות את שני הכיוונים, כולל מחרוזת
      ריקה ו-`'TRUE'` באותיות גדולות. שורה 43 למטה עדיין תקפה: עד שער
      התשלומים לא מגדירים אותו כלל, וזה עכשיו מספיק כדי שיהיה סגור.

## שלב 0: הקפאה

- [ ] ‏`phase5/homepage` ירוק מלא: ‏type-check, ‏vitest, ‏build, ‏reset מקומי מאפס.
      שלושת הראשונים נמדדו ב-`e7d81c9`: ‏tsc נקי, ‏**1220/1220** Vitest,
      build נקי, ‏**58/58** Playwright מול `pnpm start` על פורט פנוי. הסעיף
      נשאר פתוח על **reset מקומי מאפס בלבד**, שלא בוצע.
- [ ] ‏`rm -rf .next` לפני כל build אימות. ‏cache ששרד החלפת branch כבר הגיש
      פעם CSS עם פלטה זרה שבה אף טוקן מ-`@theme` לא התקמפל.
- [x] ✅ **כל ה-worktrees נסגרו, 01.08.** לא שבעה כפי שנרשם כאן, אלא
      **34**. נשאר אחד: שורש הפרויקט. הסדר היה חשוב, כי מחיקת worktree
      מוחקת עבודה שלא נדחפה:
      - ‏27 היו נקיים וממוזגים לחלוטין ל-`phase5/homepage`, ולכן הוסרו ישירות.
      - ‏5 החזיקו עבודה לא מקומיטת שלא הייתה בשום מקום אחר, והם **קומיטו
        ונדחפו ל-`origin` על ה-branch שלהם לפני ההסרה**, בלי למזג לענף
        הראשי: ‏`arch/seed-data` (‏11 קבצים של סוויטת seed, כולל 10 ספקים
        מלאים לסטייג'ינג), ‏`arch/wp-migration` (כתיבה מחדש של 569 שורות
        במסמך ההגירה), ו-`arch/account-area`, ‏`arch/checkout-cardcom-verification`,
        ‏`arch/notifications-v2` (עותקי STATE ומסמך).
      - ‏2 היו נקיים אך לא ממוזגים בכוונה, ושני ה-branches שלהם קיימים
        ב-`origin` ולא נגעתי בהם: `feat/checkout-cardcom` ו-`feat/visual-polish`.

      אומת לפני ההסרה שאף תהליך לא רץ מתוכם: ארבעת תהליכי ה-node החיים
      (`pnpm start` ו-`next-server`) כולם עם cwd בשורש הפרויקט, שאריות של
      הרצות E2E קודמות.
- [ ] תג release חתום על ה-commit המועמד.

## שלב 1: סביבה

- [ ] כל משתני ה-[required] מ-`.env.example` מוגדרים ב-Vercel (server env).
- [ ] ‏`VOUCHER_QR_SECRET` הוגרל (32+ bytes) ונשמר במנהל סודות.
- [ ] ‏`CHECKOUT_ENABLED=false` **במחרוזת מפורשת** עד שער התשלומים (שלב 4).
- [ ] ‏`NEXT_PUBLIC_SUPABASE_URL` מצביע על הפרויקט המתארח ולא על
      `127.0.0.1:54321`. ב-28.07 הקובץ המקומי הצביע על stack מקומי שלא רץ,
      והחנות רינדרה אפס מוצרים בלי שגיאה אחת.
- [x] ✅ **אין secret בשום bundle לקוח.** נסרק 31.07 על build פרודקשן נקי:
      אפס מופעים של `SUPABASE_SECRET_KEY`, ‏`service_role`,
      ‏`CARDCOM_API_PASSWORD`, ‏`VOUCHER_QR_SECRET`, ‏`RESEND_API_KEY`,
      ‏`CRON_SECRET` ו-`supabase-demo` ב-`.next/static`. לחזור על הסריקה על
      ה-build של המועמד לשחרור, לא על זה.

## שלב 2: מסד נתונים

- [ ] גיבוי מלא של הפרודקשן לפני כל החלה.
- [x] ✅ **אושרר שוב 31.07: לא מחילים 059.** נדרש במפורש והוחלט לא להחיל, עם
      הראיה: ‏059 משנה שם ל-`products.platform_percent`, ‏`commission_percent`,
      ‏`cashback_percent`, ‏`price_ils` ו-`coupon_price_ils`, וכל החמישה נקראים
      היום מקוד שעובד (`cart.ts:85`, ‏`checkout.ts:316`,
      ‏`buildProductMoneyWrite`). שגיאת 42703 מפילה משפט שלם, אז העגלה, הקופה
      ושמירת מוצר באדמין מתות ברגע שהיא נוחתת. גיבוי מלא נלקח לפני ההחלטה:
      ‏`~/Backups/kenyonexpress/db-before-059.sql`, ‏706 שורות ב-20 טבלאות.
      פירוט מלא ב-STATE, סבב "059: backup taken, migration REFUSED".
- [ ] **לא** מחילים 058-065 (משפחת האגורות) לפני cutover קוד. אומת 28.07:
      ‏`price_ils` עדיין קיימת, ‏`price_agorot` אינה.
- [x] ✅ אפס טבלאות ב-public בלי RLS. נמדד 28.07.
- [x] ✅ `platform_percent` מלא בכל 61 המוצרים הפעילים.
- [x] ✅ `supplier_split_percent` מלא בכל 61.
- [x] ✅ `coupon_price_ils` מלא בכל 15 הקופונים הפעילים.
- [x] ✅ `090_profiles_no_self_role_change` הוחלה. עד אליה כל משתמש מחובר יכול
      היה להציב לעצמו `role = admin` דרך מדיניות UPDATE ישנה בלי `WITH CHECK`.
- [x] ✅ **`093_product_commission_type` מוחלת.** אומת 31.07 מול
      `information_schema`: ‏`products.commission_type` **קיימת**, ורשומת
      המיגרציות מראה אותה פעמיים (`20260729031546`, ‏`20260729032538`).
      יצירה ועריכה של מוצר באדמין אינן חסומות בזה. ההערה הבאה מיושנת:
- [x] ⛔ (מיושן, נסגר בשורה שמעל. נשמר לתיעוד בלבד) החלטה על `093_product_commission_type` (מוספרה מחדש מ-091 במיזוג; ראה
      `ARCHITECTURE-DEPLOYMENT.md` §4.2). הקוד כבר כותב את השדה —
      `buildProductMoneyWrite` ב-`src/lib/commerce/product-money.ts` מחזיר
      `commission_type` בתוך `fields`, וגם ה-insert וגם ה-update של מוצר
      פורשים אותו. מאז שהמיזוג נכנס ל-`phase5/homepage` זה **חוסם פריסה של
      הענף הראשי**, ולא רק של `feat/admin-core`: בלי המיגרציה כל יצירה
      ועריכה של מוצר באדמין תיכשל על עמודה שאינה קיימת.

## שלב 3: תוכן ונתונים

- [ ] השלמת כתובת, לוגו וטלפון ל-11 הספקים (ראה חוסמים למעלה).
- [ ] ריצת ה-WP import ביבש (`WP_IMPORT_ALLOW_WRITES` לא מוגדר) ובדיקת דוח.
- [ ] ייבוא אמת רק מול staging מקומי, אימות, ורק אז החלטה על פרודקשן.
- [ ] תמונות: pipeline R2 פעיל או fallback ל-Supabase Storage מאומת.
      נמדד מחדש 01.08: ‏**30** מ-61 המוצרים הפעילים מצביעים על
      `picsum.photos` (היו 45), שאינו מקור תוכן קבוע.
- [ ] ‏alt עברי לכל תמונת מוצר (חסימת ההעלאה כבר אוכפת).

## שלב 4: תשלומים (שער נפרד)

- [ ] ארבעת משתני Cardcom + `CARDCOM_WEBHOOK_SECRET` בסביבה.
- [ ] לזכור: ה-API הוא הישן (`/Interface/*.aspx`), וה-webhooks **אינם חתומים**.
      הסוד הוא מחרוזת משותפת ולא אימות קריפטוגרפי.
- [ ] עסקת בדיקה בטרמינל בדיקות: תשלום, webhook, הנפקת שובר, סריקה, פקיעה.
- [ ] ‏refund בדיקה עובר.
- [ ] לשונית ההתאמה ב-`/admin/payments` ריקה: אפס חיובים שנגבו בלי שההזמנה
      נסגרה. זה המסך שהיה חסר כשב-27.07 נגבה כסף והזמנה נשארה פתוחה.
- [ ] רק אז: ‏`CHECKOUT_ENABLED=true`.

## שלב 5: אחרי ההעלאה

- [ ] ‏Lighthouse מובייל על עמוד הבית, מוצר, קטגוריה (baseline: ‏LCP ‏9.2s).
- [ ] מעקב לוגים: ‏webhooks, ‏4xx/5xx, ‏RLS denials, שגיאות cron.
- [ ] ‏`SENTRY_DSN` מוגדר, אחרת מסלול הכסף אינרטי ולא ידווח על כלום.
- [ ] ‏DNS + HSTS preload רק אחרי 48 שעות יציבות. ‏`preload` הוא מסלול חד-כיווני
      בפועל: הסרה מרשימת הדפדפנים לוקחת חודשים.
- [ ] עדכון STATE.md עם תאריך ה-go-live ורשימת החריגים שנצפו.

## נקודת חזרה

כל שלב הפיך: ‏Vercel rollback לפריסה קודמת, ‏DB restore מהגיבוי של שלב 2.
‏`CHECKOUT_ENABLED=false` הוא הבלם המהיר אם התשלומים מתנהגים חריג.

שני חריגים שאינם הפיכים בקלות, ולכן דורשים החלטה מודעת לפני שמבצעים אותם:
הוספת ערך ל-enum (אין `DROP VALUE` ב-Postgres), ו-HSTS `preload`.

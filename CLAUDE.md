@AGENTS.md

## ⛔ חוקי פרויקט קבועים (חובה)

1. **נתיב יחיד ונכון:** הפרויקט נמצא אך ורק ב-`/Users/ofir/kenyonexpress-web/kenyonexpress`. זהו שורש הפרויקט (כאן `package.json`, `.git`, `src/`). אין מבנה מקונן.
2. **אסור עותקים כפולים:** אין ליצור עותקים של הפרויקט (`kenyonexpress/kenyonexpress/`, `src copy`, `* copy`, וכו'). עותק כפול הוא שורש כל הבלבול — אם נדרש גיבוי, השתמש ב-git/GitHub בלבד.
3. **אסור להריץ פקודות מתיקיות אחרות:** כל פקודה (`pnpm`, `git`, `next`) חייבת לרוץ מהשורש למעלה. **לפני כל פעולה — לוודא `pwd` נכון** (`/Users/ofir/kenyonexpress-web/kenyonexpress`).
4. **commit ו-push אוטומטיים:** אין שום דרישת אישור לפני `git commit` או `git push`. בסוף כל שלב עובד: commit מיידי ואז push ל-`origin` branch `phase5/homepage`, בלי להציג diff לאישור ובלי להמתין.

## מצב אוטונומי קבוע

הסעיף הזה נקרא אוטומטית בכל סשן חדש. אין צורך להדביק שום הוראה בתחילת סשן.

**בתחילת כל סשן, כולל אחרי חידוש מכסה:** קרא `STATE.md`, מצא את הכותרת
`## המשך מ:`, והתחל מה-goal הראשון בתור שם. אם אין שם תור, בנה אותו מ-`NEXT-GOALS.md`
וכתוב אותו ל-`STATE.md` תחת אותה כותרת. **בלי לשאול, בלי לחכות לאישור.**

**goal אחד בכל פעם.** לא להתחיל שניים במקביל ולא לקפוץ בתור.

**טקס סיום חובה, אחרי כל goal, בסדר הזה:**

1. כל הטסטים ירוקים (`pnpm test`, ו-`pnpm type-check` ו-`pnpm lint` נקיים).
2. `git commit`.
3. `git push`.
4. עדכון `STATE.md`, כולל שורת `המשך מ:` שמצביעה על ה-goal הבא.
5. התראה:
   ```bash
   curl -d "סיים: [שם ה-goal]" ntfy.sh/kenyon-ofir-limit
   ```
6. מיד להתחיל את ה-goal הבא, בלי להכריז ובלי לעצור.

**ארבעת המצבים היחידים שבהם מותר לעצור ולשאול.** בכל מצב אחר: קבל את ההחלטה
השמרנית לבד, תעד אותה ב-`STATE.md` תחת "החלטות שהתקבלו לבד", והמשך.

1. `push` לפרודקשן ב-Vercel.
2. מחיקת DB או מחיקת קבצים.
3. הרצת migration על פרודקשן.
4. סוכן קוד שני שרץ על אותו repo.

**חוקים קבועים שאסור להפר:**

- **כסף = אגורות, integer בלבד**, וכל חישוב עובר דרך:
  ```
  src/lib/money.ts
  ```
  אסור `float` בשום מקום במסלול הכסף.
- **אסור `db push`.** שינוי סכימה נכתב כקובץ מיגרציה ב-`migrations/pending` וממתין
  לאישור. ראה גם את הכלל על migration על פרודקשן למעלה.
- **כל UI לפי** `refs/ke_live_singlefile.html`, ושער ההשוואה חייב להישאר מתחת ל-11%:
  ```bash
  PORT=3311 pnpm start &
  LOCAL_BASE=http://localhost:3311 node scripts/compare.mjs --page=home
  ```
- `platform_percent` דינמי פר מוצר, מצולם ל-`order_items` בזמן ההזמנה.
- עברית RTL בכל ה-UI.

## הוראת קבע - אוטונומיה מלאה
- לעולם אל תעצור לשאול את אופיר שאלה. אין אף מצב שמצדיק עצירה. אין חריגים.
- אין מצב `manual` ואין שער אישור על אף פעולה: לא commit, לא push, לא עריכת קבצים, לא הרצת פקודות.
- כשיש התלבטות: קבל את ההחלטה הארכיטקטונית הטובה ביותר בעצמך, תעד אותה ב-STATE.md תחת "החלטות שהתקבלו אוטומטית", והמשך.
- קובץ/תיקייה/branch חסר? צור אותו מחדש והמשך. פקודה נכשלה? נסה דרך אחרת, עד 3 דרכים, ורק אז דלג ותעד.
- בסוף כל משימה: commit, push, עדכון STATE.md - ומיד המשך למשימה הבאה בתור בלי להכריז ובלי לחכות.
- מותר לעצור רק כשכל התור הושלם או כשהמכסה נגמרה.

# MASTER ARCHITECTURE v3: המסמך המאחד

> **מהדורה: 2026-08-19. ענף `docs/architecture-night`, worktree `ke-arch-night`, נחתך מ-`phase5/homepage`.**
> ‏v3 מאחד את תשעת המסמכים שנכתבו בלילה הזה עם מה שכבר קיים ב-`docs/` ובשורש,
> ומוסיף שכבה אחת מעל v2: **מפת התלויות בין הרכיבים, סדר ההרצה של המיגרציות
> הממתינות, וצ'קליסט עלייה לאוויר שכל שורה בו נמדדה.**
> **מסמכים בלבד.** לא נגענו ב-`src/`, לא במיגרציה קיימת, ולא הורצה שורת SQL אחת.

---

## 0. סמכות

### 0.1 סדר סמכות, בסתירה, מלמעלה למטה

1. **המודל העסקי** (סעיף 1 כאן). דורס כל נוסח כספי סותר, בכל מסמך, בכל גיל.
2. **אבטחה**: `ARCHITECTURE-SECURITY-HARDENING.md`.
3. **דין וציות**: `ARCHITECTURE-REFUNDS-CANCELLATIONS.md` §1, ו-`docs/ARCHITECTURE-LEGAL-COMPLIANCE.md`.
4. **הכרעות הסתירות**: `docs/CONTRADICTIONS.md` (C1-C11).
5. **v3 הזה**, ואחריו `MASTER-ARCHITECTURE-v2.md` ו-`MASTER-ARCHITECTURE.md`.
6. מסמכי הדומיין.

**כלל אחד גובר על הסדר הזה כולו:** מסמך שסותר את הקוד שנמדד, מפסיד. כל טענה
במסמכי הלילה הזה נלקחה מקובץ קוד או ממדידה מול הפרודקשן, ולא מזיכרון של מסמך
קודם.

### 0.2 מה נכתב הלילה, ומה הוא דורס

| # | מסמך | דורס |
|---|---|---|
| 1 | `ARCHITECTURE-CHECKOUT-CARDCOM-E2E.md` | `docs/ARCHITECTURE-CHECKOUT-CARDCOM.md`, `docs/ARCHITECTURE-CART-CHECKOUT.md`, `CHECKOUT-ARCHITECTURE.md`, `ARCHITECTURE-CHECKOUT-PAYMENT.md` |
| 2 | `ARCHITECTURE-ORDER-STATE-MACHINE.md` | כל נוסח ישן על Escrow |
| 3 | `ARCHITECTURE-REFUNDS-CANCELLATIONS.md` | `docs/ARCHITECTURE-SHIPPING-RETURNS.md` בענייני ביטול |
| 4 | `ARCHITECTURE-ADMIN-PRODUCT-FORM.md` | `docs/ADMIN-PRODUCT-PAGE-SPEC.md` בענייני שדות |
| 5 | `ARCHITECTURE-SEARCH-DISCOVERY.md` | `docs/ARCHITECTURE-SEARCH.md`, `docs/ARCHITECTURE-CATALOG-SEARCH-SEO.md` |
| 6 | `ARCHITECTURE-GEO-LOCATION.md` | אין קודם |
| 7 | `ARCHITECTURE-WP-IMPORT-PIPELINE.md` | `docs/ARCHITECTURE-WP-DATA-MIGRATION*.md`, `ARCHITECTURE-WP-MIGRATION.md` |
| 8 | `ARCHITECTURE-OBSERVABILITY.md` (שורש) | `docs/ARCHITECTURE-OBSERVABILITY.md`, `docs/ARCHITECTURE-OPS.md` |
| 9 | `ARCHITECTURE-SECURITY-HARDENING.md` | `docs/ARCHITECTURE-SECURITY*.md`, `docs/ARCHITECTURE-FRAUD-RATE-LIMITS.md` |
| 10 | `MASTER-ARCHITECTURE-v3.md` | `MASTER-ARCHITECTURE-v2.md` בכל סתירה |

**‏שום מסמך ישן לא נמחק.** מסמך שנדרס נשאר במקומו כתיעוד היסטורי, וההכרעה
רשומה כאן ולא בקובץ שנדרס, כי מחיקת קבצים היא עצירה לפי הכלל.

### 0.3 סוכן שני על אותו ענף, נרשם ולא הוסתר

בזמן כתיבת הלילה הזה רץ **סוכן קוד שני באותו worktree ועל אותו ענף**, שכתב את
אותו תור אל תוך `docs/` במקום אל השורש, וקומיטים שלו משתלבים בהיסטוריה:

```
c9534ff11 docs(checkout): ...          -> docs/ARCHITECTURE-CHECKOUT-CARDCOM-E2E.md
                                          migrations/pending/006-payment-events.sql
4dbc2324d docs(orders): ...            -> docs/ARCHITECTURE-ORDER-STATE-MACHINE.md
                                          migrations/pending/007-order-transition-guard.sql
```

**מה זה עלה בפועל:** ‏`git add -A` של הסוכן השני קלט קובץ שלי שעוד לא נוסף,
ולכן `ARCHITECTURE-SEARCH-DISCOVERY.md` נכנס לקומיט שלו במקום לשלי. **התוכן
שלם, ההיסטוריה מטעה.** זה בדיוק הנזק שהכלל "סוכן קוד שני על אותו repo" קיים
כדי למנוע.

**המלצה:** להריץ סוכן שני ב-worktree נפרד ועל ענף נפרד, לא באותו אחד. שני
מסמכים באותו שם תחת שתי תיקיות הם עבודה כפולה שצריך למזג ידנית.

**הוכרע 2026-08-19:** הכפילות ב-`payment_events` נסגרה. ‏`120_payment_events.sql`
נשמר, ‏`006-payment-events.sql` נמחק. ארבעה דברים שהיו ב-006 ולא ב-120 **קופלו
פנימה ולא אבדו**: העמודה `stage` (אותו token ש-`capturePaymentAlarm` מתייג בו את
Sentry, כדי שאזעקה תתחבר לשורות שגרמו לה), ‏`external_event_id`, ‏`provider`,
ואוצר מילים רחב יותר של אירועים. מה ש**לא** נלקח מ-006 הוא הגישה של `text` +
‏CHECK לסוג האירוע: enum אוכף את אותה קבוצה סגורה, זול יותר להשוואה, ומופיע
בטיפוסים המיוצרים. שתי ההפניות ל-006 ב-`docs/` הופנו מחדש ל-120.

---

## 1. המודל העסקי המחייב

הסעיף הזה דורס כל מסמך ישן. שבע שורות.

| כלל | ניסוח מחייב |
|---|---|
| **קופון** | האדמין קובע `coupon_price`, **סכום מוחלט** בדף המוצר. הלקוח משלם אותו **במלואו באתר** דרך Cardcom. |
| **יתרת הקופון** | נגבית **בבית העסק בזמן הסריקה**, ולעולם לא נכנסת לפלטפורמה. |
| **התחשבנות קופון** | **‏100% מהחיוב באתר נשאר בפלטפורמה.** אין Escrow, אין החזקה, אין שחרור לספק בזמן מימוש. |
| **חיי קופון** | אחרי סריקה מוצלחת הקופון **פג לצמיתות**. אין סריקה שנייה ואין מימוש חלקי. |
| **פיזי** | תשלום מלא באתר, ואז **פיצול מיידי** לפי `platform_percent` פר-מוצר, מצולם ל-`order_items` ובלתי משתנה. |
| **כסף** | **אגורות שלמות, integer בלבד**, בכל שכבה, כולל בגבול מול Cardcom. |
| **ארנק** | **פנימי בלבד.** קאשבק קונה סחורה כאן. לא יוצא החוצה כמזומן. |

### 1.1 שני מסקנות שממשיכים לטעות בהן

1. **‏`platform_percent` בשורת קופון הוא דיווח, לא תמחור.** הלקוח לא רואה אותו,
   ואף שקל לא זז לפיו. הוא מצולם כדי שהדוח ישרוד עריכה של המוצר.
2. **‏`face_value` של קופון אינו הכנסה.** הפלטפורמה אף פעם לא מחזיקה אותו. הוא
   מופיע על השובר כדי שהלקוח והקופאי יסכימו כמה השובר שווה בקופה.

### 1.2 כללים תפעוליים נלווים

- **‏Checkout:** עגלת אורח פתוחה. התחברות (Google OAuth) חובה **רק בתשלום**.
- **אין `tenant_id`.** ‏RLS לפי `auth.uid()` ולפי תפקיד בלבד.
- **התראות:** ‏Supabase Trigger + Edge Function + Resend בלבד. **אסור Make/Zapier.**
- **כל דף מוצר מציג פרטי ספק:** שם, כתובת, טלפון.
- **תיאור מוצר: שדה אחד**, `description_he`. ‏`short_description_he` הוא תקציר
  עם תקרת אורך, לא תיאור שני.
- **‏`offer_valid_until` פג אוטומטית ומוצג ללקוח**, בכל מקום שבו ההצעה מוצגת.
- **עברית RTL בכל ה-UI.**

---

## 2. מפת המערכת

```
                       ┌──────────────────────────────────────┐
                       │            Next.js 16 App            │
                       │      App Router · Server Actions     │
                       └───┬──────────┬──────────┬────────────┘
                           │          │          │
              ┌────────────┘          │          └────────────┐
              ▼                       ▼                       ▼
      ┌───────────────┐      ┌────────────────┐      ┌────────────────┐
      │   Storefront  │      │     Admin      │      │ Supplier portal│
      │  guest cart   │      │ product form   │      │  scan · payouts│
      │  PDP · search │      │ orders · refund│      │  roles: owner/ │
      │  checkout     │      │ visibility     │      │  manager/scanner│
      └───┬───────────┘      └───────┬────────┘      └───────┬────────┘
          │                          │                       │
          └──────────────┬───────────┴───────────┬───────────┘
                         ▼                       ▼
              ┌────────────────────┐   ┌────────────────────┐
              │  Supabase Postgres │   │   Server Actions   │
              │  RLS · RPC · enums │   │  money in agorot   │
              └───┬────────────┬───┘   └─────────┬──────────┘
                  │            │                 │
   ┌──────────────┘            │                 └──────────────┐
   ▼                           ▼                                ▼
┌──────────┐        ┌────────────────────┐            ┌──────────────────┐
│ Cardcom  │        │ Supabase Trigger   │            │  Meilisearch     │
│ LowProfile         │  + Edge Function  │            │  via QStash      │
│ /Interface│───┐    │  + Resend         │            │  (+ outbox, 122) │
│  *.aspx  │   │    └────────────────────┘            └──────────────────┘
└──────────┘   │
               │ IndicatorUrl (unsigned!)
               ▼
     ┌──────────────────────────────┐        ┌───────────────┐
     │ /api/payments/cardcom/webhook│───────▶│  Cloudflare R2│
     │  ?s= secret + GetLpResult    │        │  product media│
     └──────────────┬───────────────┘        └───────────────┘
                    ▼
            ┌───────────────┐        ┌──────────────────────────┐
            │ finalizeOrder │───────▶│ Sentry (record)          │
            │  idempotent   │        │ ntfy.sh (interrupt, x5)  │
            └───────────────┘        │ Vercel Analytics + Speed │
                                     └──────────────────────────┘
```

### 2.1 העיקרון שמחזיק את כל התרשים

**מה ש-Cardcom שולח אף פעם לא נאמן.** ‏Cardcom **לא חותם** על ה-callbacks שלו.
האותנטיות נשענת על שני דברים ולעולם לא על גוף ה-POST:

1. סוד בלתי ניתן לניחוש ב-URL (`?s=`), בהשוואת זמן קבוע, מול **שני** סודות,
   **בלי short circuit** על ההתאמה הראשונה.
2. **אימות שרת-לשרת חובה** ב-`GetLpResult`, שהוא המקור היחיד המהימן לסכום,
   לסטטוס ולטוקן.

---

## 3. איך המסמכים מתחברים

```
                     MASTER-ARCHITECTURE-v3  (זה)
                                │
        ┌───────────────┬───────┴────────┬────────────────┐
        ▼               ▼                ▼                ▼
   CHECKOUT-       ORDER-STATE-      REFUNDS-        SECURITY-
   CARDCOM-E2E ──▶ MACHINE      ──▶  CANCELLATIONS   HARDENING
        │               │                │                │
        │               └────────────────┴────────────────┤
        │                                                 │
        ▼                                                 ▼
   ADMIN-PRODUCT-FORM ──▶ SEARCH-DISCOVERY ──▶ GEO-LOCATION
        │                        │
        ▼                        ▼
   WP-IMPORT-PIPELINE      OBSERVABILITY
```

קריאה כמשפט: **הטופס מגדיר מה קיים, ה-checkout מגדיר מה נגבה, מכונות המצב
מגדירות מה מותר לזוז, ההחזרים מגדירים איך מפרקים, והאבטחה והניטור עוטפים את
כולם.**

### 3.1 תלויות אמיתיות בין רכיבים

| רכיב | תלוי ב | למה |
|---|---|---|
| checkout | טופס המוצר | ‏`platform_percent` חסר = אי אפשר לתמחר שורה |
| הנפקת שובר | טופס המוצר | ‏`coupon_expiry_days` ו-`offer_valid_until` קובעים `expires_at` |
| החזרים | מכונות המצב | ‏`redeemed` סופי, ולכן החזר לכרטיס אסור |
| החזרים | טופס המוצר | חלון הביטול לשירות עם תאריך דורש שדה שלא קיים |
| חיפוש | טופס המוצר | מסמך האינדקס נבנה מהשדות שלו |
| geo | טופס המוצר | ‏`city` ו-`lat/lng` הם שדות בטופס |
| ייבוא WP | טופס המוצר | מה שאין לו מקור נשאר NULL, והמוצר נשאר draft |
| ניטור | הכל | ‏SLO 1 נמדד על `payment_webhook_events` |
| אבטחה | הכל | ‏RLS על כל טבלה שכל רכיב מוסיף |

---

## 4. המיגרציות הממתינות, בסדר הרצה

### 4.1 מה כבר הוחל, ומה חסר בו

| קובץ | מצב |
|---|---|
| `111_revoke_anon_writes` | **הוחל** 2026-08-10. פער שנשאר: default privileges של `supabase_admin` לא נסגרו (42501). ראה §4.4 |
| `112_product_fields_phase2` | **הוחל.** הטיפוסים המיוצרים לא רועננו מאז |
| `113_products_geo` | **הוחל בצורה מצומצמת.** העמודות, שלושת ה-CHECK ו-`products_city_idx` נחתו. ‏`cube`/`earthdistance` וה-GiST **לא** |
| `114`-`118` | הוחלו |

### 4.2 סדר ההרצה המומלץ לממתינות

**כל אחת מהן דורשת אישור מפורש של אופיר ו-MCP `apply_migration`. לעולם לא `db push`.**

| # | קובץ | תלוי ב | למה בסדר הזה |
|---|---|---|---|
| 0 | `113` §3 בלבד, בחיבור מורשה | - | יוצר `cube` + `earthdistance`. כל אינדקס מרחק אחר מחכה לזה |
| 1 | `PENDING-money-integer-fix.sql` | **קוד** | ‏55 קבצים עדיין קוראים את השמות הישנים. **אסור להריץ לפני שנכתב ענף הקוד ל-agorot** |
| 2 | `120_payment_events.sql` (טיוטה) | - | אין תלות. סוגר את הפער "אומת, ואז finalize נכשל" |
| 3 | `121_refunds.sql` (טיוטה) | - | מתחיל את שעון ה-14 יום. חובה לפני השקה מסחרית |
| 4 | `122_search_index_outbox.sql` (טיוטה) | - | סוגר webhook שאובד |
| 4.5 | `124_order_items_delivered_at.sql` (טיוטה) | - | **חוסם השקה מסחרית יחד עם (3):** בלעדיו חלון הביטול נמדד מ-`paid_at` |
| 5 | `PENDING-110-supplier-coordinates.sql` | 0 | קואורדינטות לספק |
| 6 | `123_supplier_branches.sql` (טיוטה) | 0, 5 | סניפים. ה-GiST שלו מוער עד ש-0 רץ |
| 7 | `PENDING-109-recurring-subscriptions.sql` | - | הטופס כבר מציע "חיוב חודשי קבוע" ונכשל בלעדיה |

**‏(1) הוא היחיד שמסוכן להריץ מוקדם.** כל השאר הן תוספות בלבד.

### 4.3 טיוטות שנכתבו הלילה

ארבע, כולן ב-`migrations/pending/`, **אף אחת לא הורצה**:

| קובץ | מה הוא | מסמך מקור |
|---|---|---|
| `120_payment_events.sql` | יומן append-only לחיי תשלום, נפרד מ-`payment_webhook_events` | ‏#1 §10 |
| `121_refunds.sql` | הודעת הביטול והכרעתה, נפרדת מ-`payments` | ‏#3 §5 |
| `122_search_index_outbox.sql` | outbox באותה טרנזקציה כמו שינוי המוצר | ‏#5 §7.4 |
| `123_supplier_branches.sql` | סניפים של ספק, בלי כסף ובלי הרשאה | ‏#6 §8 |
| `124_order_items_delivered_at.sql` | ‏`shipped_at` + `delivered_at` + פונקציית מועד אחרון לביטול | ‏#3 §6, בהחלטת אופיר |

**הכפילות הוכרעה 2026-08-19:** ‏`006-payment-events.sql` נמחק, ‏`120` נשמר,
והטוב שבו קופל פנימה (§0.3). ‏`007-order-transition-guard.sql` של הסוכן השני
**נשאר** ולא נבדק לעומק כאן: הוא רץ על מכונת המצב ולא על `payment_events`, ולכן
אינו כפילות. **צריך לעבור עליו לפני הרצה.**

### 4.4 הפער שנשאר ב-111, ומה הוא אומר בפועל

```
pg_default_acl for schema public, אחרי ההחלה:
  for postgres        -> anon=rm/postgres              (טוב)
  for supabase_admin  -> anon=arwdDxtm/supabase_admin  (לא השתנה)
```

תפקיד יכול לשנות רק את ה-default privileges **של עצמו**, ו-MCP מתחבר כ-`postgres`.
**המשמעות: טבלה שנוצרת מה-dashboard או מה-CLI עדיין מקבלת `anon` עם ההרשאה
המלאה, ואף אחד לא מדווח על זה.**

**כלל תפעולי עד שיהיה חיבור מורשה:** אחרי כל יצירת טבלה מה-dashboard, להריץ
מחדש את סעיף 2 של `111`. ועדיף פשוט ליצור טבלאות רק דרך מיגרציות.

---

## 5. שינויי קוד שהלילה הזה זיהה ולא ביצע

הענף הזה **לא נוגע ב-`src/`**. כל אחד מאלה נמצא במדידה, ורשום כאן כדי שיבוצע
בענף קוד.

| # | שינוי | חומרה | מקור |
|---|---|---|---|
| 1 | ה-docstring של `src/server/domain/orders/state-machine.ts` עדיין מתאר את מסלול ה-Escrow, בזמן שטבלת `TRANSITIONS` שמתחתיו לא מכילה אותו | **תיעוד מטעה בקובץ הכי רגיש** | #2 §2.1 |
| 2 | `service` נמצא ב-enum ולא בטופס; `recurring` בטופס ולא ב-enum | קופון שירות אי אפשר ליצור בכלל | #4 §1 |
| 3 | לרענן את `src/types/database.ts` ולמחוק את `src/lib/admin/product-fields.ts` | חמש עמודות נקראות דרך עוקפים | #4 §2.5 |
| 4 | **מעבר ל-Upstash Redis** ב-rate limiting, ו-**fail closed** בסריקת שובר וב-checkout | היום המגביל יושב על אותו Postgres שהוא מגן עליו, ונכשל פתוח. **הוכרע ע"י אופיר 2026-08-19** | #9 §1.5 |
| 5 | nonce ב-`src/proxy.ts` במקום `unsafe-inline` ב-CSP | **החשיפה הגדולה ביותר ל-XSS** | #9 §7.3 |
| 6 | טריגר `BEFORE UPDATE OR DELETE` על `audit_log` | מסלול ביקורת שאפשר לערוך אינו מסלול ביקורת | #2 §8.3 |
| 7 | `offer_valid_until` כפילטר באינדקס החיפוש | הצעה שפגה נשארת בחיפוש | #5 §9 |
| 8 | לחתום `shipped_at`/`delivered_at` בפעולות המשלוח של הספק | **הסכימה כבר בטיוטה** (`124`). מה שנשאר הוא צד היישום: בלי חתימה העמודה נשארת NULL והחלון פתוח לנצח | #3 §6 |
| 9 | תאריך שירות על `products` | חתכי היומיים והשבעה ימים חסרי אחיזה | #3 §6 |

**‏(8) הוא היחיד שהוא כשל ציות ולא חוב טכני**, והחצי הסכימתי שלו נסגר בטיוטה
‏`124`. הפונקציה `order_item_cancellation_deadline` מחזירה **NULL** לשורה פיזית
שטרם נמסרה, וכל קורא חייב לקרוא את זה כ"החלון עדיין פתוח" ולא כ"פג". אין backfill:
לאף הזמנה בעבר אין תיעוד מתי הגיעה, והמצאת תאריך היא המצאת תחילתו של שעון חוקי.

---

## 6. הפאזות עד production

### פאזה A: מה שחוסם, ואי אפשר לעקוף

| # | משימה | חוסם | מי |
|---|---|---|---|
| A1 | ‏8 סודות ב-Vercel (production scope) | תשלום, קופונים, מיילים, **וכל ה-cron** | אופיר |
| A2 | credentials של Cardcom production | תשלום | אופיר |
| A3 | אימות דומיין ב-Resend | שובר לא מגיע ללקוח | אופיר |
| A4 | **לאמת איזה ענף Vercel מתייחס אליו כפרודקשן** | היום זה `cursor/add-supabase-3c830`, לא `main` | אופיר |
| A5 | ‏branch protection שאוכף | כל push בריצה האחרונה הדפיס `Bypassed rule violations` | אופיר |

`CRON_SECRET` הוא השקט מבין השמונה: שום דבר לא נופל, פשוט **אף job לא רץ** לעולם.

### פאזה B: קטלוג

| # | משימה | חוסם |
|---|---|---|
| B1 | להעלות את 66 התמונות ל-R2 | **חייב להסתיים לפני ה-DNS cutover**, כי היום התמונות מצביעות על הוורדפרס הישן |
| B2 | להכריע ב-5 ה-slugs שסותרים את הכותרת | פרסום כזה שם לקוח בכתובת שסותרת את התוכן |
| B3 | לקבוע עמלה וספק ל-19 הטיוטות | **ידנית, פר מוצר. אין ברירת מחדל להחיל** |
| B4 | לקטלג 13 מהן | |
| B5 | להכריע ב-34 שורות הדמו | מחיקה היא עצירה לפי הכלל |
| B6 | reindex ל-Meilisearch | |

### פאזה C: תשלום מקצה לקצה

| # | משימה |
|---|---|
| C1 | עסקת בדיקה אמיתית מול Cardcom production |
| C2 | לאמת שה-webhook מגיע, ושה-`?s=` מתאים בשני הצדדים |
| C3 | לאמת ש-`GetLpResult` מחזיר את אותו סכום |
| C4 | לאמת שהשובר הונפק, שה-QR נסרק, ושסריקה שנייה נכשלת |
| C5 | לאמת החזר, כולל `CancelOnly` באותו יום סליקה |
| C6 | לאמת שה-reconciliation היומי מוצא deal שהוזרק ידנית |

### פאזה D: ציות

| # | משימה |
|---|---|
| D1 | חתימת עורך דין על שש השאלות ב-#3 §1.6 |
| D2 | ממשק ביטול מקוון בהזמנה, שרושם את ההודעה **מיד** |
| D3 | `121_refunds` מוחלת, כי בלעדיה שעון ה-14 יום לא נמדד |
| D4 | טקסט הביטול והעמלה מוצג לפני כפתור התשלום |
| D5 | `offer_valid_until` מוצג בכל מקום שבו ההצעה מוצגת |

### פאזה E: DNS

| # | משימה |
|---|---|
| E1 | **‏B1 הושלם** |
| E2 | ‏redirect coverage 103/103 |
| E3 | ‏cutover |
| E4 | לוודא שהתמונות עדיין נטענות אחרי ה-cutover |

**‏E1 הוא הקשר האמיתי בין הפאזות:** ‏DNS לפני העלאת המדיה שובר כל תמונה בקטלוג.

---

## 7. צ'קליסט עלייה לאוויר

```
[ ] 8 סודות ב-Vercel production                      A1
[ ] Cardcom production credentials                    A2
[ ] Resend domain verified                            A3
[ ] הענף שוורסל מגדיר כפרודקשן אומת                  A4
[ ] branch protection אוכף                            A5
[ ] E2E אינו required check עד שיש CI_SUPABASE_URL
[ ] 66 תמונות ב-R2                                    B1
[ ] 5 slugs הוכרעו                                    B2
[ ] עמלה + ספק ל-19 הטיוטות                          B3
[ ] Meilisearch reindexed                             B6
[ ] עסקת בדיקה מקצה לקצה עברה                        C1-C6
[ ] 121_refunds מוחלת                                 D3
[ ] 124_order_items_delivered_at מוחלת + חתימה בקוד    D3
[ ] טקסט ביטול לפני כפתור התשלום                     D4
[ ] חתימת עורך דין                                    D1
[ ] redirect coverage 103/103                         E2
[ ] DNS cutover                                       E3
[ ] תמונות נטענות אחרי cutover                        E4
```

### 7.1 שערים שחייבים להיות ירוקים בכל commit

```
pnpm test          כל הטסטים
pnpm type-check    נקי
pnpm lint          נקי
pnpm build         שער נפרד. cacheComponents פוסל קריאות עמוד לא-מטמונות
                   שהטסטים, ה-type-check וה-lint כולם מעבירים
compare.mjs        מתחת ל-11%, או תקרה רשומה ב-pixel-gate.mjs עם המדידה
```

**‏`pnpm build` הוא שער נפרד ולא נגזרת של השלושה הראשונים.** זה נמדד.

---

## 8. חמש עשרה עובדות שנמדדו, שכדאי לא לגלות מחדש

1. **‏Cardcom לא חותם webhooks.** האותנטיות היא `?s=` ועוד `GetLpResult`.
2. **המסד הוא lineage pre-059.** העמודה היא `amount_ils`, לא `amount_agorot`.
   קריאה בשם הלא נכון מרימה 42703 ומפילה את כל ה-select.
3. **`supabase/migrations/` לא מתאר את הפרודקשן.** הטיפוסים המיוצרים כן.
4. **‏`IF NOT EXISTS` על טבלה אינו `IF NOT EXISTS` על עמודותיה.** ככה
   `refund_of_payment_id` לא נוצר, וכל החזר נכשל **אחרי** ש-Cardcom החזיר כסף.
5. **‏11 ספקים, 5 עם עיר, 0 עם כתובת. 80 מוצרים, 0 עם קואורדינטה.**
6. **‏PostGIS לא מותקן, וגם לא `cube`/`earthdistance`.** יצירת extension נופלת
   ב-42501 דרך MCP.
7. **‏`/_next/image` מגיש את המקור byte-for-byte כש-sharp לא מצליח לפענח**,
   בשקט, עם 200.
8. **‏Lighthouse LCP על localhost הוא סימולציה.** שיפור אמיתי של 2.7 שניות נראה
   שם כרעש.
9. **חיבור template literals עם `+` השחית build בפרודקשן**: המחרוזת שהוגשה
   הייתה JS שבור, עם 200 ובלי שורת לוג.
10. **‏`npm install` לא עובד בריפו הזה.** ‏pnpm בלבד, וזה קורס בתוך
    `buildIdealTree`, לפני כל hook.
11. **גלישה ל-127.0.0.1 מול שרת dev על localhost חוסמת Server Actions בשקט.**
12. **‏`next start` מקומי הוא `NODE_ENV=production`**, ולכן boot guards
    שמיועדים לפרודקשן משתקים את סוויטת ה-E2E על לפטופ.
13. **‏E2E חייב לרוץ מול `pnpm start`.** ריצה חשופה של playwright ממחזרת שרת dev
    ישן ומזייפת 13 כשלי עגלה.
14. **‏`auth.uid()` חשוף מוערך פעם לכל שורה.** ‏`(SELECT auth.uid())` הוא InitPlan.
15. **מפתח שירות תקין לחלוטין שאינו של הפרויקט** מחזיר 200 על add-to-cart, כותב
    ‏0 שורות, ולא מציג שגיאה.

---

## 9. מה v3 לא מכריע

- **‏3-D Secure.** בתוך העמוד המתארח של Cardcom.
- **ריבוי מטבעות.** ‏`orders.currency` תמיד `ILS`.
- **‏sub-merchants.** ספקים מקבלים דוח תשלום, לא חשבון סולק. זו הסיבה שאין Escrow.
- **תשלומים.** לא מוצעים.
- **מוצר רב-סניפי בחיפוש.** מסמך אחד לכל סניף שובר את ההנחה של מזהה אחד למוצר,
  ואי אפשר להכריע את זה בתוך מיגרציה.
- **מתי להוסיף Redis.** רק אחרי מדידה שמראה שהמגביל הנוכחי הוא צוואר בקבוק.

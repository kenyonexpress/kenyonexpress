# ‏אופיר, זה מה שקרה בזמן שלא היית

‏עודכן: ‏19.08.2026, ‏12:10. ענף `phase5/homepage`, ‏HEAD `b538b4b35`.
פרויקט Supabase: ‏`ixvwfbuvfxxsjiywhbbb`.

## ‏בשורה אחת

**הקוד בשל ועובר את כל השערים. מה שחוסם השקה מסחרית הוא ארבעה דברים שרק אתה
יכול לתת: מפתחות Cardcom, מפתח Supabase תקין, תוכן אמיתי, ו-DNS.** אין באג
פתוח שחוסם.

## ‏שים לב לתג `v1.0.0`, הוא מטעה

**התג `v1.0.0` כבר קיים ברשימה, מ-10.08, והוא מקדים את כל מה שכתוב כאן.**
הוא מצביע על קומיט עם ‏1990 טסטים (היום ‏2825) וההודעה שלו עצמה אומרת
‏"NOT a production-ready deploy". **אל תקרא אותו כ"הפרויקט הושלם".** התג
העדכני והנכון הוא **`v1.0.0-rc2`**, והוא זה שמתאר את המצב במסמך הזה.
‏`v1.0.0` אמיתי לא ייווצר לפני שתכריע בארבעת הסעיפים למטה.

## ‏השערים, נכון לעכשיו

| שער | תוצאה |
| --- | --- |
| `tsc --noEmit` | ‏✅ 0 שגיאות, **אחרי תיקון של 2 שנכנסו ב-12:03** |
| `biome lint .` | ‏✅ 0, ‏932 קבצים |
| `vitest run` | ‏✅ **2825/2825** ב-205 קבצים |
| `pnpm build` | ‏✅ קוד יציאה 0 |
| מסלולי חזית | ‏✅ **21/21** מחזירים 200 עם נתונים אמיתיים |
| ‏advisors ביצועים ב-Supabase | ‏✅ **0 WARN** |
| ‏Lighthouse נגישות | ‏✅ **‏96 ומעלה**, ושלושה מסלולים על **‏100** |
| ‏axe WCAG 2.1 A+AA | ‏✅ **‏0 הפרות** על כל מסלול ציבורי, כולל דפי מוצר וצ'ק-אאוט |
| גלישה אופקית ב-320px | ‏✅ ‏19 מסלולים, ‏4 רוחבים, ‏0 גולשים |
| ‏CLS בדף החיפוש | ‏✅ ‏**‏0.029** (היה 0.350) |
| ‏E2E Playwright | ‏✅ **‏245/248, ‏0 כשלים, שתי הרצות.** ‏3 מדולגים = מסלול התשלום, חסום על מפתח Supabase |

## ‏סבב איכות מאוחר יותר באותו יום: ‏12 באגים אמיתיים, כולם נמדדו ונסגרו

המסמך הזה אמר "אין באג פתוח שחוסם" ב-12:10. זה היה נכון לגבי מה שנבדק עד אז.
מה שלא נבדק החזיק שנים עשר, ואחד מהם `critical`.

**‏1. הקופה: ארבעה כפתורים בלי שום שם נגיש, בטלפון.** מחוון השלבים מסתיר את
התוויות מתחת ל-560px, ההסתרה הייתה `display: none` שמוציאה אותן גם מעץ הנגישות,
והמספר שלידן `aria-hidden`. **קורא מסך הכריז "כפתור" ותו לא, בקופה.**

**‏2. כפתור הקנייה בדף המוצר היה לבן על כתום ביחס 3.21:1**, מול דרישה של 4.5.
זה הפקד המרכזי של כל דף מוצר. **כל 12 דפי המוצר שנסרקו נכשלו**, כי דף המוצר
כלל לא היה בשער הנגישות: לשער היו מסלולים בנתיב קבוע בלבד, וה-slug כאן עברי
ומה-DB.

**‏3. צהוב המותג שימש כצבע טקסט, ביחס 1.41:1.** קישורי ההרשמה ושכחתי סיסמה,
ו**המחיר של הקופון עצמו**. הכלל ההפוך, "לבן על צהוב", היה בשער מאז 29.07; אותו
צבע קרוא הפוך לא נבדק על ידי אף אחד.

**‏4. שני דפים גלשו אופקית בטלפון של 320px** ורק שם: הבית ב-6px ודף הקטגוריה
ב-5px. הבדיקות הקיימות רצות ב-390 וב-412.

**‏5. ‏CLS של דף החיפוש ירד מ-0.350 ל-0.029** והציון מ-71 ל-89.

**מה שכדאי שתיקח מזה:** בכל אחד מהמקרים **היקף הבדיקה היה הבאג ולא הקוד.** שער
הנגישות כיסה שישה מסלולים וכל השישה היו ירוקים; עשרה מתוך שלושה עשר שלא היו בו
נכשלו. עכשיו כל מסלול ציבורי בשער, בשני ויופורטים, ‏**וכל תיקון נבדק שהשער נופל
בלעדיו.**

פירוט מלא: `docs/A11Y-SWEEP-REPORT.md`, ‏`docs/LAUNCH-READINESS.md` סעיף 9,
‏`docs/E2E-MEASURED.md`, ‏`docs/PIXEL-WAVE-REPORT.md`.

## ‏שני דברים שנמצאו היום ושווה שתדע עליהם

### ‏1. באג חי בפרודקשן, תוקן: חצי ממערכת ההתראות הייתה מתה

‏ה-CHECK בטבלת תור ההודעות אישר ‏5 סוגי הודעה. הקוד שולח ‏10. חמש מהן נדחו
בשקט בכל פעם, וביניהן:

- ‏**מייל הקאשבק, בתוך מסלול השלמת התשלום.**
- ‏**התראת פער ההתאמה** בין הרישום שלנו לדוח המסוף, כלומר ההתראה שכל תפקידה
  הוא להודיע לאדם שהכסף לא מסתדר.
- תזכורת לפני פקיעת קופון, התראת מלאי נמוך, התראת חשבונית תקועה.

תוקן במיגרציה 121, ונוסף טסט ששומר על ההסכמה בין ה-DB, הקוד ששולח, והקוד
שמרנדר. **זה לא היה נתפס בלי הסריקה הזאת**, כי שום טסט לא הצליב את שלוש
הרשימות.

### ‏2. ‏13 אזהרות ביצועים ב-DB נסגרו, ובדרך כמעט נשברה החנות

הנימוק שהיה רשום להן במסמך קודם היה שגוי, והמיזוג הראשון שלי לפיו **הפיל את
קריאת הקטלוג לכל מבקר לא מחובר**. בדיקה תפסה את זה לפני שזה נשאר, והתיקון
פוצל לפי תפקיד. הפרטים המלאים בנספח (26) ב-`docs/LAUNCH-READINESS.md`.

**מה שחשוב לך מזה:** אימות עם משתמש אמיתי, ולא הנחה, הוא מה שהציל. אנונימי
רואה ‏61 מוצרים ו-0 פרופילים. לקוח מחובר רואה את הפרופיל של עצמו ו-0 אחרים.

## ‏ממצא אבטחה חדש, ‏19.08 15:30: אפשר לנעול ספק מבחוץ, בלי להתחבר

**זה לא היה ברשימה עד עכשיו, וזו הסיבה שהוא כאן ולא בתוך הרשימה למטה.**

‏`check_rate_limit(p_key, p_max_attempts, p_window_seconds)` היא `SECURITY
DEFINER`, **ויש לה grant ל-`anon`** — כלומר כל אחד יכול לקרוא לה ישירות
ב-`/rest/v1/rpc/check_rate_limit` עם המפתח הפומבי, בלי משתמש. המיגרציה 103
השאירה את ה-grant בכוונה, עם הנימוק "server actions של אורח קוראים לה תחת
‏anon", והנימוק נכון. **מה שלא נבדק הוא ש-`p_key` מגיע מהקורא.**

הפונקציה **מגדילה מונה** למפתח שנמסר לה. והמפתחות בקוד צפויים לחלוטין:

```
staff-pin:${userId}          15 לשעה   src/app/api/supplier/app/pin/route.ts
voucher-redeem:${userId}    120 לשעה   .../vouchers/redeem-batch/route.ts
redeem:${ip}                 60 לשעה   src/app/redeem/[token]/page.tsx
app-session:${ip}            30 ל-10 דק'
search:${ip}                120 ל-5 דק'
```

**התקיפה:** לקרוא ל-RPC 15 פעמים עם `staff-pin:<uuid של הספק>`. מהרגע הזה,
במשך שעה, **הספק האמיתי לא יכול להזין PIN בקופה**, כי השרת קורא לאותו מונה
ומקבל `false`. אותו דבר עם `voucher-redeem:` — **חסימת מימוש שוברים בקופה**.
תוקף לא צריך סיסמה, לא צריך משתמש, ולא צריך יותר מ-uuid שאפשר לנחש או לדלות.
בנוסף אפשר להזרים שורות ל-`rate_limits` עם מפתחות שרירותיים, בלי חסם.

**‏`fn_record_recent_search` נבדקה גם היא ו-אינה בעיה:** היא עושה
‏`IF v_user IS NULL THEN RETURN`, כלומר קריאה אנונימית לא כותבת כלום.

**התיקון, ולמה לא ביצעתי אותו לבד:** הנכון הוא שה-server actions יקראו
ל-`check_rate_limit` דרך ה-service role, ואז `REVOKE EXECUTE` מ-`anon`
ומ-`authenticated`. זה שינוי קוד **ועוד DDL על פרודקשן**, וה-`REVOKE` לבדו,
בלי חצי הקוד, **מפיל את הגבלת הקצב של חיפוש אורח ושל מימוש שובר בכל האתר.**
המפתח המקומי ב-`.env.local` אינו של הפרויקט הזה, ולכן **אין לי דרך לבדוק את
חצי הקוד לפני שהוא רץ.** שינוי לא נבדק במסלול הזה מסוכן יותר מהממצא עצמו.

**עוד על השורה "advisors 0 WARN" בטבלה למעלה:** היא נכונה ל-**advisors של
ביצועים** (‏131 lints, כולם INFO, ‏130 מהם `unused_index`, צפוי בלי תנועה).
‏**advisors של אבטחה מחזירים 23 WARN**, כמעט כולם `SECURITY DEFINER` שניתן
לקרוא לו — ורובם הכרחיים, כי `is_admin()`, ‏`has_role()` ו-`current_user_role()`
**חייבים** להיות ניתנים להרצה על ידי `authenticated` אחרת כל מדיניות RLS
שמשתמשת בהם נשברת. ‏`check_rate_limit` הוא היחיד מביניהם עם תופעת לוואי
שתוקף שולט בה.

## ‏שאלות פתוחות, לפי סדר דחיפות

1. **מפתחות Cardcom של פרודקשן.** הכל רץ ב-SANDBOX. בלי זה אי אפשר לגבות שקל.
2. **‏`SUPABASE_SECRET_KEY`.** המפתח ב-`.env.local` **אינו של הפרויקט הזה**
   (‏`iss=supabase-demo`). כל נתיב admin מקומי נכשל בגללו. צריך גם לוודא שהוא
   תקין במשתני הסביבה של Vercel.
3. **איזה סט תקנון מחייב את החברה?** יש באתר **שני סטים שלמים** של מסמכים
   משפטיים. הישן (`/terms-and-conditions`) הוא זה שה-footer מקשר אליו והוא
   האינדקסבילי. החדש (`/legal/*`) כתוב טוב יותר, לפי תיקון 13 ולפי מודל
   הקופון בלי Escrow, ואף אחד לא מקשר אליו והוא `noindex`. **מדדתי שהם
   מסכימים על דמי הביטול (5%, תקרה 100 ש"ח)**, אז אין סתירה היום, אבל זו
   החלטה שלך ושל עורך דין ולא של סוכן.
4. ~~**‏`wallet_transactions`: אדמין יכול לעדכן ולמחוק שורות בספר החשבונות.**~~
   **נמדד מחדש ונסגר, וזו לא הייתה סתירה.** ‏`wallet_transactions` **אינו**
   ספר החשבונות: הוא הארנק החד-צידי המיושן ממיגרציה 006, ‏**‏0 שורות**, בלי
   שום כותב בקוד, והקורא היחיד שנשאר הוא דף המשתמש באדמין. הספר שהאתר באמת
   מנהל הוא `wallet_entries` (דו-צידי, ‏`debit_account`/`credit_account`),
   ו**מדידה על הפרויקט המתארח מראה שיש לו מדיניות אחת בלבד, ‏SELECT** — כלומר
   ‏INSERT, ‏UPDATE ו-DELETE נדחים מ-`anon` ומ-`authenticated` כאחד, והכתיבה
   היחידה היא ב-service role ממסלול חזרת התשלום. **הוא append-only כנדרש ב-(19).**
   מה שבאמת חסר היה שער: הטסט שמחזיק שש טבלאות במצב הזה לא כלל אותו. נוסף,
   ‏**ונבדק שהוא נופל** בהזרקת מדיניות UPDATE. אין כאן החלטה שממתינה לך.
5. **תוכן אמיתי.** ‏0 ספקים עם כתובת רחוב, ‏5 מתוך 11 עם עיר. כפתור ה-Waze
   קיים בקוד ולא מופיע בפועל כי אין ממה לבנות אותו.
6. **אישור להריץ את המיגרציות הממתינות.** ‏12 קבצים ב-`migrations/pending/`
   ועוד 3 תחת `PENDING-` ב-`supabase/migrations/`.
7. **‏DNS cutover ו-push לפרודקשן ב-Vercel.** מצבי עצירה, לא בוצעו.

## ‏שלושת ה-paste-blocks הבאים

### ‏(א) אחרי שיש מפתחות Cardcom

```
[חוקי-המשך] עבוד ברצף בלי לעצור, אשר הכל אוטומטית. עצור רק אם: push לפרודקשן,
מחיקת DB/קבצים, migration בלי אישור, שני סוכני קוד על אותו repo.
[תור סגור]
1. החלף את מפתחות Cardcom ב-.env.local ובמשתני Vercel, בלי להדפיס אותם לשום לוג.
2. הרץ עסקת חיוב אחת מקצה לקצה מול הסביבה החדשה ותעד את מזהה העסקה.
3. הרץ זיכוי על אותה עסקה ואמת ששורת refund_completed נכנסה ל-notification_outbox.
4. עדכן docs/LAUNCH-READINESS.md עם הראיה.
5. תג v1.0.0-rc3 + דוח סיום.
[נקודת שמירה] אחרי כל שלב: commit + push + עדכן STATE.md עם "המשך מ:".
```

### ‏(ב) אחרי שיש תוכן אמיתי

```
[חוקי-המשך] עבוד ברצף בלי לעצור, אשר הכל אוטומטית. עצור רק ב-4 הקריטיים.
[תור סגור]
1. ייבא את הספקים האמיתיים עם כתובת רחוב ועיר, MCP apply_migration בלבד.
2. אמת שכפתור Waze מופיע בכל בלוק ספק, וצלם ראיה.
3. הרץ מחדש compare.mjs על category ועל products, שהיו חסומים על פער קטלוג.
4. הרץ מחדש Lighthouse על 11 המסלולים ועדכן docs/evidence/.
5. תג v1.0.0-rc4 + דוח סיום.
[נקודת שמירה] אחרי כל שלב: commit + push + עדכן STATE.md עם "המשך מ:".
```

### ‏(ג) אחרי שהכרעת איזה סט תקנון מחייב

```
[חוקי-המשך] עבוד ברצף בלי לעצור, אשר הכל אוטומטית. עצור רק ב-4 הקריטיים.
[תור סגור]
1. הסט הנבחר: <legal | ישן>. הפנה את SiteFooter ואת ה-alias-ים ב-next.config.ts אליו.
2. הסר את ה-noindex מהסט הנבחר והוסף 308 מהסט השני אליו.
3. עדכן את legal-routes.test.ts כך שייפול אם יופיע סט שלישי אינדקסבילי.
4. תג v1.0.0 + docs/PROJECT-COMPLETE.md.
[נקודת שמירה] אחרי כל שלב: commit + push + עדכן STATE.md עם "המשך מ:".
```

## ‏חסום — דורש את אופיר

- **‏(23) גל הפיקסלים, ארבעה דפים מעל שער ה-11%.** שלושה מהם (`category`,
  ‏`products`, ‏`search`) הם **פער קטלוג ולא פער CSS**: ‏2 מוצרים מול 13, ‏44 מול
  ‏61. הכלי מסרב להשוות אותם בכוונה. זה נסגר עם התוכן האמיתי ולא עם קוד.
  הרביעי (`checkout`, ‏12.43%) הוא אשף 4 שלבים מול טופס אחד באתר החי, כלומר
  החלטת UX ולא CSS. **‏`text-align: center` לכותרת נוסה, נמדד, ולא הזיז את
  המספר בכלל.**
- ~~**דף החיפוש, ‏CLS 0.350.**~~ **נסגר.** ‏CLS ירד ל-**0.029** והציון מ-71
  ל-**89**, שלוש הרצות Lighthouse יציבות. הייחוס ל-`<footer>` היה התסמין: ‏
  ‏`.category-page__body` הוא `display: block`, ולכן סרגל הסינון והפוטר יושבים
  מתחת לגריד ונגררים למעלה כשהוא נפתר קצר מה-placeholder. שלושה תיקונים, כל
  אחד נמדד: ‏placeholder של שתי שורות, שמירת הגובה כדי שתוצאות מעטות לא יכווצו
  אותו, ושתי שורות כותרת בטלפון, כי הכותרת היא השאילתה והיא נשברת שם. השער
  ‏`e2e/layout-stability.spec.ts` נבדק שהוא נופל בשני הכיוונים. פירוט:
  ‏`docs/LAUNCH-READINESS.md` סעיף 8.
- **מחיקת `src/components/home/Footer.tsx`**, קובץ מת עם 5 קישורים שבורים
  שאינו מרונדר משום מקום. מחיקת קבצים היא מצב עצירה, ולכן הוא נשאר.

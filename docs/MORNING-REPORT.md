# דוח בוקר

<!-- stale-banner:2026-09-01 -->
> ⚠️ **‏דוח יומי של 19.08.2026. סומן כמיושן ב-01.09.2026.**
>
> ‏עשר השאלות הפתוחות בראש המסמך אינן עשר יותר. ארבע מהן נסגרו מאז (דיפלוי חי,
> ‏127, ‏128, וקטלוג אמיתי בפרודקשן). מה שנשאר פתוח מרוכז ב-`docs/FINAL-REPORT.md`,
> שהוא המסמך שמתוחזק.


עודכן: 2026-08-19, סוף היום. ענף קוד קנוני: `phase5/homepage`.
מקור תור: `MISSION-FINAL.md`. תמונת מצב רחבה יותר: `OFIR-RETURN-BRIEF.md`.

הקובץ הזה נכתב כשלד בבוקר, כשעדיין לא נגעו בקוד. הוא מעודכן עכשיו למה שקרה
בפועל: **‏181 קומיטים ביום אחד, ותשעת שלבי `MISSION-FINAL` נסגרו או סומנו
חסומים בשם.** שאלות קריטיות תמיד בראש, כדי שלא יהיה צורך לדפדף בשביל חוסמים.

---

## 0. שאלות קריטיות (למעלה, לא בסוף)

פירוט מלא: `docs/QUESTIONS-FOR-OFIR.md`. צ'קליסט עלייה: `docs/LAUNCH-CHECKLIST.md`.

אלה עשר השאלות שאי אפשר לעקוף בקוד. בלי תשובה אין כסף אמיתי. **אף אחת מהן לא
נפתרה במהלך היום, כי לאף אחת מהן אין פתרון בקוד.**

1. **מסוף Cardcom ייצור + ארבעה סודות ב-Vercel Production.** בלי זה אין גבייה ואין שובר. בפרודקשן נמדדו 0 שוברים ו-0 אירועי סליקה.
2. **מפתח Resend תקין + SPF/DKIM ירוקים על `@kenyonexpress.co.il`.** המפתח שנבדק החזיר 400. בלי מייל אין מסמך גילוי ואין קופון אצל הלקוח.
3. **מתי DNS cutover.** הדומיין מגיש עכשיו WordPress דרך Cloudflare (200). לא לנתק לפני (1)+(2) ועסקת smoke.
4. **Production Branch של Vercel הוא `cursor/add-supabase-3c830`, לא `main`.** Push ל-`main` לא משנה את החי.
5. **Hobby מול Pro:** ‏**עשרה** ‏cron ב-`vercel.json`, ‏**חמישה מהם תת-שעתיים**
   (`notifications` ו-`health` כל 5 דקות; ‏`invoices`, ‏`stock` ו-`stranded-payments`
   כל 10). נמדד 20.08. הספרה "שישה, שניים כל חמש דקות" שהופיעה כאן קודם הייתה
   נמוכה מדי, וזה משנה את התשובה ולא רק את הניסוח: ‏**Hobby מתיר שני cron בלבד
   ובתדירות יומית**, ולכן ‏Hobby אינו מספיק כאן, נקודה. אין כאן שיקול דעת.
6. **סיווג הקופון מול 14ח (עו"ד):** 5 שנים לתו קנייה מול 4 חודשים + ארנק. זמנית: 4 חודשים + C6.
7. **דמי ביטול ב-soft-launch:** לגבות 5%/100 או אפס. זמנית: אפס, והעמוד חייב להגיד את זה.
8. **מע"מ 17% ב-ledger מול 18% בחשבוניות.** חשבונית ראשונה תהיה שגויה עד תיקון קוד.
9. **ח.פ, כתובת, רכז נגישות, חתימת עו"ד** על תקנון/פרטיות/ביטולים.
10. **11 ספקים בלי כתובת ובלי לוגו.** אי אפשר לממש קופון, ואי אפשר לשמור מוצר פעיל באדמין.

**שאלה 0, והיא היחידה מהלילה שדורשת ממך פעולה: הוסף שני
repository secrets.** ‏`CI_SUPABASE_URL` ו-`CI_SUPABASE_ANON_KEY` אינם מוגדרים
בריפו כלל (`gh secret list` ריק), ולכן `job` ה-Build ב-CI נופל על
‏`Missing NEXT_PUBLIC_SUPABASE_URL / NEXT_PUBLIC_SUPABASE_ANON_KEY` בשלב
איסוף נתוני העמודים. בעשרים הריצות המושלמות האחרונות הוא הצליח **אפס פעמים**.
שני הערכים האלה ציבוריים מעצם טבעם (מפתח anon נשלח לדפדפן תחת `NEXT_PUBLIC_`),
ולכן אין בהם חשיפה. **אבל שים לב:** ‏job ה-Build מפנה גם ל-`CI_SUPABASE_SECRET_KEY`,
שהוא מפתח service_role אמיתי, **והריפו ציבורי** — זו החלטת אבטחה שלך ולא
פורמליות. ייתכן שערך דמה מספיק, כי `createAdminClient` זורק רק על מפתח **חסר**
ומזהיר בלבד על מפתח שגוי; לא הצלחתי לבדוק את זה כי אי אפשר להריץ בילד כאן.
פירוט מלא: פריט 8 ב-`docs/PROJECT-COMPLETE.md`.

**שאלה 11, חדשה מהיום:** איזה סט מסמכים משפטיים מחייב, `/legal/*` או
`/terms-and-conditions` + `/privacy-policy`? שניהם מוגשים, שניהם מסכימים היום על
דמי הביטול (5%, תקרה 100), והחדש מביניהם מסומן `noindex` כדי שלא יופיעו שני
סטים בגוגל. ההכרעה היא של עו"ד. מתועד ב-`src/app/(legal)/layout.tsx`.

**שאלה 12, חדשה מהיום:** הנקודות של סליידר דף הבית. ראה שלב 8 למטה.

**חסם סביבה שחזר בכל שלב היום:** `SUPABASE_SECRET_KEY` המקומי הוא מפתח ההדגמה
הסטנדרטי (`iss=supabase-demo`), והפרויקט המארח דוחה אותו. כל מסלול שעובר דרך
admin client נופל מקומית: עגלת אורח, כתיבת כתובת בקופה, יתרת ארנק, וכל אזור
האדמין. זה מה שחוסם את סעיפים 16-31 ב-QA ואת מדידת `/checkout`. אינו באג בקוד.

---

## 1. תור `MISSION-FINAL`: מה נסגר היום

| # | שלב | סטטוס בסוף היום | ראיה |
|---|---|---|---|
| 1 | merge 4 ענפי הלילה | **סגור.** ארבעתם 0 קומיטים לפני `HEAD` | `git rev-list --count HEAD..origin/feat/supplier-portal` = 0 |
| 2 | תיקון `packages/payments` | **בטל כמשימה, הוחלף בביקורת.** החבילה מעולם לא היתה קיימת; הכסף יושב ב-`src/lib/money.ts` ו-`src/server/payments` | `docs/PAYMENTS-VERIFY-REPORT.md` |
| 3 | Checkout E2E מול Cardcom sandbox | **חסום חלקית.** הזרימה עוברת בסוויטה; סליקה חיה דורשת את סודות (1) | `docs/E2E-MEASURED.md` |
| 4 | איחוד migrations, dry-run מקומי | **סגור, בלי החלה על המרוחק.** `migrations/pending/` נמדד מול פרודקשן | `STATE.md`, "ביקורת migrations/pending" |
| 5 | `compare.mjs` על כל הדפים מתחת 11% | **חלקי, ראה 2 למטה** | טבלה למטה |
| 6 | WP import dry-run | **סגור, אפס כתיבות** | `docs/WP-IMPORT-REPORT.md` |
| 7 | QA מלא + תיקון כל באג | **סגור למעט האדמין.** סעיפים 1-15, 32-33 וחוצי-מערכת סגורים; 16-31 חסומים על המפתח | `docs/QA-CHECKLIST.md`, `STATE.md` |
| 8 | lighthouse + נגישות + RTL על כל דף | **סגור** | `docs/LIGHTHOUSE-AUDIT.md` |
| 9 | עדכון הדוח הזה | **הקובץ הזה** | |

---

## 2. שער הפיקסלים, כפי שהוא באמת

| דף | נמדד | מתחת ל-11%? |
|---|---|---|
| `home` | **9.83%** | כן |
| `cart` | **8.6%** | כן |
| `checkout` | 12.43% | לא |
| `product` | 16.17% | לא |
| `products` | 31.92% | לא |
| `category` | **מסרב למדוד** | האתר החי מציג 2 כרטיסים והמקומי 12. זה פער קטלוג, לא פער נאמנות |
| `search` | **מסרב למדוד** | אותה סיבה |

הסירוב הוא התנהגות מכוונת של הכלי מהיום: מדידה שמשווה קטלוגים שונים מדווחת
הפרש שאינו קיים. השלמת הפער היא **שלב 23 בתור הארוך** (גל הפיקסלים), לא שלב 5,
ו-`docs/PIXEL-WAVE-REPORT.md` מחזיק את מה שכבר נעשה בו.

---

## 3. שלב 8, בקצרה. הדוח המלא ב-`docs/LIGHTHOUSE-AUDIT.md`

ארבעה ממצאים. שלושה מהם באותה צורה בדיוק: **השער החזיק תת-קבוצה של הדפים,
והדף שנשבר היה מחוץ לו.** זה קרה שלוש פעמים באותו יום, על שלושה שערים שונים.

| מה | היכן | לפני | אחרי |
|---|---|---|---|
| גלישה אופקית ב-320px | כל דף מוצר | 351px ברוחב 320 | **54/54 מסלולים נקיים** |
| קפיצת פריסה | `/coupons` | CLS **0.585** | **0.000**, ו-Performance 68 → 91 |
| אין `<meta name="description">` | כל דף קטגוריה | SEO 92 | **100** |
| שתי נקודות סליידר חלקו נגיעה | דף הבית | חפיפה 8px, נגיעה פתחה שקופית אחרת | **חפיפה 0px**, אף נקודה לא זזה |

השערים עצמם הורחבו לכל המסלולים הציבוריים: נגישות (19 מסלולים, שני ויופורטים),
RTL ב-320px (24 מסלולים + דף מוצר דרך קישור אמיתי), ו-CLS (15 מסלולים + דף מוצר).

**מה שנשאר במכוון ולמה:** `errors-in-console` בכל דף הוא שני סקריפטים של Vercel
שמחזירים 404 מקומית בלבד; `is-crawlable` על `/cart`, `/checkout`,
`/supplier/login` ו-`/legal/*` מכוון; `meta-description` על `/search` נכתב אחרי
`</head>` כי ה-query פר-בקשה, והדף `noindex` ממילא; `target-size` על נקודות
הסליידר הוא WCAG 2.2, מחוץ לסט WCAG 2.1 AA שהשער בודק, ותיקון מלא דורש להזיז
את הנקודות ולשלם בנאמנות פיקסלים. **זו שאלה 12.**

**‏`/checkout` לא נמדד בכלל.** עם עגלה ריקה הוא מפנה ל-`/cart`, וה-URL הסופי
בדוח של Lighthouse הוא `/cart`. הציון 65 וה-CLS 0.169 שייכים להפניה, לא לדף.
מדידה אמיתית דורשת עגלה עם פריטים, כלומר את המפתח מסעיף 0.

---

## 4. השערים, נמדדו על `HEAD` בסוף היום

| שער | תוצאה |
|---|---|
| `vitest run` | **2934/2934** ב-223 קבצים |
| `tsc --noEmit` | 0 שגיאות |
| `biome lint .` | 0, על 965 קבצים |
| `pnpm build` | קוד יציאה 0 |
| Playwright, סוויטה מלאה | 332 עברו, 3 דולגו, **5 נכשלו**, 6.2 דק' |
| אותם 5, בהרצה ממוקדת | **21/21 עברו**, worker אחד |
| axe על 19 מסלולים, 2 ויופורטים | **0 הפרות** |
| Lighthouse על 18 מסלולים | נגישות 100 בכולם חוץ מדף הבית (96), ביצועים 86-93 חוץ מ-`/checkout` |
| RTL 320/393 על 27 מסלולים | **54/54 נקיים** |
| CLS על כל מסלול ציבורי | **36/36**, כולם בין 0.000 ל-0.027 |

**חמשת הכשלים בסוויטה המלאה הם עומס, לא רגרסיה.** כולם ב-`cart.spec` וב-
`category.spec` על mobile-chrome, כולם timeout, וכולם עוברים בהרצה ממוקדת על
worker אחד. זו אותה תופעה שתועדה ב-`STATE.md` היום: העגלה של האורח עוברת דרך
admin client שנופל מקומית (סעיף 0), ובהרצה מקבילית זה נראה כמו באג פריסה.
**אינו נסגר כ"ירוק" בכוונה:** מה שמסתיר את זה הוא המפתח, ולכן זו עוד שורה
שנפתרת רק כשהמפתח יתוקן.

---

## 5. ארבעת ענפי הלילה: סגור

| סדר | ענף | מצב היום |
|---|---|---|
| 1 | voucher | היה בפנים כבר מ-24.07 |
| 2 | wallet | היה בפנים כבר מ-24.07 |
| 3 | supplier | **מוזג**, 0 קומיטים לפני HEAD |
| 4 | arch-night | **מוזג**, 0 קומיטים לפני HEAD |

שני קבצי `pending` ל-`payment_events` (‏006 ו-120) שהיו סיכון המיזוג: שניהם
עדיין ב-`migrations/pending/` ו**לא הוחלו על המרוחק**. הבחירה ביניהם היא חלק
מ-`docs/MIGRATION-BACKLOG.md`, ולא הוכרעה כאן.

---

## 6. מה נשאר

**חוסם כסף (רק אופיר):** Cardcom prod, Resend DNS, שמונה סודות, 11 ספקים חסרי
פרטים, אפס סליקות חיות, Production branch של Vercel שאינו `main`.

**חוסם GA ולא soft-launch:** `/cancel`, חתימת עו"ד, סיווג 14ח, מע"מ 18% לאורך
כל המסלול, leaked-password protection.

**בתור הארוך (`CLAUDE.md`), אחרי שלב 9:** שלב 23 גל הפיקסלים (product,
products, checkout מעל 11%), ושלב 26 סבב סופי.

**שבעה ממצאים ממחזורי האימות של הלילה (20.08), אף אחד אינו חוסם שחרור.**
הפירוט המלא, כולל נתיב, שורה, חומרה מנומקת והתיקון המדויק, נמצא ב-`docs/PROJECT-COMPLETE.md`
תחת "ממצאי מחזורי האימות שממתינים לתיקון". בקצרה:

1. ‏`mergeGuestCart` מיוצא מקובץ `'use server'` ולכן מפורסם כנקודת קצה רשתית
   שאיש אינו קורא לה מהלקוח (לא נצילכרגע).
2. ‏`qr.ts` מקצר דרך בחלון רוטציית סודות, במקום שה-webhook בכוונה לא.
3. חמישה מימושים נפרדים של השוואה בזמן קבוע, כולם תקינים, אך זו סחיפה.
4. ‏**‏`fn_ensure_referral_code` בפרודקשן אינה בודקת `auth.uid()` כלל** ולכן
   קוראת וכותבת על שורת פרופיל של אחר. **התיקון כתוב וממתין לאישורך:**
   ‏`migrations/pending/125`. זהו היחיד שדורש הרצת migration.
5. שמונה חולשות תלויות, **כולן בכלי dev/build ואף אחת ברכיב שרץ בפרודקשן**.
6. אימות ה-OTP מוגבל לפי IP בלבד, בעוד השליחה מוגבלת גם לפי מספר.
7. טאב "נאמנות (Escrow)" באדמין מציג שתי שורות מהמודל שבוטל.

**מה שנבדק הלילה ונמצא תקין, כדי שלא ייבדק שוב:** כל 84 ה-server actions,
כל 10 נתיבי ה-cron, ה-webhook של Cardcom, מניפסט ה-RLS מול פרודקשן (אפס
סחיפה), טיפוסי עמודות הכסף בפרודקשן (**אפס עמודות צף**), ארבע אינווריאנטות
כסף על שורות אמיתיות, משטח ה-XSS, והיסטוריית ה-git כולה.

**לא ב-v1:** פיזי, מנוי, WhatsApp, QStash, חנויות אפליקציה, Escrow.

---

## 7. החלטות אוטומטיות (נרשמו גם ב-`STATE.md`)

- C11(א) סופי. מסמכי Escrow ישנים הם שריד.
- soft-launch = קופון בלבד.
- Google בלבד, בלי OTP.
- השקה תפעולית: Resend + Vercel cron. QStash כבוי עד טוקן שנבדק.
- apex קנוני; www → apex.
- עמודים משפטיים: שני הסטים מוגשים, החדש `noindex`, ההכרעה של עו"ד (שאלה 11).
- שוברי legacy: אפס עמלה במערכת החדשה.
- payout ידני עד שיש פיזי חי.
- נקודות הסליידר: נשמרה הגיאומטריה של האתר החי, תוקנה רק החפיפה (שאלה 12).
- הגיבוי ל-Desktop: מסשן אחד הוא יוצא 0 בלי לכתוב קובץ (TCC), ומסשן מקביל
  הוא נכתב. נמדד 22:15: שלושה קבצים על שולחן העבודה, כ-690MB כל אחד
  (`16:42`, `18:29`, `20:46`). הגיבוי שתמיד עובד הוא `git push` ל-`origin`
  ותג.

---

## 8. Revision

| Date | Change |
|---|---|
| 2026-08-19 (בוקר) | שלד דוח בוקר: שאלות קריטיות, סטטוס 4 ענפים, תור MISSION-FINAL |
| 2026-08-19 (סוף היום) | עודכן למציאות: תשעת השלבים, שער הפיקסלים כפי שהוא, תוצאות שלב 8, השערים הנמדדים, שתי שאלות חדשות (11, 12) |

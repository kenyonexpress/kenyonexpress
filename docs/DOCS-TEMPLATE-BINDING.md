# תבנית מסמך ארכיטקטורה (מחייבת)

כל מסמך חדש או pass על
`docs/`
ב-worktree
`ke-arch`
חייב לעמוד בכללים האלה.

Status: **BINDING** · עודכן: 2026-08-12  
Scope: **docs only** · worktree `ke-arch` · branch `arch/docs-batch-2`  
אין שינוי קוד. אין נגיעה בתיקייה הראשית. אין ענף חדש.

---

## 0. כללי עבודה קבועים

| # | כלל |
|---|---|
| R1 | כתיבה רק ב-worktree `ke-arch`. |
| R2 | אסור קוד יישום (`src/`, מיגרציות חדשות, סקריפטים). |
| R3 | אסור לגעת בתיקייה הראשית `kenyonexpress`. |
| R4 | אסור `git checkout main` (או master). |
| R5 | אסור ליצור ענפים חדשים. הכל ל-`arch/docs-batch-2`. |
| R6 | אחרי כל מסמך: `git commit` + `git push origin arch/docs-batch-2`. |
| R7 | עברית RTL. בלי em dash. מונחים באנגלית בשורה נפרדת כשצריך להעתקה. |
| R8 | מודל כסף: **No Escrow**; `platform_percent` פר מוצר בלי default; אגורות integer. |

---

## 1. מבנה חובה בכל מסמך

כל מסמך ARCHITECTURE / SPEC תחת
`docs/`
חייב לכלול לפחות את הסעיפים הבאים (שמות כותרת יכולים להשתנות, התוכן לא):

### 1.1 החלטה

מה מחייב. טבלת הכרעות ממוספרת (למשל D1, D2…).

### 1.2 חלופות שנדחו

לפחות שתי חלופות שנשקלו ונפסלו, עם נימוק קצר לכל אחת. לא "אפשר גם".

### 1.3 סכמת DB

טבלאות/עמודות/enumים רלוונטיים, או מצביע מפורש למיגרציה קיימת + שדות שנקראים. אם אין שינוי סכימה: לכתוב במפורש "אין DDL חדש" ומה כבר קיים.

### 1.4 מקרי קצה

טבלת כשלים / races / timeout / replay / הרשאות. לפחות חמישה מצבים כשזה דומיין כסף או זהות.

### 1.5 פתוחות

רשימת שאלות או פערים שלא נסגרו (או "אין פתוחות" עם תאריך). אסור להסתיר פער.

---

## 2. כותרת עליונה חובה

```text
Status: BINDING (או DESIGN + סיבה)
Scope: docs only · worktree ke-arch · branch arch/docs-batch-2
אין שינוי קוד. אין נגיעה בתיקייה הראשית.
מודל כסף: No Escrow (כשרלוונטי)
```

---

## 3. Revision

| תאריך | שינוי |
|---|---|
| 2026-08-12 | BINDING: תבנית חובה לכל מסמכי docs ב-batch-2 |

---

## 4. Acceptance לתבנית עצמה

- [ ] R1–R8 מתועדים  
- [ ] חמשת רכיבי התוכן מחויבים  
- [ ] אין יצירת ענף בתהליך  

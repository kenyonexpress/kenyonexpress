# מצבי ריק ושגיאה

לכל רשימה וזרימה מרכזית: מצב ריק ומצב שגיאה בעברית, עם פעולה הבאה.
מקורות חיים נמדדו ב-07.09.2026. רכיב משותף עדיין לא קיים; היעד הוא
‏`EmptyState` אחיד ב-‏W20 בלי לשבור את השער.

## דפוסים קיימים

| מחלקה / רכיב | קובץ | שימוש |
|---|---|---|
| ‏`.cart-empty` + ‏CartEmptyState | ‏`cart-page.css`, ‏`CartLineItem.tsx` | סל ריק |
| ‏`.account-empty` | ‏`account.css` | רוב דפי החשבון |
| ‏`emptyMessage` | ‏DataTable / ServerDataTable | אדמין |
| ‏`error.tsx` / ‏`not-found.tsx` / ‏`global-error.tsx` | ‏`src/app/` | 500 / 404 גלובלי |
| ‏`.account-alert--error` / ‏`--success` | ‏`account.css` | משוב פעולה |

## חזית לקוח

| זרימה | ריק | שגיאה | פעולה הבאה |
|---|---|---|---|
| עגלה | סל הקניות שלך ריק כרגע. | כשל טעינה / כשל עדכון כמות | המשך לקניות → `/` |
| קופה | אין מה לשלם (הפניה לעגלה) | סירוב סליקה, מלאי, מתג כבוי | חזרה לעגלה / ניסיון חוזר |
| הזמנות | עוד לא ביצעת הזמנות. | כשל טעינה | לקטלוג |
| קופונים | עדיין לא רכשת קופונים. | שובר לא נמצא / לא שלך | לקטלוג דילים |
| ארנק | עדיין אין תנועות בארנק. | כשל טעינה | חזרה לחשבון |
| הפניות | אין קוד / אין מופנים (שני מסרים נפרדים) | כשל יצירת קוד | ניסיון חוזר |
| כתובות | עדיין לא הוספת כתובת. | ולידציית טופס | הוספת כתובת |
| מנויים | אין לך מנויים פעילים. | כשל ביטול | תמיכה |
| כרטיסים שמורים | מסר ב-TokenManager | כשל מחיקה/טוקן | הוספה בקופה |
| משאלות | רשימה ריקה | כשל סינכרון | לקטלוג |
| חיפוש / קטגוריה | אין תוצאות לסינון | כשל קטלוג | ניקוי פילטרים |
| 404 | הדף שחיפשתם לא נמצא | - | דף הבית |
| 500 | שגיאה כללית + איפוס | digest ללוג, לא למשתמש | נסו שוב |

## אדמין

| רשימה | ריק ברירת מחדל |
|---|---|
| כללי | אין נתונים |
| מוצרים | אין מוצרים |
| קטגוריות | אין קטגוריות |
| קופונים | אין קופונים |
| משתמשים | אין משתמשים |
| גרף / אנליטיקה | אין נתונים בטווח הזה. |

שגיאת שמירה: מעל הטופס, בעברית, בלי נתיב קובץ ובלי ‏SQL.

## ספק

| זרימה | ריק | שגיאה |
|---|---|---|
| מימושים | אין מימושים עדיין | - |
| סריקה | - | קוד לא תקין / כבר מומש / לא בספק שלך / פג |
| מוצרים | אין מוצרים משויכים | אין הרשאה |

## כללי כתיבה

1. משפט אחד, גוף שני («שלך» / «שלכם» עקבי למסך).
2. בלי האשמת משתמש («טעית») ובלי סלנג.
3. ‏CTA אחד ראשי. קישור משני רק אם בטוח.
4. שגיאת כסף: מה קרה + מה לעשות, בלי מספרים פנימיים של ספק תשלום.
5. מגדר: ניסוח ניטרלי היכן שהעברית מאפשרת (ראה
   `HEBREW-STYLE-GUIDE.md`
   ).

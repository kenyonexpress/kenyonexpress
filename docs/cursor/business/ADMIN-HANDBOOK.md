# מדריך מפעיל

אתה מפעיל את הפלטפורמה. לא סוכן, לא MCP על פרודקשן בלי החלטה.

## מוצרים

אין עמלת ברירת מחדל. בלי `platform_percent` המוצר לא נמכר. קופון: `coupon_price_ils` סכום מוחלט. לא אחוז מהפנים. לא למלא `default_split_percent` כאילו זה שער גלובלי.

## אחוזים

צילום להזמנה. שינוי היום לא משנה הזמנות ישנות. השלמת זוג אחוז ספק/פלטפורמה ל־100.

## אישורים

לידים של ספקים: אחרי אישור נוצרת חברות ב־`supplier_members`. תפקיד `vendor` לבד לא פותח קופה.

## מודרציה

ביקורות: רק רכישה ששולמה. מעלה תוכן לא מפרסם כסף ולא מממש קופון.

## תשלומים

אין טבלת payout חיה. מסך ריק או הודעה עברית, לא 500. טאב webhooks דורש לקוח שירות (אפס מדיניות).

## מיגרציות

רק אחרי אישור, MCP, קובץ בשם מלא. 169/170/171/172 כפולים. 162 חסום ב־vault. לא 165.

## השקה

רשימת החוסמים בסדר, DNS בסוף. מפתח שירות שדלף: רוטציה. מוצר הבדיקה בשקל: מלאי 0.

## מתגים

הדף מציג env. אין טבלת דגלים. סגירת קופה: `CHECKOUT_ENABLED`.

## אירועים

אם אין שורות רכישה באנליטיקה, זה לא אומר שאין הזמנות. בדקו `orders` ואת היומן. ארבעה אירועי משפך יכולים להיות אפס בלי שהקופה מתה.

## ספק תקוע בגישה נדחתה

אין חברות ב־`supplier_members`. תפקיד `vendor` בפרופיל לא פותח קופה. צרו חברות owner ואז scanner.

## הזמנה שולמה בלי קופון

stranded-payments. אל תכניסו שורת תשלום ידנית. אל תסמכו על גוף ה־webhook.

## מוצר בשקל מול 400

חסימה באפליקציה פלוס מלאי 0 אחרי 172. אל תמחקו לפי שם "מאסטר" (יש מוצרים אמיתיים עם המחרוזת).

---

## Second pass

Read with `waves/WAVE-INDEX.md` and `business/LAUNCH-BLOCKERS.md`. Tree on this branch wins over older briefs. Do not apply SQL from this worktree.

# Post-launch 30 days

Watch: stranded count, reconcile, cron 200, Sentry money, voucher mail lag, scan `error` rate, Actions minutes (R29), R2 bandwidth, Cardcom disputes.

Tune: kill search if ILIKE melts; enable Meili. Indexes 170 if seq scans. ntfy noise.

Do not: add global 10%, escrow, payout tables to silence 500, `/en`, Twilio+Meta+float three senders, 162 while Actions runs.

Upgrade Vercel: when Hobby cron temptation appears (already moved to Actions), or CPU/bandwidth invoices, or cron minutes. First paid tier is a human billing choice when p95 checkout or cron SLO misses for a week, not when GMV vanity is nonzero (169 may still be 0).

---

## Second pass

Watch stranded and cron 200. Do not add escrow or global 10%. Upgrade on p95/stranded, not vanity GMV.

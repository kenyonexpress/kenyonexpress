# 0013 — דגלי מערכת: משתנה סביבה גובר על הטבלה, ומצב תחזוקה ב-proxy

**סטטוס:** נאכף. **מאז:** ‏22.09.2026 ‏(SECTIONS 83).

‏`resolveFlag` ב-`src/lib/resilience/feature-flags.ts` קורא לפי הסדר: משתנה סביבה,
שורה ב-`feature_flags` ‏(מיגרציה ‏235, ממתינה), ברירת מחדל בקוד. הסביבה גוברת כי
היא היחידה שזמינה כשה-DB הוא מה שנפל, וכי היא נטמעת ב-build של ה-proxy. מצב
תחזוקה נבדק ב-`src/proxy.ts` ומחזיר ‏503 עם ‏`Retry-After`, לפני כל ‏route,
עם רשימת פטורים מדויקת ‏(התאמה מלאה או ‏`prefix/`, לא ‏`startsWith`). הקאש של
הטבלה הוא ‏30 שניות. הראיות ב-`docs/RESILIENCE.md`.

# Known issues

The v1.2.0 ledger of what is knowingly imperfect. Every entry names its
severity and the reason it is not fixed in code; nothing here is a surprise
waiting to be re-discovered.

| # | Severity | Owner | Issue | Why it stays |
| --- | --- | --- | --- | --- |
| 1 | **BLOCKER (env, not code)** | Ofir | Production checkout runs against the mock Cardcom provider (`CARDCOM_USE_MOCK=true`, no terminal credentials) | The fix is credentials only Ofir has (03-9436100). Removing the flag alone makes it worse. `docs/DEPLOYMENT.md` has the exact order. |
| 2 | high | Ofir | Two `TODO(cardcom)` in `src/lib/payments/cardcom.ts` (:246, :309): the legacy refund and document endpoints' exact field names are unconfirmed against a live terminal | Cannot be confirmed without the production terminal — same blocker as #1. Verified against docs, not against the wire. |
| 3 | high | קוד (פרויקט layout) | Mobile layouts (380/768) diverge structurally from live (height ratio 0.59; whole-page diffs 35-43%) | A layout project, not a tuning pass. Desktop (1440) is under the 11% gate at 7.08%. **Re-measured 2026-09-08 pass 59 against the localized reference, all three widths on one build: 380 30.26%, 768 28.99%, 1440 8.29%.** **The attribution below was wrong and is withdrawn, pass 80.** It read the 1440 delta of 1.2 points as the cost of the reference's missing fonts, and concluded the mobile figures were this row's divergence rather than a measurement artefact. That rested on the claim in `compare.mjs` that all 96 of the reference's failed requests were fonts. Counted instead of described, deterministic over four runs at two widths: **37 fonts, 57 scripts, 1 stylesheet, 1 xhr**. The reference runs none of the theme's JavaScript — Elementor Pro's element handlers and Electro's bundles are among the 57 — so its carousels, tabs, mega-menu and sticky header never initialise, and its body measures 18146px at 380 against 5492px at 1440. How much of the mobile figure is our layout and how much is a page that never finished becoming itself **cannot be decided from this snapshot**, and nothing in the repo can decide it now that the origin is gone. Every gate run now prints the shortfall beside its percentage and writes it into the report row, so the figure is no longer quotable bare. |
| 4 | medium | Ofir | 14 of 45 active products carry slugs that describe a different product ("copy", numeric, wrong item) | Content: rewriting slugs changes public URLs and needs 301s plus a per-product decision. |
| 5 | medium | Ofir (MCP) | 31 migrations sit in `migrations/pending/` unapplied — five of them are what turns payment_events, refunds destination, analytics, payouts and ai_usage fully on | Applying is a hard stop reserved for Ofir via MCP, in APPLY-ORDER.md order. Each of this closeout's six carries a rolled-back production dry run. |
| 6 | medium | קוד (אחרי 150) | Subscriptions have no pause/card-swap verbs and no status emails; renewal journal events not emitted | Named in docs/SUBSCRIPTIONS.md; the email kinds ship in a migration first (outbox constraint). |
| 7 | low | dependabot | `pnpm audit --prod`: 2 high / 1 low transitive advisories | Report-only in the nightly; fixes are dependency PRs (dependabot now targets main again). |
| 8 | low | Ofir | Hero headline antialiasing differs from live (Open Sans there, Heebo here, 51px glyphs) — the floor under the 5% fold target at 1440 | Heebo is a standing project rule; swapping fonts is Ofir's call. |
| 9 | low | קוד (perf) | Per-route client JS is **303.3 KB gz** (home) vs the 180 KB spec budget; the 255.6 KB figure is the shared floor, which is all `scripts/bundle-gate.mjs` can measure. `scripts/measure-route-js.mjs` measures the rest in a browser | The largest chunk, 130.1 KB, is the Next 16 client runtime plus react-dom — 72% of the budget before any app code, so this is not reachable by trimming and the budget needs re-deriving. The ratchet holds the line meanwhile. |
| 10 | ~~high~~ **resolved, and replaced by a smaller one** | Ofir (MCP) | ~~36 product image URLs in production still point at `kenyonexpress.co.il/wp-content/uploads/...`, which answers 403.~~ **Re-counted against production 2026-09-09: ZERO `wp-content` references remain** in any of the five places `audit-product-images.mjs` reads (`products.images`, `categories/banners/coupon_deals/product_variants.image_url`). What is there instead: 45 active and approved products, all with a real primary image, and **one published product with `images: []`** - `/product/מזקקת-ויסקי`, an empty frame on a live page. The 49 remote references point at picsum.photos (45) and images.unsplash.com (4) and belong entirely to the **35 draft** products, which no shopper sees. | The dead-URL half is gone and the row is kept only so the number is not re-quoted. The remaining half is one row of data, and it stayed invisible because the audit walks `p.images`: a product with an empty array contributes no references, every reference that exists resolves, and the run printed "all resolve". The audit now reports active products with no image at all and exits non-zero on them, so this is the last time it is found by hand. |
| 11 | high | Ofir (env or DNS) | **Every host-bearing declaration points at `kenyonexpress.co.il`, and that host 308-redirects to `www.kenyonexpress.co.il`.** Measured 2026-09-08 pass 69, six of them: `rel=canonical`, `hreflang=he-IL`, `og:url`, **`og:image`**, **`twitter:image`**, plus `robots.txt` `Host:` and `Sitemap:` and every sitemap `<loc>`. **The two image tags cost more than the rest:** social scrapers commonly do not follow redirects for images, so this can mean no preview card at all rather than a slower one | Measured 2026-09-08 with `node scripts/audit-canonical-host.mjs`. A crawler resolves the chain and indexes `www`, so the effective canonical is the host the code does not name and the platform's domain setting silently overrides the build. No test in this repo can see it: `src/lib/site-url.ts` falls back to the apex, the fallback is well formed, and every SEO test passes - the defect lives between the build and the domain configuration, and only one of those is in here. The fix is one line either way (`NEXT_PUBLIC_APP_URL` to the `www` host, or make the apex primary at the platform) and which way is a branding decision. |
| 12 | medium | Ofir (policy) | **`/api/account/export` and `/api/account/delete` do not exist.** `src/app/api/account/` is not present at all | STEP 16 names both. Export was declined twice as unrunnable from here — it returns a person's whole record. Erasure is the larger gap and is not a code decision first: Israeli tax law requires keeping invoices for seven years, so "delete my account" has to mean anonymise-and-retain for the order history, and which fields survive is the owner's call. Recorded rather than built blind. |
| 13 | high | Ofir (mainline decision) | **Three files that the platform reads only from the default branch differ on `closeout/v1-final`, so three fixes are inert:** `.github/dependabot.yml` (pass 41 raised the starved PR limit 5→10; `main` still says 5), `scripts/nightly-health.sh` (pass 55 wired five never-run audits; `main` has 5 run steps to this branch's 9), and `scripts/cron-jobs.json` (the original cron drift) | Measured with `node scripts/audit-default-branch-config.mjs`, wired into CI's `gates` job as report-only. Each edit is correct in the file, passes review, passes CI and is visibly present in the branch — the failure is entirely in which copy the platform reads. All three resolve the moment the mainline question is settled and this branch merges; none is separately fixable. |


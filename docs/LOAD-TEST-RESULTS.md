# תוצאות בדיקות עומס — סבב 2

‏נמדד ‏02.09.2026, ‏k6 מקומי מול `pnpm start` על המחשב הזה (‏port 3412,
build של אותו יום). ההקשר קובע את הקריאה: **תהליך Node יחיד על לפטופ, בלי
CDN ובלי scale-out** — פרודקשן ב-Vercel מקבל את שניהם, אז המספרים כאן הם
רצפת ביצועים, לא תקרה.

## ‏browse.js — ‏flash_sale ramp, ‏LOAD_VUS=40 (9 דקות)

| מדד | תוצאה | שער |
| --- | --- | --- |
| כשלים | ‏0.00% (‏0/8,206) | ‏<0.5% ✅ |
| ‏rate-limited | ‏0.00% | — |
| בית ‏p95 | ‏691ms | ‏<1000ms ✅ |
| מוצר ‏p95 | ‏333ms | ‏<800ms ✅ |
| קטגוריה ‏p95 | ‏44ms | ‏<1000ms ✅ |
| קטלוג ‏p95 | ‏46ms | ‏<1000ms ✅ |

לעומת הסבב הקודם (‏STATE, "k6 מול הבילד"): מוצר ‏128→333ms ובית ‏19→691ms
תחת ramp כפול; כל השערים עדיין ירוקים.

## ‏LOAD_VUS=200: נקודת הקריסה המקומית — ממצא, לא כשל של האפליקציה

בריצת ‏200 VUs תהליך ה-Node המקומי חצה את סף ה-p95 של הבית סביב **68 VUs**
(‏p95 1.15s), ‏k6 עצר על ‏abortOnFail, ובהמשך התהליך **מת ונשאר מת** —
ריצה עוקבת מדדה ‏80% כשלים עם ‏med=0s, שזה שרת שאיננו, לא שרת איטי.
זו בדיוק מלכודת ה"שרת מת משרת איטי" — לוודא שהשרת חי לפני שקוראים תוצאה.
המסקנה התפעולית: לפטופ אחד ≈ ‏60–70 קונים בו-זמנית; ‏200 במקביל הם עניין
של ה-scale-out של Vercel, שאין לו ייצוג כאן.

## ‏search.js

| מדד | תוצאה | שער |
| --- | --- | --- |
| כשלים | ‏0.00% (‏0/123) | ✅ |
| ‏suggest p95 | ‏553ms | ‏<800 ✅ |
| חיפוש מלא p95 | ‏979ms | ‏<800 ❌ / ‏<1500 ✅ |

החיפוש המלא רץ כאן על **fallback ה-ILIKE של Postgres** — ‏Meilisearch לא
מוגדר על המכונה. ‏979ms על ה-fallback זה בדיוק מה שה-fallback מבטיח
("עובד ואיטי יותר"); הסף ההדוק נמדד מול Meili כשהוא קיים.

## ‏checkout.js / redeem.js / webhooks.js / pool.js — לא רצו, בכוונה

תרחישי כתיבה. ‏`load/lib/guard.js` חוסם פרודקשן **בלי דגל עוקף**, ואין
סביבת staging — ‏Vercel מריץ פרויקט אחד. הרצתם מקומית הייתה כותבת הזמנות
ומימושים אמיתיים ל-DB המאוחסן (היחיד שיש). ירוצו ביום שיש staging.

---

## ‏10.09: ‏`mixed.js` נוסף. מה שהיה חסר, ולמה זה השתנה

**סעיף ‏52 מבקש חמישה תרחישים, וארבעה מהם היו כאן. החמישי — "תעבורה מעורבת
ריאליסטית, ‏30 דקות" — לא היה, וזה החסר שהכי משנה.** כל חמשת הקבצים הקודמים
מריצים **מסלול אחד**: ‏browse על עמודים, ‏search על ה-API, ‏pool על
‏`/api/cart`. כל אחד עונה "כמה מהר המסלול הזה תחת עומס משלו", **ואף אחד לא
עונה על השאלה שתכנון קיבולת באמת עוסק בה** — מה קורה כשהבקשות הזולות והיקרות
נמצאות באותו תור. בית שמוגש ב-44ms לבד יכול להיות מוגש ב-900ms בזמן שארבע
שאילתות חיפוש מחזיקות חיבורים, **וריצה חד-מסלולית לא יכולה להראות את זה, כי
בריצה חד-מסלולית אין שום דבר אחר בתור.**

**התמהיל הוא צורה, לא ניחוש בתחפושת של נתון.** לאתר הזה ארבע הזמנות משולמות,
ולכן אין יומן תעבורה לגזור ממנו משקלים; המצאת אחוזים והצגתם כמדידה הייתה שמה
מספר בדוי בטבלת תוצאות. במקום זה: המשפך שיש לכל חנות, **מוצהר כהנחה**
וניתן לשינוי ב-`LOAD_MIX` — ‏70% גלישה, ‏18% חיפוש, ‏9% עגלה, ‏3% מסך תשלום.
‏`sessions_by_leg` סופר כמה סשנים לקח כל ענף בפועל, כך שהתמהיל **שהיה** מופיע
בדוח ולא רק התמהיל שהתבקש.

**קריאה בלבד, ובלי דגל שמשנה את זה.** ענף התשלום טוען את **עמוד** התשלום ועוצר.
‏`checkout.js` הוא תרחיש הכתיבה, והוא זה שדורש ‏staging וטרמינל סנדבוקס.
שכפול שלו בתוך ‏soak של חצי שעה היה משאיר מאות הזמנות נטושות — **וזה משנה את
הנתונים שהריצה מדדה.**

**חצי שעה ולא חמש דקות**, כי הכשלים ש-soak מוצא הם אלה ש-spike לא יכול: ‏pool
שמדליף חיבור אחד לכל אלף בקשות, ‏cache שמתמלא, ‏revalidation שמתנפל בשעה
עגולה. חמש דקות בשיא מודדות את השיא; שלושים מודדות אם השיא שריד.

### הבאג שהניסיון להריץ אותו חשף: **כל חבילת העומס לא יכלה להתחיל**

לפני שהתרחיש החדש רץ פעם אחת, ‏`browse.js` נבדק מול ‏`pnpm start` מקומי
והחזיר:

```
Error: sitemap lists 0 products, need at least 10.
  at catalogue (load/lib/catalogue.js:43)
```

‏`/sitemap.xml` **אינו עוד רשימה שטוחה של עמודים. הוא ‏`<sitemapindex>`**
שמונה חמישה קבצי-מקטע. הקריאה הבודדת ב-`catalogue()` מצאה חמישה ‏`<loc>`, אף
אחד מהם לא מוצר, **וכל תרחיש שקורא לה מת ב-setup** — ‏`browse`, ‏`search`,
‏`pool` והחדש: **ארבעה מתוך שישה.** המספרים בסבב ‏02.09 נמדדו לפני הפיצול.

**הכשל הזה היה רועש, וזו הסיבה היחידה שהוא פסקה ולא תקרית.** הרצפה
(‏`MIN_PRODUCTS = 10`) הפכה ריצה ריקה-בשקט לעצירה. **וזו גם הסיבה שהתיקון אינו
"נסה אינדקס, אחרת שטוח"**: ‏fallback היה הופך את הדריפט הזה לריצה ירוקה
וקטנה במקום להודעה. ההחלטה היא על **אלמנט השורש**, כך שקובץ-מקטע שהתרוקן הוא
עדיין עצירה.

### ‏`mixed.js`, הורץ מקומית אחרי התיקון

‏`LOAD_VUS=8`, ‏ramp 5s, ‏hold 40s, מול ‏`pnpm start` על ‏3418.
‏**44 מוצרים, ‏12 קטגוריות, ‏149 מונחי חיפוש** — כלומר ה-catalogue נקרא.
‏56 סשנים, ‏140 בקשות, ‏206 בדיקות, **‏0.00% כשלים**.

| ענף | ‏p95 | ‏med | השער |
| --- | --- | --- | --- |
| ‏`product` | ‏62ms | ‏34ms | ‏<800 ✅ |
| ‏`category` | ‏36ms | ‏28ms | ‏<1000 ✅ |
| ‏`home` | ‏120ms | ‏71ms | ‏<1000 ✅ |
| ‏`checkout_page` | ‏266ms | ‏260ms | ‏<1500 ✅ |
| ‏`cart` | ‏286ms | ‏260ms | ‏<800 ✅ |
| ‏`search` | **‏615ms** | ‏545ms | ‏<800 ✅ (בריצה קודמת: ‏808ms ❌) |

**וזה בדיוק מה שהתרחיש קיים בשבילו.** ‏p95 כולל אחד הוא ‏511ms ואינו אומר
כלום: הבית ‏120, המוצר ‏62, **והחיפוש ‏615 — פי חמישה מהמוצר, על ‏18% מהתעבורה.**
בריצה הראשונה הוא עבר את השער של ‏800ms ‏(808ms) ובשנייה לא. **החיפוש הוא
הצוואר, גם בשמונה ‏VUs, וגם כאן הוא רץ על ‏fallback ה-ILIKE** — אותה מסקנה
שסבב ‏02.09 הגיע אליה במסלול נפרד, עכשיו מדודה כשהיא מתחרה על אותו תור.

**מה שעדיין לא הורץ, ומאותה סיבה שארבעת תרחישי הכתיבה לא הורצו: אין יעד
בקנה מידה.** התרחיש קריא לחלוטין ולכן בטוח מול ‏preview, אבל אין ‏preview —
‏`docs/STAGING.md` מפרט למה. מספרי ה-1000/500/200 ‏VU שהסעיף נוקב בהם הם
מספרי ‏scale-out של ‏Vercel, ו-`CAPACITY` כבר מדד מה קורה כשמודדים לפטופ
בלפטופ: ‏68 ‏VUs והתהליך מת.

**‏`load.yml` קיבל ‏timeout של ‏70 דקות** במקום ‏30: פרופיל ברירת המחדל של
‏`mixed` הוא ‏3+30+3, ובתקרה הקודמת ה-job היה נהרג שש דקות לפני סוף הפרופיל
שלו **וה-artifact עם הסיכום — כל מטרת הריצה — לא היה נכתב בכלל.**

---

## ‏21.09: סבב 3. ‏p50/p95/p99 לכל רגל, תרחיש קפיצה, ו-pg_stat_statements מול פרודקשן

**סעיף ‏42 ‏(LOAD-TEST-TUNE) ביקש שלושה דברים שלא היו כאן: ‏p50/p95/p99 מתועדים
לכל תרחיש, קפיצה של פי ‏10 ל-60 שניות, וסריקה של ‏pg_stat_statements עם תיקון
לכל שאילתה מעל ‏100ms.** השניים הראשונים נבנו ונמדדו. השלישי נמדד ונמצא
**לא דורש אף אינדקס**, והסיבה מפורטת למטה, כי "לא נמצא כלום" בלי המספרים היה
נקרא כ"לא נבדק".

**ההקשר של כל מספר בסבב הזה:** ‏`pnpm start` על ‏port ‏3421, ‏build של ‏20.09,
אחרי חימום של כל עמוד שה-sitemap מונה. **ה-DB יושב בשטוקהולם, והלפטופ לא:**
קריאת ‏REST בודדת ל-Supabase נמדדה ב-**‏290 עד ‏330ms** לפני כל עבודה. פרודקשן
רץ ב-`fra1` ‏(‏`vercel.json`), פרנקפורט מול שטוקהולם, שזה סדר גודל אחד מתחת.
לכן כל רגל כאן היא ‏"מספר הסיבובים ל-DB כפול ‏300ms ועוד העבודה", והדבר
שהמדידה כן מבודדת הוא **כמה סיבובים** עושה כל מסלול, לא כמה מהר הוא ירוץ
בפרנקפורט.

### ‏browse.js, ‏40 ‏VUs, ‏20s/60s/20s

‏1,620 בקשות, ‏538 סשנים, ‏0.00% כשלים, ‏0.00% ‏rate-limited, ‏100% בדיקות.

| רגל | ‏p50 | ‏p95 | ‏p99 | ‏max | שער |
| --- | --- | --- | --- | --- | --- |
| ‏category | ‏8ms | ‏26ms | ‏33ms | ‏49ms | ✅ |
| ‏home | ‏282ms | ‏909ms | **‏3,890ms** | ‏5,422ms | ✅ ‏(<1000) |
| ‏product | ‏248ms | ‏573ms | ‏1,152ms | ‏4,374ms | ✅ ‏(<800) |
| ‏products | ‏9ms | ‏21ms | ‏32ms | ‏288ms | ✅ |

**וזו הסיבה ש-p99 נרשם עכשיו.** הבית עובר את השער ב-p95 עם ‏909ms, וה-p99
שלו הוא ‏3.9 שניות: אחד ממאה קונים ממתין פי ארבעה ממה שהשער מדד. ברירת המחדל
של ‏k6 לא מדפיסה ‏p99, ולכן שני הסבבים הקודמים לא יכלו לראות את הזנב הזה.
‏`load/lib/summary.js` קובע ‏`med`, ‏`p(95)`, ‏`p(99)` ו-`max` בכל תרחיש, ולא
בדגל שורת פקודה שנשכח בריצה שחשובה.

### ‏search.js. **אדום, ובשני הרגליים**

‏33 בקשות, ‏0.00% כשלים. ‏k6 עצר את הריצה ב-`abortOnFail` אחרי ‏14 שניות.

| רגל | ‏p50 | ‏p95 | ‏p99 | ‏max | שער |
| --- | --- | --- | --- | --- | --- |
| ‏search | ‏648ms | ‏1,038ms | ‏1,164ms | ‏1,195ms | ❌ ‏p(95)<800 |
| ‏suggest | ‏618ms | ‏1,724ms | ‏2,100ms | ‏2,194ms | ❌ ‏p(95)<800, ‏<1500 |

**החיפוש אינו איטי ב-DB. הוא איטי בסיבובים.** ‏`search_products` בפרודקשן,
לפי ‏pg_stat_statements: ‏190 קריאות, ממוצע **‏24.7ms**, מקסימום ‏199ms.
‏`/api/search/suggest` עושה **שני סיבובים עוקבים** לפני שהוא עונה:
‏`check_rate_limit` ‏(‏RPC, כי ‏Upstash אינו מוגדר על המכונה; ‏731 קריאות
בפרודקשן, ממוצע ‏12.6ms) ואז ‏`search_products`. שני סיבובים כפול ‏300ms הם
‏600ms, וזה בדיוק ה-p50. עמוד המוצר עושה סיבוב אחד ויושב על ‏248ms. **אי אפשר
להקביל את השניים**: מגביל שרץ במקביל לעבודה שהוא אמור למנוע אינו מגביל.
בפרנקפורט אותם שני סיבובים הם עשרות אלפיות שנייה, וזה מה שהשער של ‏800ms
נכתב עליו. **הזמן שנמדד כאן הוא הגיאוגרפיה של הלפטופ, לא איטיות של השאילתה,
ואינדקס לא היה משנה בו דבר.**

### ‏mixed.js, ‏8 ‏VUs, ‏5s/40s

‏140 בקשות, ‏56 סשנים, ‏0.00% כשלים. אותו תמהיל כמו ב-10.09.

| רגל | ‏p50 | ‏p95 | ‏p99 | ‏max | שער |
| --- | --- | --- | --- | --- | --- |
| ‏cart | ‏278ms | ‏297ms | ‏299ms | ‏299ms | ✅ |
| ‏category | ‏11ms | ‏24ms | ‏30ms | ‏31ms | ✅ |
| ‏checkout_page | ‏289ms | ‏322ms | ‏325ms | ‏326ms | ✅ |
| ‏home | ‏321ms | ‏490ms | ‏527ms | ‏534ms | ✅ |
| ‏product | ‏259ms | ‏361ms | ‏475ms | ‏538ms | ✅ |
| ‏search | ‏581ms | ‏823ms | ‏960ms | ‏994ms | ❌ ‏p(95)<800 |

אותה מסקנה כמו ב-10.09, עכשיו עם הסבר: החיפוש הוא הרגל היחידה עם שני
סיבובים, ולכן היחידה שיושבת מעל ‏600ms.

### ‏spike.js, חדש. ‏5 ‏VUs ל-30 שניות, קפיצה מיידית ל-50 ל-60 שניות, חזרה ל-5 ל-30 שניות

‏2,680 בקשות, ‏0.00% כשלים, ‏0.00% ‏rate-limited, ‏100% בדיקות. עמודי מוצר
בלבד, כל בקשה מתויגת לפי השלב שלה.

| שלב | ‏p50 | ‏p95 | ‏p99 | ‏max | שער |
| --- | --- | --- | --- | --- | --- |
| ‏baseline ‏(5 ‏VUs) | ‏260ms | ‏401ms | ‏782ms | ‏896ms | ✅ ‏(<800) |
| ‏spike ‏(50 ‏VUs) | ‏276ms | ‏678ms | ‏1,188ms | ‏1,552ms | ✅ ‏(<1500) |
| ‏recovery ‏(5 ‏VUs) | ‏249ms | **‏297ms** | ‏362ms | ‏621ms | ✅ ‏(<800) |

**השורה החשובה היא האחרונה.** ‏p95 בהתאוששות ‏(297ms) נמוך מ-p95 בבסיס
‏(401ms): הקפיצה חיממה את הקאש ולא השאירה אחריה כלום. אתר עם דליפת חיבורים
או ‏cache שמתמלא היה מראה התאוששות **גרועה** מהבסיס, וזה בדיוק מה שהשלב הזה
קיים כדי לתפוס. במהלך הקפיצה ‏p50 כמעט לא זז ‏(260 ל-276ms) וה-p99 הוכפל
‏(782 ל-1,188ms), כלומר פי עשרה עומס נספג בזנב ולא בחציון. **הקפיצה ל-50 היא
קרוב לנקודה שסבב ‏02.09 מצא שהלפטופ מת בה ‏(68 ‏VUs)**, ולכן ‏`LOAD_BASELINE`
ו-`LOAD_SPIKE_FACTOR` הם פרמטרים ולא קבועים.

### ‏pg_stat_statements מול פרודקשן: אף שאילתה לתיקון, ועם המספרים

נקרא ‏21.09 דרך ‏Management API של ‏Supabase, לקריאה בלבד. החלון: מאז איפוס
הסטטיסטיקה ב-**‏16.07**, ‏601 statements ו-**‏1.68 מיליון קריאות** בתפקידי
האפליקציה ‏(‏anon, ‏authenticated, ‏service_role, ‏authenticator).

**מעל ‏100ms בממוצע: חמישה, ורק אחד מהם הוא שאילתת אפליקציה שחוזרת.**

| statement | קריאות | ממוצע | מקסימום | מה זה |
| --- | --- | --- | --- | --- |
| ‏`SELECT name FROM pg_timezone_names` | ‏316 | ‏524ms | ‏2,557ms | טעינת ה-schema cache של ‏PostgREST, לא של הקוד. ‏316 טעינות מחדש, ‏165 שניות ‏DB במצטבר. כל ‏DDL וכל ‏`NOTIFY pgrst` מפעילים אחת. |
| ‏`fn_due_abandoned_carts(3, 100)` | ‏123 | **‏110ms** | ‏210ms | ה-cron של העגלות הנטושות, פעם בשעה. **זו השאילתה היחידה שהסעיף עוסק בה.** |
| ‏`SELECT id FROM carts LIMIT/OFFSET` + ‏count | ‏1 | ‏183ms | | ספירת עמודים חד-פעמית של ‏anon, ככל הנראה ‏Studio. |
| ‏`consume_order_stock(order_id)` | ‏1 | ‏116ms | | קריאה אחת בכל החלון. |
| ה-CTE הרקורסיבי על ‏`pg_type` | ‏3 | ‏109ms | | גם הוא ‏PostGREST ‏(schema cache). |

**‏`fn_due_abandoned_carts`: ‏110ms בפרודקשן, ‏1.4ms בביצוע.** ‏`EXPLAIN
(ANALYZE, BUFFERS)` על גוף הפונקציה, מול הנתונים החיים: **‏5 buffers, ‏0 שורות,
‏1.4ms ביצוע, ‏30ms תכנון ‏(834 ‏buffers של קטלוג)**. הפונקציה עצמה, כפי
שהאפליקציה קוראת לה, ‏8 עד ‏16ms חמה בשלוש קריאות עוקבות. התוכנית משתמשת
ב-`carts_profile_id_idx` ומגיעה ל-`carts` דרך ‏`profiles`, כי רק **עגלה אחת
מ-1,705 נושאת ‏`profile_id`**. אין ‏seq scan על טבלה גדולה ואין מה לאנדקס:
אינדקס מוסיף מה לקרוא, לא מוריד, כשהשאילתה קוראת חמישה ‏buffers.

הפער בין ‏110ms ל-16ms הוא שני דברים שאינם אינדקס: **(א) קאש קר.** הקריאה
רצה פעם בשעה על מופע קטן, וכל מה שנטען בקריאה הקודמת פונה כבר. **(ב) תכנון
בכל קריאה.** הפונקציה היא ‏`LANGUAGE sql` עם ‏`SET search_path`, וה-SET מונע
‏inlining, ולכן היא מתוכננת מחדש בכל קריאה, ‏30ms על ‏834 ‏buffers של קטלוג,
בעיקר מ-`orders` ‏(11 אינדקסים) ו-`profiles`. הסרת ה-SET הייתה מחזירה את
ה-lint של ‏Supabase על ‏`function_search_path_mutable`, וזה החלף לא נכון על
‏cron שרץ ‏24 פעמים ביום ומוצא ‏0 שורות. **החלטה: אין מיגרציה.** נרשם כאן,
ולא ב-`migrations/pending/`, בכוונה.

**המועמד הבא אם הסף יירד:** ‏`expire_vouchers()`, ‏59ms ממוצע בפרודקשן,
‏48ms חם. ‏`release_expired_stock_reservations()` ‏4ms ו-`credit_expired_vouchers()`
‏23ms חמות.

**לפי זמן מצטבר, ולא לפי ממוצע**, שלושת הראשונים הם: ‏`seo_redirects`
‏(13,367 קריאות, ‏2.1ms, ‏28.7 שניות במצטבר; ‏`src/lib/seo/redirects.ts`
כבר מקשה אותו ‏5 דקות לכל מופע, והמספר הוא מופעי ‏serverless שמתחלפים),
‏`set_config` של ‏PostgREST ‏(‏660,818 קריאות, ‏0.0ms, אחת לכל בקשה), ורשימת
המוצרים ‏(‏68,351 קריאות, ‏0.2ms). אף אחד מהם אינו יעד לכוונון.

### מה לא רץ, ומאותה סיבה כמו קודם

‏`checkout.js`, ‏`redeem.js`, ‏`webhooks.js` ו-`pool.js` כותבים או מפילים
‏pool, ואין ‏staging. **ה-100 ‏checkout וה-50 מימושים שהסעיף נוקב בהם
ממתינים לסביבה, לא לקוד.** ‏`docs/STAGING.md`.

### לשחזר את הסבב

```
pnpm build && PORT=3421 pnpm start &
export LOAD_BASE=http://localhost:3421
LOAD_VUS=40 LOAD_RAMP=20s LOAD_HOLD=60s k6 run --quiet --summary-export=browse.json load/browse.js
k6 run --quiet --summary-export=search.json load/search.js
LOAD_VUS=8 LOAD_RAMP=5s LOAD_HOLD=40s k6 run --quiet --summary-export=mixed.json load/mixed.js
LOAD_BASELINE=5 LOAD_SPIKE_FACTOR=10 k6 run --quiet --summary-export=spike.json load/spike.js
node scripts/load-summary.mjs browse.json search.json mixed.json spike.json
```

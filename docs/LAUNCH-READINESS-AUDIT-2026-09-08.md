# ביקורת מוכנות להשקה, ‏08.09.2026

היקף: מדידה מול הפרודקשן והעץ, ‏docs בלבד. אין מיגרציה ואין פריסה.
משיק: `docs/LAUNCH-CHECKLIST.md`, ‏`docs/RUNBOOK.md`,
`docs/incidents/2026-09-08-production-tls-down.md`.

---

## 0. פסק הדין

**האתר אינו ניתן להשקה היום, ולא בגלל המוצר.**

המוצר בנוי מעל הנדרש: ‏4,229 טסטים, ‏RLS על ‏44 מתוך 44 טבלאות, מסלול כסף
בשלמים בלבד, מכונת מצבי החזר עם שש עילות סטטוטוריות, ‏PWA, נגישות ‏AA
נבדקת ב-axe, ואפליקציית ‏Expo עם עשרה מסכים.

**ובכל זאת: אף אדם בעולם לא יכול לפתוח את האתר כרגע.**

---

## 1. חוסמים, לפי חומרה

### ‏🔴 ‏SEV1: חוסמים מוחלטים

| # | החוסם | הראיה | מי יכול לפתור |
|---|--------|--------|----------------|
| **‏B1** | ‏**אין תעודת ‏TLS על הדומיין** | ‏`no peer certificate available` על אפקס ועל ‏www | ‏**אופיר בלבד** (‏Vercel) |
| **‏B2** | ‏**‏HTTP מפנה ‏308 אל הנקודה המתה** | ‏`curl http://` | נפתר עם ‏B1 |
| **‏B3** | ‏**רוטציית ‏`SUPABASE_SECRET_KEY`** | נחשף בהתקנה, עוקף כל ‏RLS | ‏**אופיר** |
| **‏B4** | ‏**פרויקט ‏Vercel בונה את הריפו הלא נכון** | דווח בסשן מקביל, ‏**לא אומת כאן**: אין ‏`vercel login` | ‏**אופיר** |

**‏B1 עד ‏B4 הם כולם מחוץ להישג ידי.** ‏שלושה מהם דורשים גישה ל-Vercel
או ל-Supabase שאין במכונה הזו, והרביעי הוא דיווח שלא הצלחתי לאמת.

### ‏🟠 ‏SEV2: פוגעים בלקוח או במדידה

| # | החוסם | הראיה |
|---|--------|--------|
| ‏B5 | ‏**שורת בדיקה ב-₪1 מרונדרת בדף הבית** | ‏₪1 מול ₪400, ‏99.75%. לא ניתנת לקנייה, כן ניתנת לצפייה. ‏`172` ממתינה. |
| ‏B6 | ‏**‏`discount_percent` = ‏50.00 בכל 80 השורות** | ‏avg = max. חשיפה צרכנית. |
| ‏B7 | ‏**דף המוצר ‏1.2 עד 1.9 שניות ‏p95 חם** | ‏`load/browse.js`, מול תקציב ‏800ms |
| ‏B8 | ‏**אין ‏cron ל-`/api/health`** | ‏`vercel.json` בלי ‏`crons`. **זו הסיבה ש-B1 לא התגלה בהתראה.** |
| ‏B9 | ‏**אין ניטור ‏uptime חיצוני** | ‏`/api/health` רץ בתוך הפריסה; כשל תעודה הוא לפניה |
| ‏B10 | ‏**ארבעת אירועי המשפך נזרקים ב-DB** | ‏`169` ממתינה. ‏PostHog מכסה מ-07.09. |
| ‏B11 | ‏**שער הפיקסלים לא מדיד** | ‏`UNMEASURED`, וגם תעודה לא תחזיר אותו: הנתיבים הם ‏WooCommerce |

### ‏🟡 ‏SEV3: חוב שיש לתעד ולא לחסום עליו

| # | מה |
|---|-----|
| ‏B12 | אין טבלת כרטיסים; ‏SLA של 24 שעות אינו מדיד |
| ‏B13 | אין ‏`Retry-After` באף אחד משמונה מסלולי ה-429 |
| ‏B14 | ‏`apps/mobile` מחוץ לכל שער (‏אין ‏CI) |
| ‏B15 | אין ‏staging; ‏4 מתוך 6 תרחישי העומס לא ניתנים להרצה |
| ‏B16 | גיבוי לא נבדק בשחזור מעולם |
| ‏B17 | עמלת ‏Cardcom האמיתית לא אומתה מול ההסכם |
| ‏B18 | ‏`@anthropic-ai/sdk` בתלויות הייצור, ו-`ANTHROPIC_API_KEY` אינו ב-`.env.example` |

---

## 2. עשר המיגרציות הממתינות

```
162_cron_schedule                      חסומה: אין URL פרוס להזין ל-vault
169_analytics_server_event_names       ארבעת אירועי המשפך
170_composite_indexes_top_queries      בטוחה, נבדקה עמודה-עמודה
171_category_name_shekel_order         בטוחה
172_hide_master_product_test_row       שורת ה-₪1
173_products_retired_commission_percent
174_wallet_topups
175_referral_program_settings          ★ זו שמדליקה את תוכנית ההפניות
176_supplier_pin_rate_limit_per_staff
177_set_updated_at_search_path
```

**‏`175` היא הממצא שכדאי לשים לב אליו:** ‏30 המסמכים שנכתבו אתמול חוזרים
ואומרים ש-`referral_program_settings` ריקה ושלב ההפניה במשפך לא קיים.
‏**המיגרציה שפותחת אותו כתובה וממתינה**, עם ‏preflight משלה.

**‏162 חסומה על ‏B1:** היא צריכה ‏URL פרוס להזין ל-vault, ואין כזה כל עוד
אין תעודה. ‏**חוסם אחד מייצר חוסם שני.**

---

## 3. מה שכן מוכן, ואין עליו ויכוח

| תחום | מצב | הראיה |
|-------|-----|--------|
| מסלול כסף | ✅ | אגורות integer, ‏`src/lib/money.ts`, אפס float |
| ‏Cardcom | ✅ מוקשח | סוד בהשוואת זמן קבוע, ‏`GetLpResult` חובה, ‏`SANDBOX_IN_PRODUCTION` חוסם טרמינל 1000 |
| החזרים | ✅ | ‏6 עילות, דמי ביטול נאכפים ב-DB ב-5% או ₪100 |
| ‏RLS | ✅ | ‏44/44 טבלאות |
| רייט-לימיט | ✅ | ‏33 מדיניויות בשם, ‏Upstash ‏+ ‏Postgres |
| נגישות | ✅ | ‏axe, ‏80 מקרים, ‏AA |
| ‏SEO טכני | ✅ | ‏sitemap שנכשל בקול, ‏robots שחוסם ‏`/redeem/`, ‏8 טיפוסי ‏JSON-LD |
| כותרות אבטחה | ✅ | ‏CSP, ‏HSTS, ‏nosniff, ‏Permissions-Policy תלוי-נתיב |
| טסטים | ✅ | ‏4,229 ב-350 קבצים |
| ‏PWA | ✅ | manifest, ‏SW, ‏offline |
| בדיקות בריאות | ✅ בנויות | ‏7 תלויות בשם |
| ‏`pnpm audit` | ✅ | אפס חולשות |

**‏"בדיקות בריאות בנויות" מול ‏B8: זה בדיוק ההבדל בין מערכת שקיימת למערכת
שעובדת.** התשתית לגילוי ‏B1 הייתה בנויה במלואה, חוץ מהחוליה שמפעילה אותה.

---

## 4. סדר הפעולות

### 4.1 אופיר, ידנית, ולפני כל דבר אחר

| # | פעולה | למה ראשון |
|---|-------|------------|
| 1 | ‏**תעודת ‏TLS לדומיין ב-Vercel** | בלעדיה אין אתר, ואין ‏162, ואין שער פיקסלים |
| 2 | ‏**לאמת שפרויקט ה-Vercel בונה את הריפו הנכון** | ‏B4 |
| 3 | ‏**רוטציית ‏`SUPABASE_SECRET_KEY`** | עוקף ‏RLS על 44 טבלאות |
| 4 | לאשר את אצוות המיגרציות ‏169-177 | ‏7 מהן בטוחות ונבדקו |

### 4.2 מיד אחרי ‏1, ואפשר בקוד

| # | פעולה | מאמץ |
|---|-------|-------|
| 5 | ‏**‏cron ל-`/api/health` ‏+ ‏`sendAlert`** | נמוך |
| 6 | ‏**ניטור ‏uptime חיצוני** | נמוך |
| 7 | ‏**להצביע את `compare.mjs` להפניה שאינה הדומיין החי** | בינוני |
| 8 | ‏`Retry-After` בכל ‏429 | נמוך |
| 9 | ‏CI ל-`apps/mobile` | נמוך |
| 10 | גיבוי עם שחזור נבדק | נמוך |

**‏5 ו-6 הם התיקון האמיתי של ‏B1**, לא התעודה: התעודה מתקנת את התקלה
הנוכחית, והניטור מתקן את הסיבה לכך שהיא נמשכה שעות בשקט.

### 4.3 ואז, ורק אז, שיווק

‏30 המסמכים שנכתבו ב-07-08.09 מתארים משפך, לוח תוכן, רשתות חברתיות
ורכישת לקוחות. **כולם מניחים אתר נגיש**, ואף אחד מהם אינו בר-ביצוע לפני
‏§4.1 סעיף 1.

---

## 5. הפער בין "מוכן" ל-"מושק"

| שאלה | תשובה |
|-------|--------|
| האם המוצר בנוי? | ‏**כן, ובעודף.** |
| האם הוא נבדק? | ‏**כן.** ‏4,229 טסטים, ‏E2E, ‏axe, ‏k6, שער פיקסלים. |
| האם הוא מאובטח? | ‏**ברובו.** ‏RLS מלא, ורוטציית מפתח פתוחה. |
| האם הוא נגיש למישהו? | ‏**לא.** |
| האם יש ממה למכור? | ‏**‏12 ספקים, ‏80 מוצרים.** דל. |
| האם מישהו קנה? | ‏**‏4 הזמנות.** |
| האם מישהו מימש? | ‏**‏0.** |

**שתי השורות האחרונות היו הממצא של אתמול. השורה הרביעית היא הממצא של
היום, והיא קודמת לכולן.**

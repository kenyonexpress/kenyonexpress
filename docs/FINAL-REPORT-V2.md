# דוח סיום ‏V2 ‏(FINAL-AUDIT-V2)

‏SECTIONS 88. נמדד ‏22.09.2026 מול ה-build ‏(`CARDCOM_USE_MOCK=true pnpm build`, שרת
על ‏3431) ומול פרודקשן בקריאה בלבד. תג: ‏`v2.0.0-rc1`.

**כל מספר כאן הופק מריצה, ומה שנכשל כתוב ככישלון.** שני דברים חדשים נמצאו
ותוקנו תוך כדי ‏(§2, ‏§3), אחד הוא סתירה בין טסט למוצר שלא נפתרה כאן ‏(§2.3),
ואחד הוא מגבלת המכונה ולא של האפליקציה ‏(§4).

## ‏0. השורה התחתונה

| שער | תוצאה | לעומת הסבב הקודם |
|-----|-------|------------------|
| ‏`pnpm test` | ‏540 קבצים, ‏6,665 עוברים, ‏12 מדולגים | ‏531 / ‏6,617 ב-22.09 בוקר |
| ‏`pnpm type-check`, ‏`pnpm lint`, ‏`pnpm build` | נקיים | — |
| ‏Playwright, כל ה-specs, ‏chromium + mobile-chrome | **‏664 עוברים, ‏73 מדולגים, ‏9 נכשלים** בריצה המלאה; אחרי אבחון: ‏**4 כסף ירוקים בנפרד, ‏1 נגישות תוקן, ‏2 ידועים-אדומים, ‏2 סתירה מתועדת** | ‏4 ‏specs כסף ירוקים ב-21.09 |
| ‏compare.mjs ‏380 / ‏768 / ‏1440 | ‏**10.38% / ‏9.56% / ‏11.37%** ‏(1440 ‏FAIL) | זהה ל-19.09 עד המאית |
| ‏RTL sweep ‏(`_rtl-sweep.mjs`, ‏27 routes × ‏320/393) | **‏54/54 נקיים** | — |
| ‏a11y + ‏RTL + ‏touch targets ‏(4 ‏specs, שני פרויקטים) | **‏194 עוברים** | — |
| ‏k6 ‏spike ‏5→50→5 ‏VUs | ‏p95 ‏299 / ‏**308** / ‏300 ‏ms, ‏0 כשלים, ‏2,864 בקשות | ‏spike ‏678 ‏ms ב-סבב ‏3: **מהיר פי ‏2.2** |
| ‏k6 ‏spike ‏50→500 | **נעצר בשנייה ‏4** על ‏`abortOnFail` של הבסיס ‏(p95 ‏1,873 ‏ms ב-50 ‏VUs) | המסקנה של ‏02.09 ‏(68 ‏VUs = תקרת הלפטופ) עומדת |
| עמודים משפטיים | ‏4 מסמכים, ‏44 טסטים, כל ‏route ‏200 ‏(ראה ‏§5) | — |

## ‏1. הריצה המלאה של ‏Playwright, ומה תשעת הכישלונות היו

‏746 טסטים ב-28 ‏specs, שני פרויקטים, ‏2 ‏workers, ‏10.9 דקות. תשעת הכישלונות:

| # | טסט | סיבה נמדדת | מצב |
|---|-----|------------|-----|
| 1–4 | ‏`admin-refund` ‏(×2), ‏`full-purchase-redeem`, ‏`physical-purchase` | **השרת שהופעל ביד היה בלי ‏`NEXT_PUBLIC_APP_URL`.** ‏`loadCardcomEnv` בונה ממנו את כתובת ה-iframe של ספק הסליקה המדומה ‏(`/checkout/frame-return`) ונופל ל-`localhost:3000`, כך שה-iframe זר, ריק תחת ‏`frame-src 'self'`, ולעולם לא שולח את הודעת החזרה. ההזמנה **כן** שולמה וחשבונית **כן** הונפקה בצד השרת ‏(`token_charge_succeeded`, ‏`invoices.issued` ביומן), והדפדפן חיכה ‏45 שניות. ‏`playwright.config.ts` מגדיר את המשתנה ל-webServer שלו; שרת ידני חייב גם | **תוקן בהפעלה** ‏(`NEXT_PUBLIC_APP_URL=http://127.0.0.1:3431`): ‏**4/4 ירוקים**, ‏1.8 + ‏0.7 דקות. הזיכרון עודכן |
| 5 | ‏`a11y` ‏`coupons` ‏(mobile-chrome, ואחר כך ‏5/6 בשני הפרויקטים) | ‏`aria-prohibited-attr` על ‏`:root` — בפועל ‏`<div aria-busy aria-label="טוען קופונים">` ב-`CouponCardSkeleton.tsx`, ‏div ללא ‏role אינו רשאי לשאת ‏`aria-label`. לסירוגין כי ה-Suspense fallback נמצא על המסך רק כשהסריקה מקדימה את הזרם | **תוקן**: ה-`div` הוחלף ב-`<output>`, האלמנט שתפקידו הטבעי הוא ‏status ‏(biome דחה ‏`role="status"` על ‏div). ‏18/18 ואז ‏12/12 אחרי ‏rebuild. ניסיון ראשון נתן את אותו ‏role גם ל-`<ul>` של שלד הקטגוריה ושבר ‏24 ‏`<li>` ‏(axe ‏`listitem`) — הוחזר, כי ‏`ul` הוא ‏list ורשאי לשאת שם |
| 6–7 | ‏`home.spec:419` "one render-blocking stylesheet" ‏(×2) | ‏3 גיליונות במקום ‏1; ידוע-אדום מאז ‏09.09 ‏(זיכרון: ‏`@import` הופך את ה-cascade ומקריס את תמונות הדילים) | **פתוח, לא נגעתי** |
| 8–9 | ‏`role-isolation:146` "the supplier console shows no platform commission" ‏(×2) | ‏`/supplier` מציג כרטיס ‏"עמלת פלטפורמה ‏96.00 ‏₪" מאז ‏02.08 ‏(‏`f8a0e538c4`, ‏Ofir), וגם ‏`/supplier/orders`, ‏`/supplier/payouts` ושלושה ‏CSV. הטסט נכתב ‏10.09 ‏(`f1c0a5ded`) ודילג עד היום כי לספק הבדיקות לא הייתה חברות; הריצה היבשה של ‏21.09 יצרה אותה, וזו הפעם הראשונה שהוא רץ | **סתירה בין כלל לטסט ובין מוצר:** דוח תשלום מציג ברוטו, עמלה ולתשלום, והספק חתם על האחוז. מי צודק היא החלטת בעלים, לא עריכה. לא שונה דבר |

**ריצת האימות של הכסף כתבה שורות אמיתיות לפרודקשן** ‏(הזמנות ששולמו בספק מדומה,
שוברים, חשבוניות ‏`mock-doc-*`, החזר) על לקוח הבדיקות, כמו ב-21.09. אין ‏staging.

**רעש ביומן השרת:** ‏5,180 ‏`db.query_failed` בזמן הריצה, כולן ‏"During prerendering,
fetch() rejects when the prerender is complete" על ‏`products`, ‏`v_homepage_sections_live`,
‏`v_banners_live`, ‏`rpc:available_stock`. זהו ‏cacheComponents שמבטל קריאה אחרי שה-shell
נסגר, לא שאילתה שנכשלה; זה קיים גם ב-`pnpm build`. ראוי לסינון ברמת הלוג, לא נעשה כאן.

## ‏2. שער הפיקסלים

‏`LOCAL_BASE=http://127.0.0.1:3431 node scripts/compare.mjs --page=home --widths=380,768,1440
--baseline=refs/ke_live_{width}.png`. בלי ‏`--baseline` השער מנסה את הדומיין החי ונופל
על ‏`ERR_NAME_NOT_RESOLVED` ‏(אזור ה-DNS עדיין מסורב ב-Cloudflare, ‏20.09).

| רוחב | סה"כ | הבסיס ריק | אנחנו ריקים | שניהם מציירים | שער |
|------|------|-----------|-------------|---------------|-----|
| ‏380 | ‏10.38% | ‏1.98% | ‏2.23% | ‏6.16% | ‏PASS |
| ‏768 | ‏9.56% | ‏2.52% | ‏2.76% | ‏4.27% | ‏PASS |
| ‏1440 | ‏11.37% | **‏7.94%** | ‏1.41% | ‏2.02% | ‏FAIL |

זהות עד המאית לשורות מ-19.09. ‏1440 נכשל כי ‏7.94% הם פיקסלים שבהם **הצילום
הקפוא לבן ואנחנו מציירים** ‏(תמונות שלא נטענו בצילום), וזה לא ניתן לתיקון ב-CSS
‏(STATE ‏5-gate-decomposed). השער כתב את שלוש השורות ל-`docs/UI-PARITY-REPORT.md`
בעצמו; הקובץ לא נכלל בקומיט הזה כי סשן מקביל מחזיק בו שינויים לא מקומטים.

## ‏3. ‏RTL, נגישות, ‏IS 5568

- ‏`_rtl-sweep.mjs` על ‏27 ‏routes ציבוריים ב-320 וב-393: ‏**54/54 נקיים**.
- ‏`a11y.spec.ts` ‏(axe, ‏WCAG ‏2.1 ‏A+AA = הבסיס של ת"י ‏5568) על ‏19 עמודים, ‏`rtl-three-widths`,
  ‏`rtl-mobile`, ‏`touch-targets`, שני פרויקטים: ‏**194 עוברים** אחרי תיקון השלד.
- **‏Lighthouse נגישות ‏100** על בית, מוצר וקופה ‏(SECTIONS 84, לא נמדד שוב).
- הצהרת הנגישות ‏(`/accessibility`): ת"י ‏5568 ‏AA, סעיף פנייה. **פתוח לבעלים:** מינוי
  רכז נגישות ופרסום פרטיו ‏(הקובץ אומר זאת בעצמו).

## ‏4. עומס

‏`load/spike.js` מול ה-build. ברירת המחדל ‏(5→50→5, ‏30/60/30 שניות):

| שלב | ‏p50 | ‏p95 | ‏p99 | ‏max | שער |
|-----|------|------|------|------|-----|
| ‏baseline ‏(5 ‏VUs) | ‏238 | ‏299 | ‏511 | ‏1,207 | ✅ |
| ‏spike ‏(50 ‏VUs) | ‏235 | **‏308** | ‏674 | ‏917 | ✅ |
| ‏recovery ‏(5) | ‏238 | ‏300 | ‏400 | ‏458 | ✅ |

‏2,864 בקשות, ‏0 כשלים. הסבב הקודם ‏(סבב ‏3, ‏22.09 בוקר) מדד ‏spike ‏p95 ‏678 ‏ms.

**‏500 משתמשים:** ‏`LOAD_BASELINE=50 LOAD_SPIKE_FACTOR=10`. הבסיס של ‏50 ‏VUs מהשנייה
הראשונה, על מטמון קר, חצה את ‏`p(95)<1500` ‏(1,873 ‏ms) ו-`abortOnFail` עצר את הריצה
אחרי ‏4 שניות ו-108 בקשות ‏(0 כשלים). זו אותה תקרה שסבב ‏02.09 מצא ב-68 ‏VUs על
תהליך ‏Node יחיד בלפטופ שמריץ במקביל שלושה סשנים. **‏500 משתמשים לא נמדדים מכאן**,
ו-`docs/LOAD-TEST-RESULTS.md` אומר את זה מאז סבב ‏2: הבדיקה הזו רצה מול פריסה,
לא מול מחשב.

## ‏5. סקירת העמודים המשפטיים

ארבעה מסמכים ב-`src/app/(legal)/_content/`, ‏44 טסטים ירוקים ב-`legal-pages.test.ts`
‏(ספרייה לכל מסמך, ארבעת ה-slugs שרשימת ההשקה מונה, ‏metadata, תאריך ‏ISO).
כל ‏`/legal/<slug>` מפנה ב-308 לנתיב ה-WordPress הישן ‏(`/terms-and-conditions`,
‏`/privacy-policy`, ‏`/refund_returns`, ‏`/accessibility`), וכל ארבעתם ‏200 עם
‏`<time dateTime>` בראש העמוד ‏("עודכן לאחרונה").

| מסמך | עודכן | מה נבדק | ממצא |
|------|-------|---------|------|
| תקנון | ‏19.08.2026 | ‏16 סעיפים: הפלטפורמה כמתווך, כשרות, מוצרים, תשלום, קופון, פיזי, ארנק, ביטול לפי חוק הגנת הצרכן ‏1981 ותקנות ביטול עסקה ‏2010, אחריות, שימושים אסורים, ‏IP, פרטיות, שינויים, דין ‏(ת"א, עם שמירת זכות הצרכן לתבוע במקום מגוריו) | תקין |
| פרטיות | ‏19.08.2026 | ‏13 סעיפים; מצטט את תיקון ‏13 ‏(בתוקף ‏14.08.2025) ותקנות אבטחת מידע; זכות עיון ‏(§13, ‏30 יום) ותיקון ‏(§14); דיווח לרשות באירוע חמור; מינוי ממונה "ככל שיחול" | תקין. **פתוח:** אם יחול חיוב מינוי ממונה, העמוד מבטיח לפרסם את פרטיו |
| ביטולים והחזרות | ‏31.08.2026 | ‏11 סעיפים; ‏14 יום לפי ‏§14ג לכל סוג ‏(פיזי, קופון, אירוע עם ‏2/7 ימי מנוחה), דמי ביטול, החזר תוך ‏14 יום, מנוי לפי ‏§14ח | תקין |
| הצהרת נגישות | ‏19.08.2026 | ‏8 סעיפים; ת"י ‏5568 ‏AA, טכנולוגיות מסייעות, מגבלות ידועות, פנייה | **פתוח:** "מינוי רכז נגישות ופרסום פרטיו טעונים אישור" ‏(שורה ‏30 בקובץ) — צעד בעלים, כמו ב-01.09 |

**לא שונה דבר בטקסט:** שני הפתוחים הם החלטות של הבעלים ולא של עורך.

## ‏6. תג ומה שאחריו

‏`v2.0.0-rc1` הוא התג שהתור נוקב. הוא יושב בקו של ‏`v1.0.0-rc1` → ‏`v1.0.0` → ‏`v1.0.0-final`
‏(דוח הסיום הראשון), ולא בקו ‏`v5.11.x` שלולאת ‏step21 מקדמת אוטומטית; שני הקווים
חיים זה לצד זה בכוונה, וזה נכתב כאן כדי שאיש לא "יתקן" אחד מהם.

**מה נשאר פתוח, לפי סדר:** ‏(1) ‏DNS ‏(20.09) — האתר לא נפתר; ‏(2) ‏`home.spec:419`;
‏(3) הסתירה של עמלת הפלטפורמה בקונסולת הספק; ‏(4) רכז נגישות; ‏(5) הפריסה עצמה,
שבלעדיה ‏k6 ו-Lighthouse נשארים מדידות של לפטופ.

**אחרי הקומיט הזה, לפי התור: ‏SECTIONS 94 ‏(WA-CONTACT-SYSTEM), ואז לולאת התחזוקה
‏(גיבוי, ‏`pgrep`, ניקוי) בלי לעצור.**

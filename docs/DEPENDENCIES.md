# מדיניות תלויות

‏נכתב ‏02.09.2026 (‏STEP 94).

## הנעילה היא ה-pin

‏`pnpm-lock.yaml` מקבע כל תלות טרנזיטיבית לבייט; ‏CI ופרודקשן מתקינים
ממנו בלבד. **לא** משכתבים את ‏package.json לגרסאות מדויקות: זה מקפיא
עדכוני אבטחה של dependabot בלי להוסיף דטרמיניזם שהנעילה לא נותנת כבר.

## איך מתעדכנים

1. **‏dependabot בלבד** — שבועי, מקובץ, מול ‏main (ה-override ההיסטורי
   שהפנה לענף מת הוסר ‏02.09). ‏auto-merge רק כשה-CI המלא ירוק.
2. **שדרוג ידני** = ‏PR נפרד עם ‏`pnpm add`, לעולם לא עריכת גרסה ידנית
   בלי התקנה (הנעילה חייבת לזוז יחד).
3. **major** של Next / supabase-js / Cardcom-path — רק עם ריצת הרגרסיה
   המלאה (‏e2e שני פרויקטים + compare + build).

## מצב פתוח

‏`pnpm audit --prod`: ‏2 high + ‏1 low טרנזיטיביים — ‏KNOWN-ISSUES ‏#7,
מדווח לילית ב-nightly-health; התיקון מגיע כ-PR של dependabot, לא כעקיפה
ידנית. תלויות מתות מוכחות (‏drizzle) — ‏docs/DEAD-CODE.md, מחיקה באישור.

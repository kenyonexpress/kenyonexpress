# מלאי רכיבי UI מהאתר החי

תאריך: 2026-09-02.
היקף: docs בלבד. זה מלאי של מה שנראה בדפי הייחוס, לא סריקה של
`src/components`.

## מקורות

הקבצים
`refs/ke_live_home.html`,
`refs/ke_live_product.html`,
`refs/ke_live_category.html`,
`refs/ke_live_cart.html`,
`refs/ke_live_checkout.html`
מופיעים ב-
`.gitignore`
ולא נמצאים ב-worktree. הרכיבים חולצו מ-

`KE_LIVE_SPEC.md`,
`MEASURED-LIVE.md`,
`DESIGN-MEASURED.md`,
`HOMEPAGE_SPEC.md`.

עמודות הטבלה:

- **מופיע ב:** home / product / category / cart / checkout
- **מצבים:** default, hover, empty, mobile, ועוד
- **RTL:** מה חייב להישאר לוגי ב-`dir="rtl"`

---

## פס עליון (top bar)

| שדה | ערך |
|---|---|
| מופיע ב | home, product, category, cart, checkout (כל דפי החנות) |
| תוכן חי | ברוך הבא לעולם של קניון Express · בפריסה ארצית · משלוח מהיר חינם · קניה בטוחה · התחברות |
| מצבים | דסקטופ: שורה דקה ~38px. מובייל 375: הכותרת הראשית יושבת מתחתיו (top 113), כלומר הפס נשאר או מתקפל ל-off-canvas |
| RTL | רשימת הקישורים מתחילה מימין. "התחברות" בקצה ה-inline-end הוויזואלי (שמאל בדסקטופ LTR, ימין ב-RTL אם הסדר ב-DOM הוא start-first) |

---

## כותרת עליונה (header / masthead)

| שדה | ערך |
|---|---|
| מופיע ב | כל חמשת הדפים |
| תוכן חי | לוגו 300×79 · שדה חיפוש "Search for:" / "חיפוש" · בורר אזור (16 אזורים) · סל · חשבון |
| מצבים | דסקטופ: גובה 110px, חיפוש 534×41, אייקונים בשורה שמאל פיזית (LTR measure: icons at x=120). מובייל: masthead הדסקטופ מתכווץ ל-0; handheld header 375×48.63, רקע לבן. לוגו הדסקטופ 0×0 במובייל |
| RTL | לוגו ב-inline-start (ימין). חיפוש במרכז. אייקוני סל/חשבון ב-inline-end (שמאל). שדה החיפוש מעוגל ב-inline-start של הכפתור (רדיוס 22 0 0 22 על הכפתור במדידת LTR). אסור `left`/`right` קשיחים |

בורר האזור בחי (16 ערכים): תל אביב, רמת גן – גבעתיים – בני ברק, חולון – בת ים – ראשון לציון, פתח תקוה, השרון, נתניה והסביבה, חדרה והסביבה, ירושלים והסביבה, השפלה, רחובות – נס ציונה, אשדוד – אשקלון, חיפה והקריות, גליל תחתון, גליל עליון, גולן, באר שבע והסביבה, אילת.

בפרויקט: בורר האזור והחיפוש הוסרו מהכותרת. הכותרת היא לוגו + 3 אייקונים. זה סטייה מכוונת, לא רכיב חסר בטעות.

---

## תפריט מגה / מחלקות

שני משטחים נפרדים בחי, לא אחד.

### תפריט קטגוריות עליון (nav אופקי)

| שדה | ערך |
|---|---|
| מופיע ב | home בעיקר; בשאר הדפים לפי תבנית Electro (nav מתחת ל-masthead) |
| תוכן | 11 מחלקות לפי הסדר החי: דילים חמים, עד ₪99, החדשים, מסעדות ובתי קפה, יופי בריאות וטיפוח, טלפונים מחשבים ואביזרים, תינוקות וילדים, צימרים ובתי מלון, ציוד ומזון לבעלי חיים, בעלי מקצוע, קורסים Express (בקרוב) |
| מצבים | default · hover על פריט · "בקרוב" לא ניתן ללחיצה · מובייל: נבלע במגירה |
| RTL | הפריט הראשון ב-DOM יושב ב-inline-start (ימין). מפריד ₪ ב-`dir="ltr"` בתוך התווית |

### סיידבר מחלקות אנכי (Departments / mega)

| שדה | ערך |
|---|---|
| מופיע ב | home בשורת ההירו הימנית (~25% / 241px). לא בדף מוצר. בדף קטגוריה Electro יש סיידבר פילטרים, לא אותו מגה |
| מצבים | רשימה סטטית בבית. hover שורה. במובייל: הסיידבר נעלם מהגריד ונפתח ממגירה / כפתור "מחלקות" |
| RTL | העמודה הראשונה ב-DOM ב-RTL היא ויזואלית ימין. כותרת "מחלקות" על רקע צהוב |

אין מגה-תפריט עם פאנל תמונות כמו חנויות LTR של Electro home-v7 האנגלי. החי הוא רשימת מחלקות + nav עליון.

---

## כרטיס מוצר

| שדה | ערך |
|---|---|
| מופיע ב | home (גריד `columns-3`), category (6 בעמודה דסקטופ / 2 במובייל), product (related), cart (לא; שורת טבלה במקום), checkout (שורת סיכום, לא כרטיס) |
| מבנה DOM חי | `.category` קישור מעל · תמונה · `woocommerce-loop-product__title` · badge `onsale` בפינה · מחיר `del` + `ins` / `.woocommerce-Price-amount` |
| מצבים | default · hover צל על רצועת קטגוריות (`0 0 18px -2px rgba(0,0,0,0.2)`) · מבצע (יש del+ins+badge) · מחיר מלא בלי מבצע (רק amount ב-`#333e48`) · אייקון הוספה לסל שקוף · מובייל: כרטיס 172.5px רוחב, תמונה 144.5, כותרת 12px |
| RTL | תג הקטגוריה והכותרת מיושרים ל-start. באדג' `onsale` בפינת inline-end של התמונה (שמאל ב-RTL). מחיר `dir="ltr"`. אייקון ATC ב-inline-start של פוטר הכרטיס |

דסקטופ קטגוריה: כרטיס 234px, תמונה 186×186, בלי padding ובלי מסגרת. בית: 3 עמודות.

---

## בלוק מחיר

| שדה | ערך |
|---|---|
| מופיע ב | כרטיס (home/category/related), PDP (product), שורת עגלה (cart), סיכום הזמנה (checkout) |
| מצבים | רגיל (סכום יחיד `#333e48` או `#dc3545` לפי הקשר) · מבצע (`del` אפור strike + `ins` אדום) · PDP דסקטופ: נוכחי 35px `#dc3545`, מחוק 21px `#848484` · כרטיס: נוכחי 20px, מחוק 12px `#768b9e` · מובייל כרטיס: 16px / 9.6px · checkout: שורת סכום, לא 35px |
| RTL | כל מחרוזת מחיר ב-`dir="ltr"` כדי ש-₪ והספרות לא יהפכו. `del` לפני `ins` ב-DOM החי (המחוק משמאל לסכום ב-LTR; ב-RTL צריך logical: המחוק ב-inline-end או לפי החי כפי שנמדד) |

סמל המטבע הוא ₪, לא NIS טקסטואלי. כסף בקוד הוא אגורות integer; הבלוק רק מציג.

---

## באדג'ים

| באדג' | מופיע ב | מצבים | RTL |
|---|---|---|---|
| `onsale` ירוק `#44b81b`, טקסט לבן 12/700, רדיוס 4, padding 2/10 | כרטיס ב-home/category/related כשיש מחיר מחוק | חסר כשאין מבצע. מובייל: ≈48×28 בפינת התמונה | פינה פיזית שנמדדה ב-375: left 298 מתוך 360, כלומר בצד שמאל של הכרטיס הימני: ב-RTL זו פינת inline-end |
| באדג' סל צהוב 21×21, טקסט `#333e48` 12px | header בכל הדפים כשיש פריטים | 0 פריטים: הבאדג' עדיין קיים בחי עם מספר; ריק = 0 או מוסתר לפי Woo | על אייקון הסל, לא על הלוגו |
| "בקרוב" על קורסים | nav מחלקות | לא לחיץ | תווית ליד השם, לא מחליפה אותו |
| "קופון" / "פיזי" / "משלוח" | לא באתר החי של WP; יעד הפרויקט | default / חסר | start של הכרטיס או על התמונה, לא `right-2` פיזי |

---

## פירורי לחם (breadcrumbs)

| שדה | ערך |
|---|---|
| מופיע ב | product, category. לא ב-home. cart/checkout: Woo לפעמים מציג; נמדד ב-category/product |
| category דסקטופ | רוחב מלא של העמוד, פונט 14 `#333e48` |
| category מובייל | 375×41.98, top 115.75, padding 9px |
| product מובייל (מ-`ke_live_computed` דרך `COUPON-STOREFRONT-SPEC.md`) | 0,116, 380×55 |
| מצבים | בית > מחלקה · בית > מחלקה > מוצר · עמוד 2 של ארכיון לא משנה את הפירור |
| RTL | מפריד בכיוון הקריאה (בית מימין, העמוד הנוכחי בקצה). אסור שרשרת LTR עם `>` פיזי בלי `dir="rtl"` |

---

## פוטר

| שדה | ערך |
|---|---|
| מופיע ב | כל חמשת הדפים |
| מבנה חי | 1. פס ניוזלטר צהוב 80px: "קנה וחסוך, הירשם ל Newsletter לקבלת הנחות והטבות $ נוספות . . ." + אימייל + Phone + "הירשם". 2. אזור ווידג'טים `#f8f8f8`, padding ~60/62: עמודה יצירת קשר (500px) + תפריטים (700px). 3. פס זכויות `#eaeaea` 45px: "כל הזכויות שמורות © Kenyon Express" + לוגואים תשלום 324×42 |
| יצירת קשר | "יש לך שאלות, הצעות או הערות ? צור קשר" · כתובת: פארק העסקים, התעשייה וההיי-טק. Air Port City · Facebook, X, WhatsApp, Instagram, YouTube |
| שירות לקוחות | צור קשר, תקנון |
| אזור אישי | החשבון שלי, סל הקניות, מועדפים, הסטוריה, הזמנות |
| מצבים | דסקטופ: שתי עמודות. מובייל: אזור הווידג'טים נמדד 0×0 ב-375 (נערם / נכנס ל-offcanvas או שהסלקטור פספס). פס הניוזלטר והזכויות קיימים ב-CSS גם כשה-rect הוא 0 |
| RTL | עמודת הקשר ב-inline-start (נמדד: contact ב-x=828, menus ב-x=128 ב-1440 LTR measure: ב-RTL ויזואלית הקשר ימין). אייקוני רשתות בשורה אופקית מ-start. לוגואים תשלום ב-inline-end של פס הזכויות |

---

## מגירת מובייל (handheld / drawer)

| שדה | ערך |
|---|---|
| מופיע ב | כל הדפים מתחת ל-breakpoint של Electro handheld (נמדד כרכיב נפרד מ-masthead) |
| דסקטופ | handheld header 0×0, לא פתוח |
| מובייל | handheld header 375×48.63, top 113 (מתחת לפס עליון), רקע לבן. תפריט המחלקות והחיפוש נכנסים ל-off-canvas של Electro (`off-canvas-navigation` / hamburger) |
| מצבים | סגור (רק המבורגר + לוגו + סל) · פתוח (רשימת מחלקות + חיפוש + חשבון, overlay) · סל: מגירה נפרדת או מיני-cart, לא אותה מגירת ניווט |
| RTL | המגירה נפתחת מ-inline-start (ימין). כפתור סגירה ב-inline-end של הכותרת הפנימית. אסור `left-0` על הפאנל. overlay מכסה את השאר. מיקוד נעול בפנים, Escape סוגר |

מגירת סל (לא ניווט): נפתחת מאייקון הסל. שורות, סכום, CTA לקופה. במובייל זו החלופה לעמוד העגלה אחרי הוספה, עד מעבר ל-
`/cart`.

---

## רכיבים נוספים שנראים בדפים

### בית

| רכיב | מצבים | RTL |
|---|---|---|
| שורת הירו 3 טורים | דסקטופ: מחלקות 241 / סליידר 728 / באנרים 201. מובייל: נערמים, סליידר מעל, באנרים מתחת. `display:none` על `slider-das-block` מתחת ל-lg (מלכודת lazy-load) | טור ראשון ב-DOM = ימין |
| Slider Revolution, 5 שקופיות | auto-play, dots צהובים 30×8 פעיל / 8×8 כבוי. שקופיות: ברוכים הבאים, THE NEW STANDARD, ממשק מהיר ונוח, תצוגה מושלמת, בקרוב האפליקציה | טקסט ב-start, תמונה ב-end |
| 3 מיני-באנרים | Shop the Hottest Products / Catch Big Deals / Laptops Notebooks. CTA "Shop now" 26px צהוב | טקסט מעל הכפתור, יישור start |
| רצועת קטגוריות 5 | גובה 170, תמונה 100, תווית 14/600. offset ימין, לא ממורכז | פריט ראשון ב-start |
| גריד מוצרים 3 עמודות | hover כרטיס, ATC אייקון | כמו כרטיס מוצר |
| Feature bar, 5 | לכל חלקי הארץ · קניה חכמה · שירות לקוחות · מחירים מנצחים · מותגי יוקרה מובילים | אייקון ואז טקסט, מפרידים logical |
| כפתור WhatsApp צף | מספר 972524635550, פינה תחתית. תמיד | פינה inline-start תחתונה (ימין-למטה ב-RTL) |

### מוצר

| רכיב | מצבים | RTL |
|---|---|---|
| גלריה `--columns-5` | דסקטופ 470×477, מובייל 345×351 (380: ~356h). thumbs לא נמצאו במדידה על מוצר לדוגמא | גלריה ב-inline-end דסקטופ (שמאל ב-RTL: נמדד gallery ב-x=835) |
| `summary` | כותרת, מחיר, כמות, הוספה לסל, תגיות, SKU | סיכום ב-inline-start (ימין). מובייל: מתחת לגלריה |
| כמות + ATC | דסקטופ ATC 192×53 צהוב פיל. מובייל ATC full-bleed 345×53, רקע כהה בחי | כמות ליד הכפתור ב-start |
| related | דסקטופ כמה כרטיסים. מובייל 2 בעמודה (172.5), נמדד 1 פריט במוצר לדוגמא | כמו גריד כרטיסים |
| אין טאבי ביקורות | `woocommerce-tabs` חסר | לא להוסיף כדי "להשלים Electro" |
| אין wishlist / compare ב-PDP החי | | לא להוסיף ל-v1 |

### קטגוריה

| רכיב | מצבים | RTL |
|---|---|---|
| H1 מחלקה | 25px / 500 / line 40 | יישור start |
| ספירת תוצאות | 14px `#333e48` | לפני פס הבקרה |
| פס בקרה `#efefef` | דסקטופ רדיוס 9 גובה 46. מובייל רדיוס 0 גובה 51. view switcher `display:none` במובייל | select ב-start |
| גריד | 6 דסקטופ / 2 מובייל. פילטר סיידבר NOT FOUND במובייל | |
| pagination | NOT FOUND במדידת העמוד הקטן | מפרידי prev/next מתהפכים ב-RTL |

### עגלה

| רכיב | מצבים | RTL |
|---|---|---|
| כותרת "עגלה" | 40px דסקטופ | start |
| טבלת שורות | th 14 `#747474`, td 17 `#333e48`, thumb 92, כפתור X `#a7a7a7` 25px | עמודות logical; מחיר ltr |
| כמות 85×40 רדיוס 14 | | |
| סיכום + קופה צהובה | ריקה: מצב empty בלי טבלה | CTA ברוחב הסיכום |

### קופה

| רכיב | מצבים | RTL |
|---|---|---|
| טופס פרטים + סיכום הזמנה | sandbox Cardcom ב-iframe / redirect. שגיאת שדה. ביטול חזרה לעגלה | תוויות מימין לשדות. מספר כרטיס ltr |
| אין guest נפרד בחי WP (חשבון Woo) | מחובר / התחברות באמצע | |

---

## סיכום הופעה לפי דף

| רכיב | home | product | category | cart | checkout |
|---|---|---|---|---|---|
| top bar | כן | כן | כן | כן | כן |
| header | כן | כן | כן | כן | כן |
| מגה / מחלקות עליון | כן | כן (nav) | כן | כן | כן |
| סיידבר מחלקות הירו | כן | לא | לא | לא | לא |
| כרטיס מוצר | כן | related | כן | לא | לא |
| בלוק מחיר | כן | כן | כן | שורה | סיכום |
| באדג' onsale | כן | related | כן | לא | לא |
| באדג' סל | כן | כן | כן | כן | כן |
| breadcrumbs | לא | כן | כן | לעתים | לעתים |
| footer | כן | כן | כן | כן | כן |
| handheld drawer | מובייל | מובייל | מובייל | מובייל | מובייל |
| WhatsApp צף | כן | כן | כן | כן | כן |

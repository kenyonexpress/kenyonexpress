# יומן משיכת תמונות מהאתר הישן

## מצב: ‏BLOCKED על שני קלטים אנושיים (‏07.09.2026)

**לא נכתב `scripts/scrape-legacy-images.mjs`, וזו החלטה ולא דילוג.**
ראה `docs/DECISION-LOG.md` ‏D-005.

### מה כבר קיים ועושה את זה טוב יותר

‏`scripts/wp-import/06-media-sync.mjs`, שלב 6 בצנרת ייבוא בת שישה שלבים:

| מה שהמשימה ביקשה | מה שקיים |
|---|---|
| הורדה מהאתר הישן | ‏`fetchBytes` עם טיפול בכשלים |
| המרה ל-WebP | ‏sharp, ‏5 נגזרות |
| שלושה גדלים (380/768/1440) | ‏1600 ‏/ ‏800 `.card` / ‏320 `.thumb`, **plus** ‏OG ב-1200x630 |
| העלאה ל-R2 בנתיב יציב | מפתח **מכותב-תוכן** עם `sha256` של המקור |
| דילוג על מה שכבר קיים | אידמפוטנטי מעצם המפתח: ריצה חוזרת דורסת את אותו אובייקט |
| יומן כשלים | ‏`Run`, ‏`warn`, ‏`ok` עם דוח לכל שלב |

**שתי בחירות שלו טובות מהמפרט שביקשתי לממש.** הגדלים אינם 380/768/1440
אלא 320/800/1600, כי אלה המועמדים האמיתיים ל-`srcset` ולא רוחבי הפריסה:
תמונה של 1600 בלבד בתא עגלה של 320 מורידה את הקובץ המלא ומקטינה בדפדפן,
מה שנראה נכון ושוקל לא נכון. ובנוסף הוא פולט `og.jpg` ליד `og.webp`, כי
‏WhatsApp הוא המקום שבו דיל באמת משותף בשוק הזה וחלק מגרסאות התצוגה
המקדימה שלו לא מרנדרות webp, כלומר השיתוף מאבד את התמונה בשקט.

**ומפתח מכותב-תוכן עדיף על `products/<id>/hero.webp`:** ריצה שנקטעה
מחשבת את אותם מפתחות בדיוק וכותבת מעל אותם אובייקטים, ואותה תמונה
שמופיעה בשני מוצרים נשמרת פעם אחת.

### שני החסמים, ושניהם אנושיים

1. **‏R2 אינו מוגדר.** ‏`isR2Configured()` מחזיר `false`. אין מפתחות,
   ואני לא מבקש ולא מסיק סודות.
2. **אין פלט משלב 02.** הצנרת מתחילה מייצוא ‏XML של WordPress, ואין כזה
   בעץ. שלב 6 מסנכרן את מה ששלבים 1 ו-2 חילצו, ואין מה לסנכרן.

**מה שכן אפשר ביום שיהיו:** ריצת יבש. היא מורידה, מגבבת וממירה מקומית
בלי להעלות ובלי לכתוב ל-DB, וזו בדיוק הדרך לגלות כמה קבצים ישנים מחזירים
‏404 **לפני** יום המעבר ולא במהלכו. הסקריפט תוכנן לזה במפורש.

### מה יירשם כאן כשהחסם ייפתח

לכל כשל: ה-slug, ה-URL שנוסה, והסיבה. תמונות חסרות אינן חוסמות; מוצר בלי
תמונה ישנה מקבל placeholder שנרשם ב-`docs/PLACEHOLDER-CONTENT.md`.

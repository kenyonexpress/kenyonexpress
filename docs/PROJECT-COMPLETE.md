# ‏KenyonExpress — הפרויקט הושלם

‏**נכתב ‏19.08.2026, בילד `qOQvgn9UgRyJr71kDu3ia`, ענף `phase5/homepage`.**

כל מספר במסמך הזה נמדד. איפה שלא נמדד — כתוב "לא נמדד", ולא "עובר".

---

## ‏ההכרעה בשורה אחת

‏**הקוד גמור. מה שחוסם השקה מסחרית אינו באג אלא נתונים, מפתחות והחלטות של
אופיר.** ‏`docs/LAUNCH-READINESS.md` מכריע ‏**NO-GO להשקה מסחרית, GO ל-RC
ולבדיקות פנימיות**, וההכרעה הזאת עומדת.

שלושת החוסמים המוחלטים, בסדר הזה:

1. ‏**מפתחות Cardcom של פרודקשן לא הוזנו.** בלעדיהם לא ניתן לגבות שקל אחד.
2. ‏**‏`SUPABASE_SECRET_KEY` המקומי אינו של הפרויקט הזה** (`iss=supabase-demo`).
   כל נתיב admin-client נכשל מקומית. בפרודקשן זה משתנה של Vercel ולא נבדק כאן.
3. ‏**אין תוכן אמיתי: ‏0 ספקים עם כתובת רחוב**, ‏5 מתוך 11 עם עיר.

---

## השערים, כפי שנמדדו על הבילד הזה

| שער | תוצאה |
| --- | --- |
| ‏`tsc --noEmit` | ✅ **0** |
| ‏`biome lint .` | ✅ **0** על **967** קבצים |
| ‏`vitest run` | ✅ **‏2934 / 2934** ב-**223** קבצי טסט |
| ‏`pnpm build` | ✅ הידור נקי |
| שער פיקסלים — בית | ✅ **9.83%** |
| שער פיקסלים — עגלה | ✅ **8.60%** |
| שער פיקסלים — צ'ק-אאוט | ✅ **9.72%** (היה 12.43%) |
| שער פיקסלים — מוצר / products / קטגוריה / חיפוש | ⛔ הכלי **מסרב**, ולא במקרה. ראה למטה |
| ‏axe על כל המסלולים הציבוריים | ✅ **0 הפרות** |
| ‏E2E Playwright, סוויטה מלאה | ⚠️ **‏335 עברו, ‏6 דולגו, ‏9 נכשלו** — כולם אותו חסם סביבה, ראה למטה |

### תשעת הכשלים ב-E2E, ותיקון לטענה שעמדה כאן קודם

‏**כל התשעה על `mobile-chrome`, וכולם זריעת עגלת אורח.** השרת אומר את הסיבה
בעצמו, ‏177 פעמים בלוג של אותה הרצה:

```
supabase.admin_key_invalid — SUPABASE_SECRET_KEY is the stock local-development
demo key (iss=supabase-demo), which the hosted project rejects as an invalid
API key. Every admin-client path — the guest cart, ... — fails with it.
```

‏**‏`STATE.md` טען שהכשלים האלה "עוברים בהרצה ממוקדת על worker אחד". נמדד
עכשיו: הם לא.** הרצה ממוקדת על `mobile-chrome` עם worker אחד: **‏64 עברו,
‏2 דולגו, והתשעה נכשלו שוב.** הטענה הייתה נכונה בזמנו ואינה נכונה היום, והיא
מתוקנת כאן ולא מושארת לעמוד.

הודעת הכישלון עצמה שופרה כדי שלא תדרוש את הפסקה הזאת בפעם הבאה:

```
add-to-cart did not stick: the header badge went 0 -> 0. No error toast was
raised... If the server log shows `Invalid API key`, this is the known
SUPABASE_SECRET_KEY blocker and not a regression.
```

‏**זה חוסם #2 ברשימת החוסמים, ואינו ניתן לפתרון מכאן.** ‏`chromium` עובר את
אותם טסטים.

---

## מודל העסק, כפי שהוא ממומש

‏**קופון:** אדמין קובע `coupon_price` כסכום **מוחלט**. הלקוח משלם אותו במלואו
באתר דרך Cardcom, ו-**כל הסכום נשאר אצל הפלטפורמה לצמיתות**. ‏**אין Escrow ואין
תשלום לספק מכסף הקופון.** היתרה נגבית בבית העסק בסריקה, ואחרי הסריקה השובר
מומש לצמיתות. פקיעה אוטומטית לפי `offer_valid_until`.

‏**פיזי:** תשלום מלא באתר, פיצול מיידי לפי `platform_percent` **פר מוצר**,
**מצולם ל-`order_items`** ובלתי משתנה משם.

‏**כסף:** אגורות שלמות בלבד, לעולם לא float, הכל דרך `src/lib/money.ts`.

‏**‏`escrow` אינו מופיע ב-SQL של הזריעה אף פעם אחת**, ומודל העמלה נגזר פר שורה:
‏`coupon_absolute` לקופון, ‏`physical_percent` לפיזי — בדיוק כמו הפרודקשן.

---

## מצב לפי שלב

הטבלה המלאה עם הראיות: ‏`docs/LAUNCH-READINESS.md`. בקצרה:

| # | שלב | מצב |
| --- | --- | --- |
| 10 | ‏DB HARDENING | ⚠️ ‏advisors ביצועים **0 WARN**; אבטחה **‏23-25 WARN ארכיטקטוניות** שאי אפשר לאפס בלי לשבור RLS |
| 11 | ‏AUTH | ✅ ‏`docs/AUTH-MODEL.md`, ‏48 route-ים ב-CI, מניפסט RLS |
| 12 | ‏PAYMENTS VERIFY | ✅ סתירת ה-Escrow נסגרה, ‏webhook idempotency, ‏reconcile |
| 13 | ‏VOUCHERS VERIFY | ✅ ‏audit immutability **נמדד מול פרודקשן** |
| 14 | ‏WHATSAPP+SHARE | ✅ |
| 15 | ‏GEO | ✅ קוד / ❌ נתונים (‏0 ספקים עם כתובת) |
| 16 | ‏WP IMPORT | ✅ dry-run בלבד לפי **D10** |
| 17 | ‏STOREFRONT VERIFY | ⚠️ ראה שער הפיקסלים למטה |
| 18 | ‏GO/NO-GO | ✅ |
| 19 | ‏ACCOUNT AREA | ✅ ‏8 מסכים |
| 20 | ‏SUPPLIER PORTAL | ✅ ‏6 מסכים |
| 21 | ‏EMAIL TEMPLATES | ✅ ‏12 תבניות |
| 22 | ‏LEGAL PAGES | ✅ ‏4 דפים |
| 23 | ‏PIXEL WAVE | ✅ נסגר כמדידה, עם ראיה מספרית |
| 24 | ‏SEED CONTENT | ✅ ‏`docs/SEED-REPORT.md` — dry-run, ‏14 ספקים ו-40 מוצרים |
| 25 | ‏BACKUP+RECOVERY | ✅ ‏`docs/DISASTER-RECOVERY.md` |
| 26 | ‏FINAL SWEEP | ✅ שלושה מחזורים |

---

## שער הפיקסלים: מה עובר, ומה שאי אפשר למדוד בכלל

**שלושת הדפים שההשוואה אומרת עליהם משהו — עוברים.** בית 9.83%, עגלה 8.60%,
צ'ק-אאוט 9.72%.

בארבעת הנותרים הכלי **מסרב למדוד**, וזה מצב שלישי ולא כישלון:

- ‏**‏`products`:** שני הצדדים מחזיקים 24 כרטיסים, אבל **‏7 מתוך 24 משבצות
  מסכימות**. אחוז בין שני קטלוגים בסדר שונה הוא פער תוכן שלובש מספר נאמנות.
- ‏**קטגוריה, חיפוש:** ספירות שונות משני הצדדים.
- ‏**מוצר:** ‏`.woocommerce-product-gallery` **באתר החי** נושא `opacity: 0`
  ואין בדף כלל שמחזיר אותו. ‏**התמונה טעונה, פרוסה ובלתי נראית**, וכפיית
  ‏`opacity: 1` מורידה את הדף מ-16.17% ל-**12.56%**. כלומר כרבע ממה שהדף הזה
  חויב בו היה מלבן לבן בצד הייחוס. ‏**זו רגרסיה באתר החי**, ראה שאלה 15.

---

## ‏מה שנמצא בסבב האחרון, ואיך

**ארבעה מהחמישה נמצאו באותה דרך: שער שהחזיק תת-קבוצה של המשטחים, והמשטח
שנשבר היה מחוץ לו.**

1. ‏**‏`/checkout` קפץ ‏CLS 0.219** עם עגלה זרועה — פי שניים מגבול ה"טוב", בדף
   שבו הכסף עובר. שלושה שערים החזיקו את `/checkout` ברשימה ואף אחד לא מדד
   אותו: עגלה ריקה מפנה ל-`/cart`. **‏0.2190 ← 0.0003.**
2. ‏**הצ'ק-אאוט היה 12.43% והשער הוא 11%.** סריקת היסט מצאה מינימום נקי
   ב-**‏67px**, שהוא בדיוק גובה פירורי הלחם שדף העגלה נושא ושהצ'ק-אאוט היה הדף
   היחיד במסלול בלעדיהם. ‏**‏12.43% ← 9.72%.**
3. ‏**ארבעת צבעי ה-toast נכשלו ב-AA**, הגרוע 3.08:1. ‏toast חי ארבע שניות ואין
   לו URL, ולכן אף סריקה לא ראתה אותו. ‏**‏4.71 / 4.61 / 4.68 / 4.62.**
4. ‏**מגירת העגלה כיסתה את הטלפון והמקלדת לא נכנסה אליה.** שמונה Tab ושישה
   Shift+Tab נחתו על פקדים **מתחת ל-overlay**. תוקן, ואומת נכשל בלי התיקון.
5. ‏**תיבת החיפוש הכריזה "combobox מורחב" ואז פתחה מוצר שמעולם לא נקבה בשמו.**
   ‏`aria-activedescendant` לא היה קיים. ‏**‏axe דיווח אפס הפרות על המצב הזה** —
   נמדד — כי אין לו כלל על כך שהאלמנט ש-`aria-controls` מצביע עליו חייב להיות
   ‏listbox. זה סוג הפגם שסורק אינו יכול למצוא.

---

## שאלות פתוחות לאופיר

הרשימה המלאה: ‏`docs/QUESTIONS-FOR-OFIR.md` (‏15 שאלות ממוספרות) ו-
‏`docs/DECISIONS.md` (‏12 החלטות זמניות). התמצית:

1. ‏**מפתחות Cardcom של פרודקשן.** בלי זה אין גבייה. הסנדבוקס עובד.
2. ‏**‏`SUPABASE_SECRET_KEY` תקין.** בלעדיו כל מסלול admin-client נכשל מקומית,
   ו-16 סעיפי ה-QA של האדמין חסומים.
3. ‏**תוכן אמיתי.** ‏0 ספקים עם כתובת רחוב. ‏`(15) GEO` נכתב ונבדק ואין לו על מה
   לרוץ, ואין קישור Waze בפועל.
4. ‏**איזה סט מסמכים משפטיים מחייב** — `/legal/*` או `/terms-and-conditions`.
   שניהם מוגשים היום, וזו החלטה משפטית ולא טכנית.
5. ‏**‏`/coupons` — מסלול חי או שריד של המודל שבוטל?**
6. ‏**האם Vercel דורס `x-forwarded-for`?** אם לא, כל מגבלת קצב מבוססת-IP נעקפת
   בכותרת אחת. **לא שיניתי כלום** כי החלפת כותרת בלי proxy מזיזה את ההנחה ולא
   מבטלת אותה.
7. ‏**דף המוצר באתר החי אינו מציג את תמונת המוצר.** רגרסיה אצלם, ‏`opacity: 0`
   שנשאר בלי מי שיחזיר אותו. כל מבקר רואה ריבוע לבן.
8. ‏**‏D5: התקרה בעגלה היא 99 ולא 5.** לא שיניתי — הורדת תקרה במסלול כסף על סמך
   החלטה שלא אושרה חוסמת רכישה לגיטימית.
9. ‏**‏D6: אין PIN בסריקת הספק.** לא נבנה: מנגנון אימות בלי החלטה על ניהול,
   איפוס ואורך הוא הרחבה שאי אפשר למדוד.
10. ‏**‏D8/D9: אין בקוד מספר תמיכה ואין זהות משפטית.** לא יצרתי placeholder
    שמודפס למשתמש — זו הצגת מידע שקרי. צריך את המספר ואת שם החברה.
11. ‏**האם לזרוע 40 מוצרי דמו?** ‏**המלצתי: לא.** הפרודקשן מחזיק 61 אמיתיים.
12. ‏**‏WCAG 2.2 `target-size` על נקודות הסליידר** — להזיז פיקסלים או לשמור
    נאמנות לאתר החי?
13. ‏**‏DNS cutover** — לא בוצע ולא ניתן לבצע מכאן.
14. ‏**‏12 מיגרציות ממתינות** ב-`migrations/pending` — לא הורצו, ממתינות לאישור.
15. ‏**התג `v1.0.0` תפוס** מ-10.08 ומצביע על קומיט ישן, ו-`v1.3.0` קיים גם הוא.
    לא הזזתי תג שנדחף. השלמת הפרויקט מתויגת בהמשך קו ה-rc.

---

## מה שלא נעשה, במפורש

- ‏**‏`db push` לפרודקשן** — אסור לפי חוק קבוע, ולא בוצע. מיגרציות רק דרך MCP.
- ‏**ייבוא WP אמיתי** — dry-run בלבד לפי D10.
- ‏**זריעת דמו לפרודקשן** — לא בוצעה.
- ‏**דחיפה ל-7% בשער הפיקסלים** — לפי D12 זה אחרי שכל השאר עובר, ומה שנשאר
  מעל השער אינו ניתן לסגירה בטוקנים אלא בתוכן.

# NEXT-GOALS.md

## תור goals 9-20 (נמסר 03.08.2026)

- [x] ✅ **goal 9 Security** — ‏`103` ממתין לאישור + probe. ראה [36] ב-STATE.md.
      **תיקון להנחה בבקשה:** לא "רובן 0 policies". נמדד מול הפרודקשן:
      ‏**44/44 טבלאות עם RLS מופעל**, ‏38 מהן עם policies (עד 11 ב-`products`),
      ורק 6 עם אפס policies — וזו נעילה מכוונת של טבלאות service-role בלבד
      (`payment_webhook_events`, `rate_limits`, `user_rate_limits`,
      `referral_signals`, `search_index_dlq`, `settlement_events`).
      אפס policies + RLS דלוק = דחייה מוחלטת לכל מי שאינו service_role, כלומר
      המצב המהודק ביותר האפשרי, לא פער.
      החור האמיתי היה במקום אחר לגמרי: שבעה **views** עוקפי-RLS חשופים ל-anon.
- [x] ✅ **goal 10 Observability** — ראה [37]. הפער האמיתי: `capturePaymentAlarm` היה no-op בלי DSN, ו-`alertMoneyFailure` היה עם אפס קוראים. נוסף `/api/health`. ‏~~**נשאר פתוח:** structured logging עם request-id~~ **נסגר ב-[47], 03.08.**
- [x] ✅ **goal 11 Supplier portal** — ראה [38]. חסר היה רק "המוצרים שלי"; נבנה. ממצא נלווה: מיגרציה `104` (מדיניות RLS כפולה שביטלה את בדיקת ה-soft delete) **ממתינה לאישור**.
- [x] ✅ **goal 12 Admin dashboard** — כבר קיים במלואו. אומת: `ProductForm.tsx:443` חושף `platform_percent` פר מוצר ו-`ProductsTable.tsx:167` מציג אותו (`לא הוגדר` כשהוא null), ‏CRUD ספקים, דוחות (`analytics`, `payments`, `payouts`, `growth`), ‏RBAC עם `permissions.ts` + בדיקות.
- [x] ✅ **goal 13 Notifications** — [6]. ‏`supabase/functions/notifications-worker`, ‏`notification_outbox`, ‏`api/cron/notifications` (drain + retry), ‏`voucher-email.ts`. מיגרציה `102` (voucher_issued) ממתינה לאישור.
- [x] ✅ **goal 14 Search** — [11]. ‏`lib/search/` (indexer, meili, qstash), ‏`search_index_dlq` + `api/search/index-dlq`. נוסף ב-[36]: רייט-לימיט לשני המסלולים הציבוריים.
- [ ] ⛔ **goal 15 Wallet — חסום, לא ניתן לביצוע כעת.** הארנק קיים (`lib/ledger.ts`, ‏`wallet_accounts/entries/balances/transactions`, ‏`fn_wallet_transfer`) אבל **בשקלים numeric**, למשל `orders.wallet_applied_ils`. הדרישה "ledger כפול-רישום באגורות integer" **היא** `PENDING-money-integer-fix.sql`, שאסור להריץ (הוראה מפורשת 03.08) ו-52 קבצי אפליקציה קוראים את השמות הישנים כשקלים. שינוי צד-קוד לפני הסכימה מכפיל כל מחיר פי 100. ממתין לאישור.
- [x] ✅ **goal 16 Analytics** — `api/a/route.ts` (עם רייט-לימיט), ‏`AnalyticsProvider`, ‏`ViewTracker`, ‏`lib/analytics/` (aggregate + consent + attribution), ‏`admin/analytics`, ‏`ConsentBanner`. בלי כלים חיצוניים.
- [x] ✅ **goal 17 PWA** — ראה [39]. manifest, service worker, offline shell, install prompt, אייקונים בסקריפט. לא היה כלום קודם.
- [x] ✅ **goal 18 Accessibility** — ראה [40]. axe מול הבניה: הליקוי היחיד בכל 6 הדפים היה קונטרסט, 5 זוגות תוקנו בהכהיה מינימלית. 18 בדיקות. **פתוח בנפרד:** שער compare של home אדום ב-23% ולא מ-goal 18 (נמדד לפני ואחרי).
- [x] ✅ **goal 19 Load** — ראה [41]. ‏k6 (בינארי, לא חבילת npm) עם ששת
      התרחישים של סעיף 5 ב-`docs/ARCHITECTURE-TESTING.md` וטבלת הספים 5.3
      כקוד. שני שערים, שלושתם אומתו נופלים: פרודקשן חסום ב-init **בלי דגל
      עוקף**, וכל תרחיש שכותב דורש `LOAD_ALLOW_WRITES=1`. ‏L1 ו-L6 הורצו מול
      build אמיתי. **‏L6 עובר**: ‏search p95 ‏750ms, ‏suggest 677ms, אפס
      רייט-לימיט, ‏183/183 checks. **‏L1 נופל, וזה הממצא**: דף המוצר
      ‏**1.2-1.9s חם**, לא cold cache, מול תקציב של 800ms.
- [x] ✅ **goal 20 Final** — ראה [43]. סריקת שערים על בנייה נקייה, ובה
      **נסגר השער שהיה אדום**: ‏`home` compare ‏**24.16% ← 11.5%**. הגורם
      לא היה [40] ולא האתר החי (שנשלל במדידה: חי מול חי = 1.85%) אלא
      ‏`style={{width:'auto',height:'auto'}}` שנוסף ב-[35] כדי להשתיק אזהרת
      ‏dev של next/image: **סגנון inline מנצח מחלקה**, ולכן `height: 245px`
      חדל לחול ו-**31 מתוך 31** התמונות צוירו באספקט המקור (‏124px עד
      ‏361px). אותה טעות של [17] בכיוון ההפוך, והשמירה של [17] פספסה אותה
      כי היא בודקת "לא התמוטט" על הכרטיס הראשון בלבד.
      בנוסף: ‏`pnpm lint` ‏**44 שגיאות ← 0** (כולל שני `biome-ignore`
      שנימוקם גלש לשורה שנייה ולכן **לא דיכאו כלום**), ו-
      ‏`@next/bundle-analyzer` שנבנה ומעולם לא הוחל הוסר.
      **‏[42] תוקן בהיקפו:** ‏L1 לא עובר. שלוש ריצות רצופות על אותה בנייה
      נותנות ‏product p95 **1120/884/751ms** מול סף 800, כלומר ה-657ms של
      ‏[42] היה הגרלה אחת בתוך רצועת הרעש. הליד הבא נמדד: ‏`product` ו-
      ‏`category` חולקים **רצפה של ~238ms פר בקשה** בזמן ש-`home` הוא 3ms.

---

תור ביצוע סדרתי (עדכון אחרי כל שלב: סימון ✅ + commit+push + עדכון STATE.md).

כללים: בלי Escrow, בלי אחוז hardcoded, `platform_percent` דינמי מצולם, קופון = גבייה מלאה של מחיר הקופון באתר, כסף באגורות integer דרך
`src/lib/commerce/money.ts`
מיגרציות רק דרך MCP `apply_migration` (לא `db push`). אחרי כל שלב: ntfy ל-
`ntfy.sh/kenyon-ofir-limit`

## התור

- [x] ✅ [1] Cart: Zustand guest cart, אגורות integer דרך money.ts, דף `/cart` RTL (טבלת פריטים, מחיר `#E4002B`, בורר כמות 44px, הסרה, סיכום, כפתור צהוב `#fed700` hover `#fedd26`, Heebo, container 1320px), mini-cart dropdown ב-header עם counter, "הוסף לעגלה" בדף מוצר וכרטיסים, snapshot של `platform_percent` לפריט, שני סוגים coupon/physical, בלי Escrow ובלי hardcoded, Vitest, compare.mjs מתחת ל-11%.
- [x] ✅ [2] Checkout UI: דף `/checkout` RTL: שלבים, טופס פרטים עם Zod, ביקורת הזמנה, בחירת תשלום, Google login רק בלחיצת "שלם", עיצוב Electro.
- [x] ✅ [3] Cardcom integration: לפי `docs/ARCHITECTURE-CHECKOUT-CARDCOM-VERIFICATION.md` אבל בלי Escrow: multi-account client, webhook signature, order state machine, payment_events migration דרך MCP apply_migration בלבד, split מיידי לפיזי לפי `platform_percent`, קופון = גבייה מלאה באתר, token כרטיס שמור, תיקון enum finalize.ts:312 במיגרציה ALTER TYPE ADD VALUE.
- [x] ✅ [4] Coupon redemption: יצירת קוד+QR אחרי תשלום, דף `/coupon/[id]` ללקוח, דף `/scan` לספק, פג אחרי סריקה, סטטוסים, RLS.
- [x] ✅ [5] אזור אישי `/account`: פרופיל, היסטוריית הזמנות, הקופונים שלי עם QR, ארנק פנימי (קאשבק לשימוש באתר בלבד).
- [x] ✅ [6] Notifications: Resend + Supabase Trigger + Edge Function: אישור הזמנה ללקוח, התראת מכירה לספק, קופון נסרק.
- [x] ✅ [7] SEO+Performance: sitemap, meta RTL hebrew, JSON-LD products, next/image, Lighthouse 90+.
- [x] ✅ [8] E2E Playwright: קנייה מלאה קופון+פיזי, סריקה, guest cart. **58/58**.
- [x] ✅ [9] Integration pass: rebase כל ה-branches על main לפי סדר תלויות, טסטים ירוקים, merge, push.
- [x] ✅ [10] `feat/growth-core`: תיקון באג ה-x100 ומיזוג. קמפייני הנחה, ניוזלטר double opt-in, referrals, עגלה נטושה. נמצאו ותוקנו עוד שלושה: build שבור, ושני חצאים מתים של משפך ההחזרה.
- [x] ✅ [11] `feat/search-core`: **לא היה חסום**. QStash נופל ל-inline כשהוא לא מוגדר, בדיוק כמו Resend/Sentry/Meili כאן, ומיגרציה 069 הוחלה דרך MCP. נשארה הגדרה בלבד: `SEARCH_WEBHOOK_SECRET` + Database Webhook.
- [x] ✅ [12] Reaper לעגלות שפג תוקפן: מיגרציה 101 + `/api/cron/reap-carts`. ה-CASCADE של תזכורות העגלה הנטושה שונה ל-SET NULL כדי שה-reaper לא ימחק את היסטוריית ההחזרה.
- [x] ✅ [13] ביקורת GO-LIVE מדודה. התור של [1]-[12] נסגר, ולכן השער הבא של הפרויקט הוא `GO-LIVE.md`. כל שורה פתוחה בו נבדקה מול הקוד ומול הפרודקשן במקום להיקרא מהמסמך: אחד משני "החוסמים הפתוחים" כבר תוקן בקוד ומעולם לא סומן, 34 worktrees נסגרו (5 מהם החזיקו עבודה לא מקומיטת שנדחפה קודם), ומספרי הספקים והתמונות עודכנו למדידה. נשאר חוסם אמיתי אחד, והוא נתונים ולא קוד.

- [x] ✅ [14] ריצת ה-WP import ביבש, הסעיף הפתוח הראשון ב-`GO-LIVE.md` שניתן
      לביצוע. שני חוסמים מתוך שמונה נסגרו (מוצר Dokan מוחרג לפי slug, ‏27
      עמודים נכנסו ל-`url_inventory`), ובדרך התגלה שבמלאי הקיים **שורה שניתבה
      את שורש האתר הישן לעותק של מוצר חיתולים**. ‏`redirect_coverage` עבר קודם
      76/76 כי לא ספר עמודים בכלל; עכשיו הוא נופל 98/103 ומוציא בשם את חמשת
      העמודים שחייבים להיכתב. אף שורה לא נכתבה לשום מקום.

- [x] ✅ [15] ‏Lighthouse מובייל, שלא נמדד מעולם (‏[7] מדד דסקטופ). בית
      **72**, מוצר 88, קטגוריה 85. ‏LCP של דף הבית הוא **10.1s**, גרוע
      מה-baseline של 9.2s שרשום ב-GO-LIVE, ואחראי לו קובץ ירו מונפש אחד של
      **777KB** שיוצא גולמי בלי `/_next/image`, פי 8 מהתמונה הבאה. ‏TBT 80ms
      ו-CLS 0: רק המשקל הזה מפריד את הבית מ-90+. התיקון הוא סטילס בנייד
      והנפשה בדסקטופ, ולא בוצע כי הוא משנה עיצוב שנמדד מול האתר החי.

- [x] ✅ [16] התיקון ש-[15] הגדיר ולא ביצע: הירו מוגש **סטילס מתחת ל-1024px
      והנפשה בדסקטופ**, דרך `<picture>` ולא שתי תמונות מוחלפות (‏`display:none`
      לא עוצר משיכה, ולכן הנייד היה משלם על שתיהן). נמדד: ‏**775.8KB ← 27.7KB**
      בנייד, דסקטופ ללא שינוי. בית **72 ← 75**, ‏LCP ‏**10.1s ← 7.7s**.
      **הבניה הראשונה של התיקון שברה את הדסקטופ בשקט** ונתפסה רק במדידת בייטים.
      ‏[15] טעה בהנחה אחת: זה לא היה כל מה שהפריד את הבית מ-90. אלמנט ה-LCP
      עכשיו הוא **באנר הקוקיז**, לא הירו.

- [x] ✅ [17] בעקבות [16], שער הפיקסלים נבדק ולא סומן כרעש: **כל 32 תמונות
      כרטיסי הדילים בדף הבית צוירו לקופסה בגובה 0**. ‏`fill` כותב
      `position:absolute` כסגנון inline, התמונה יוצאת מהזרימה, ו-
      `.p_con__image-wrap` שלוקח את גובהו ממנה מתמוטט. התמונות נמשכו, פוענחו
      ונזרקו. דף הבית 3504px במקום 5492px, והשער **22.44% ← 11.56%**. אף
      שגיאה, אף בקשה שנכשלה, וציון ה-Lighthouse שבשבילו נעשה השינוי **עלה**.

- [x] ✅ [18] השורה ש-[17] השאיר: `CategoryProductCard` מגיש `<img>` גולמי,
      "פוגע במשקל בלבד". **שתי המילים האחרונות היו שגויות.** ‏45 משורות
      התמונה בקטלוג הן picsum, ו-picsum אינה ב-`img-src` של ה-CSP, כלומר
      בשלושת הדפים הן צוירו **תמונה שבורה**. מעבר לאופטימייזר הוא same-origin
      ולכן הן נמשכות לראשונה. ‏`/products` ‏**613.5KB ← 365.3KB** (ו-16
      תמונות מצוירות ← 25). הירו: ארבע שקופיות שהצהירו 100vw ומציירות 193px,
      ‏**326.7KB ← 106.7KB**. **התיקון עצמו החמיר CLS** מ-0.197 ל-0.414
      בקטגוריה, כי תמונה חסומה לא מזיזה כלום; קופסת התמונה מקבעת עכשיו את
      אותם 186.03px שהשלד של הכרטיס כבר הבטיח. ‏Lighthouse נייד: קטגוריה
      **78 ← 85**, מוצרים **60 ← 82**, ‏CLS 0.01 בשניהם.

- [x] ✅ [19] הליד ש-[18] מדד (‏400KiB בכרטיסי הדילים) התגלה כתסמין.
      ‏**האופטימייזר של next רץ על `sharp@0.34.5` משלו** בזמן שהאפליקציה
      פותרת את 0.35.3 שהיא מצהירה, ו-0.34.5 לא יודע לפענח את קובצי ה-AVIF
      של הפרויקט. ‏`optimizeImage` בולע את החריגה ומחזיר את **קובץ המקור**
      בלי לוג: אותה כתובת החזירה 93264 בייטים ב-w=288, ב-w=384 וב-w=640.
      ‏**זו הסיבה ש-[17] ו-[18] מדדו את אותם בייטים בלי קשר ל-`sizes`.**
      הוצמד sharp אחד לכל העץ: **93264 ← 4176**. בנוסף `sizes` שהיה שגוי
      לשני הכיוונים (עודף פי 2.67 בדסקטופ, **חוסר** בין 641 ל-1023) ומדרגת
      288 ב-`imageSizes`. דף הבית בנייד **1330.4KB ← 403.3KB**, ‏Lighthouse
      **78 ← 80**, ‏Speed Index **3.3s ← 1.5s**, שערי הפיקסלים לא זזו.

- [x] ✅ [20] המועמד ש-[19] נקב בשמו: אלמנט ה-LCP של הבית. **הוא באמת באנר
      הקוקיז, והוא באמת חיכה ל-hydration**: ‏3712-3880ms מול FCP של 1012ms,
      ‏slow 4G אמיתי + cpu/4. עכשיו הוא נצבע **בדיוק ב-FCP**, ‏**2.7 שניות**.
      ‏**החצי השני של הטענה של [19] היה שגוי**: הציון לא זז מעבר לרעש
      (‏80/81/80 ← 78/81/82), כי ה-LCP שנצפה בטרייס של Lighthouse היה 615ms
      כבר לפני התיקון, ו-5.1s הוא סימולציה של Lantern על גרף שמול localhost
      מכיל את כל הדף. בדרך נמצא באג בנייה: **שרשור `` `a${x}b` + `c` `` מגיע
      לדפדפן חסר קטעים**, והסניפט הראשון ששלחתי היה regex לא סגור שזורק
      ‏SyntaxError אצל כל מבקר, עם 200 ובלי לוג.

- [x] ✅ [21] הגרף, שהוא מה ש-[20] הוכיח שמחזיק את הבית ב-80. **נסגר: שלושת
      הלידים.** ✅ ‏CSS: ארבעה קבצים חוסמי רינדור לאחד, אותם
      ‏93374 בייטים בבקשה אחת במקום ארבע, ‏`render-blocking` **870 ← 250ms** ו-
      ‏FCP **1.7 ← 1.1s** ב-4 זוגות A/B משולבים. ✅ ‏Sentry: `webpack.treeshake`
      הוא DefinePlugin של webpack ולכן **מעולם לא הוריד בייט** תחת Turbopack;
      הועבר ל-`compiler.define` שכן מגיע ל-node_modules, ‏**1802668 ← 1743875**
      ו-`unused-javascript` ‏78 ← 62KiB. ‏`inlineCss` נוסה ובוטל: המסמך תפח פי 2
      כי הסגנונות נפלטים גם ל-RSC payload. **הציון לא נפרד** כי LCP דו-שיאי
      (‏3.6s או 5.3s) **בשתי הבניות**, וזה הסליידר. ✅ הליד השלישי,
      ‏`cacheComponents` על כל האפליקציה, בוצע: **‏78 שגיאות prerender ← 0,
      ואפס נתיב `ƒ` שאינו API**. הגורם היה שורה אחת לכל route group ולא 64
      עמודים — דף הבית, שאינו קורא שום נתון, היה דינמי אך ורק בגלל ה-layout
      שמעליו. ‏TTFB **385 ← 4ms**, ‏FCP **564 ← 84ms**, ‏4 סבבים משולבים בלי
      חפיפה. ‏Lighthouse נייד **69/77/77 ← 91/78/83** ו-Speed Index
      ‏**5.1-7.2s ← 1.1-1.6s**: הפעם הראשונה בקו [15]-[21] שהציון נפרד.
      **הכותרת `no-store` עצמה לא זזה, ולא יכלה** — התשובה נושאת
      ‏`x-nextjs-postponed`, כלומר יש בה חור לכל קונה, ותשובה כזו אינה ניתנת
      לקאשינג כשלמות. זה החלק ש-[21] ניסח שגוי, וההמשך שלו הוא לקוח קטלוג
      חסר-עוגייה תחת `use cache`.
      ✅ ובעקבותיו סריקת 412px של כל הנתיבים מצאה את אותה טעות שוב בקטלוג
      (`309f1ac`, **365.2 ← 193.9KB** ו-4px גלילה צידית שנעלמו).
      ✅ שתי השורות מ-[19] נסגרו: `DefaultProductCard` נמדד (`2e247e2`,
      **236.3 ← 117.0KB** בדף המוצר), וארבעת מסכי ה-`<img>` הגולמי התבררו
      כשניים נכונים (‏QR ב-data URL) ושניים שהובילו ל-`(main)` שהיה **ברוחב
      2px בנייד** ולרשימת מארחי תמונות בארבעה עותקים (`24c686c`).
      נמצא בדרך ותוקן (`f603ab6`): **6 בקשות 404 בכל צפייה בעמוד** מ-prefetch
      לשישה קישורים בשלד; שניים מהם היו נתיב שגוי לעמוד שקיים, וארבעת העמודים
      החסרים נרשמו ב-`GO-LIVE.md` כי מה שנשאר הוא תוכן. גם 13.6KB פוליפילים
      שהם מודול פנימי של next ולא browserslist. **היקף המיגרציה נמדד**: 25
      קובצי segment config לפני שהבנייה בכלל מגיעה לעמודים, ואז 64 מתוך 73
      קובצי page/layout שהם async — **ואומדן ה-64 התברר כמטעה**: חמישה
      layouts ושני הוקים לא עטופים הורידו 78 שגיאות ל-12.

- [x] ✅ [22] מה ש-[21] השאיר מדוד. **הכל סגור או נדחה במדידה.**
      ✅ **‏(א) הסליידר** (`2eda4f0`): אוטופליי רק אחרי קלט; LCP נייד 9096←~900ms.
      ✅ **‏(ב) קטלוג חסר-עוגייה** (`8e863c7`): `createPublicClient` + `use cache`.
      ❌ **‏(ג) 404 ל-slug מת**: נדחה (עלות שאילתה בכל PDP); נשאר 200+noindex.
      ✅ **‏(ד) הירו בדסקטופ**: still-first + swap אחרי load (ArtDirectedHeroImage).
      ✅ **‏(ה) CartBootstrap**: כבר client fetch מ-`/api/cart`; `no-store` של החור נסגר.
      ✅ **‏E2E mobile-chrome** (`e0bddad`): באנר קוקיז כיסה כפתור התחברות; 5 בדיקות
      היו assertions שגויים ל-dpr/WebP ולא באג אפליקציה. ‏163/174 ← 169/174, בית 84.

- [x] ✅ [23] הסרת Escrow ממשק הספק + helpers. המודל הנעול: אין Escrow; מקדמת
      קופון נשארת בפלטפורמה; יתרה בקופה; `platform_percent` מצולם. כרטיסי
      "מוחזק/שוחרר" הוסרו; `supplierDue` = פיזי מיידי בלבד; `escrow_held` הוסר
      מ-`mark-order-item-redeemed`. QA#6 (לעדכן order_items ב-redeem) **נדחה**:
      ‏085 + harness SQL אוסרים במכוון.

- [x] ✅ [24] Lighthouse בית נייד 84→90+. שיא **93/97**, LCP נצפה ~120–170ms.
      מתכון: באנר LCP עם Arial+גיאומטריה inline+מיקום לפני `{children}`;
      שער mount להירו (בלי badge של שקופית app); `priority` רק למובייל;
      Heebo בלי preload. חציון Lantern על localhost נשאר ~86 (דו-שיא 3.2/4.2s)
      כמו שתועד ב-[20]. המנוף הבא: unused-javascript 62KiB.

- [x] ✅ [25] unused-javascript + פיצול client בדף הבית. ‏62←58KiB unused;
      JS xfer ~307←303KB. ConsentBanner = Server Component + `decideConsent`;
      ProductDealCard בשרת; CartDrawer/Toaster ב-dynamic ssr:false;
      optimizePackageImports ל-lucide. חציון Lantern נשאר ~86 (דו-שיא LCP);
      שיא 92. תקרת localhost כמו [20]/[24].

- [x] ✅ [26] Lighthouse SEO בדף מוצר נייד 58→**92** על מוצר פעיל. גורמים:
      ‏404/לא פעיל בלי noindex ב-metadata, ורוב הקטלוג בלי seo/short/body.
      ‏fallback תיאור עברי + robots noindex ללא-פעיל; קריאה ב-
      ‏`getProductSeoBySlug` (use cache + anon). ביקורת `meta-description` עדיין
      נכשלת תחת streaming metadata (התג אחרי `</head>` הראשון); ‏`htmlLimitedBots: /.*/`
      נמדד והחזיר SEO 100 אבל שבר PPR resume, ולכן בוטל. slug חסר נשאר 58
      (noindex מכוון).

- [x] ✅ [27] sitemap: `createAdminClient` ← `createPublicClient` + `CATALOGUE_TAG`.
      מקומית **3 ← 76** URLs (קטגוריות + מוצרים). בדיקת מקור ב-`sitemap.test.ts`.

- [x] ✅ [28] הוסרו מועדפים + הסטוריה מהפוטר והלב מ-masthead. contact/terms
      נשארים `built: false` (תוכן).

- [x] ✅ [29] `/contact`: טופס + `submitContactForm` → Resend transactional עם
      replyTo, honeypot, rate limit 5/שעה, `CONTACT_TO`. תקנון נשאר חסום.

- [x] ✅ [30] תור הקוד נסגר זמנית. נפתח מחדש אחרי מציאת פערים מדודים.

- [x] ✅ [31] התנגשות שני קבצי `096_*`: `notification_voucher_issued` הועבר ל-
      ‏`102_…` (ממתין לאישור apply_migration). נוסף תבנית `voucher_issued`
      ב-`notifications.ts` (reuse של `buildVoucherEmail`) כדי שה-drain לא יהרוג
      שורות אחרי ההחלה. GO-LIVE עודכן על CategoryProductCard + sitemap.

- [x] ✅ [32] מדידת חציון בית: **87** (5 ריצות: 87/87/87/87/94). שיא 94.
      הוסרו polyfills מיושנים של Next (alias ל-`modern-polyfill.js`); ביקורת
      legacy-javascript נעלמה. חיפוש masthead נטען רק מ-md ומעלה. חציון עדיין
      על תקרת Lantern (FCP דו-שיא 0.9/1.5s על אותו CSS); `inlineCss` כבר נמדד
      כשלילי ב-[21].

- [x] ✅ [33] דילים: `quality={50}` (sizes נשארו מדודים). w=288 ירד
      ‏~14.5KB←~8.7–11KB. ביקורת image-delivery עדיין ~7KiB על תמונת צילום
      (היוריסטיקה של LH, לא sizes שגוי).

- [x] ✅ [34] Playwright מול build: **169 passed / 5 skipped / 0 failed**.

- [x] ✅ [35] דילים: `style={{ width: 'auto', height: 'auto' }}` על Image.

- [x] ✅ [46] הליד ש-[43] מדד ו-STATE.md קרא לו "החור הדינמי של PPR":
      ‏**הוא לא היה PPR, הוא היה שאילתה אחת**. ‏TTFB הוא 4ms בשני הדפים; מה
      שעולה 268-289ms (מוצר) ו-273-327ms (קטגוריה) הוא **התשובה המלאה**, מול
      סיבוב keep-alive חם ל-Supabase מכאן של **266-313ms**. שני שורדים מ-[42]:
      ‏`RelatedProducts` קרא דרך `createClient()` רמה אחת מתחת לארבע הטבלאות
      ש-[42] קאשש, ו-`generateMetadata` של הקטגוריה ירה שאילתה משלו בזמן
      ש-`getCategoryBySlug` כבר `use cache` על אותן שתי עמודות בדיוק.
      אחרי: מוצר **1.8-6.9ms**, קטגוריה **6.5-14ms**. דף המוצר **סטטי לגמרי**
      ‏(`x-nextjs-prerender`, ‏`s-maxage=3600`) במקום `private, no-store`.
      ‏**‏k6 L1 עבר, השער שאדום מאז [41]**: ‏product p95 ‏**1120/884/751 ←
      ‏16.81ms** מול 800, שלוש ריצות נקיות ברצף ב-100% checks. ריצה רביעית
      חצתה את הסף בזמן ששתי ריצות compare החזיקו את המכונה, וזה נרשם ולא נמחק.
      ‏`generateStaticParams` ל-12 הקטגוריות (העוזר ישב בלי קורא מאז שהקובץ
      נכתב) הוריד פגיעה קרה מ-**1.769s ל-0.285-0.671s**.
      **שלושה דברים שהזירוז שבר, כולם נמצאו במדידה ותוקנו כאן:**
      ‏**(א)** כל ניווט צד-לקוח למוצר מת על "Connection closed" בזמן שכל טעינה
      ישירה נראתה מושלמת: עם הגוף עדיין עטוף ב-`<Suspense>` שורשי, בקשת מסמך
      החזירה מוצר שלם (‏94338B) אבל בקשת RSC החזירה את קליפת ה-fallback,
      ‏**31852B זהים לכל slug** בלי שם מוצר ועם משבצות `$W` לא פתורות. ‏6/6.
      ‏**(ב)** הוספה לעגלה הפסיקה לפתוח את המיני-קארט: ‏`MiniCartDropdown` סגר
      את המגירה מ-effect על `pathname`, שרץ **גם ב-mount**, ו-`usePathname`
      מתייצב אחרי hydration. זה היה בלתי מזיק רק כל עוד הדף עלה בסיבוב DB;
      ב-2ms ה-effect נחת **אחרי** ש-add-to-cart הדליק `drawerOpen`, ולכן
      הפריט נוסף, המונה הראה את הסכום האמיתי, והפאנל נסגר בשקט. ‏5/5.
      ‏**(ג)** ‏`home.spec.ts` "reaching the footer costs no 404s" האדים,
      **ובצדק**: ‏**8 מתוך 32** ה-slugs של דילי הבית אינם קיימים בקטלוג, ולכן
      רבע מהגריד ירה רינדור שרת שחזר 404 **בכל צפייה בדף הבית**. סמוי כל עוד
      ה-PDP היה קליפת PPR. ‏`prefetch={false}` על הכרטיס, ושמונת ה-slugs
      נרשמו ב-GO-LIVE.md כעבודת נתונים. ‏12/12 הקטגוריות חיות.
      **שמירה ולא רק תיקון:** ‏`lib/catalogue-render-path.test.ts` הולך על
      **כל גרף הייבוא הטרנזיטיבי** של כל נקודת כניסה קטלוגית ונופל על כל מודול
      שמגיע ללקוח קורא-העוגיות (מודולי `'use server'` מוחרגים עם הנימוק).
      אומת שנכשל כשמחזירים את הבאג, ונוקב בשם המסלול המדויק.
      ‏**191 passed / 3 skipped / 0 failed** (הבסיס ב-[43] היה 187/5/2),
      ‏1361/1361, ‏compare בית **11.5% ← 9.73%** וקטגוריה 9.09% ללא תזוזה.

- [x] ✅ [52] Open Graph + share. **קישור השיתוף שציטט את המחיר הלא נכון,
      והכרטיס שנבנה והגיע לכלום.** כפתור הוואטסאפ שרשר `shekels(price)`, ועבור
      קופון זה `price_ils` - מחיר הסחורה **בבית העסק**. מי ששיתף קופון של ₪80
      שלח **"₪200"**, והחבר נחת בדף שמצטט ₪80. פעם שלישית שהכלל נדרש כאן.
      ‏`lib/share/message.ts` הוא המקום היחיד; קופון בלי מחיר **לא מצטט כלום**.
      **הסיכון שנרשם מראש אומת:** ל-Satori אין פונטי מערכת, וכרטיס עברי בלי
      ‏face **מצייר ריק והבנייה נשארת ירוקה**. ‏Heebo כ-TTF (‏woff2 אינו נקרא),
      ‏44KB למשקל, ושער שבודק את **כותרת ה-sfnt** ולא רק קיום.
      **הממצא שהיה נשלח ונראה גמור:** עם `openGraph.images` במטא-דאטה,
      ‏`opengraph-image.tsx` **מעולם לא נוצל** - מוסכמת הקובץ ממלאת את השדה רק
      אם המטא-דאטה לא תפסה אותו. הכרטיס נבנה, הופיע ברשימת הנתיבים, החזיר 200
      בשליפה ישירה, **והגיע לכלום**.
      שני תיקונים שה-PNG-ים חשפו: לוח לבן ריק על מוצר בלי מחיר (**4 מתוך 61**),
      ופסיק שנחת בצד הלא נכון (ל-Satori אין bidi). לדף הבית לא היה כרטיס בכלל.
      שיתוף בפייסבוק נוסף; ה-sharer מקבל **כתובת ובלבד**, ולכן הכרטיס הוא
      **התוכן** של השיתוף. שער הטוקנים תפס hex גולמי בשלושת הקבצים ובצדק.
      ‏**1637/1637** (היו 1609), ‏tsc+lint על 724, ‏build 0, שני הכרטיסים נבדקו
      **כפיקסלים**.

- [x] ✅ [51] Sitemap + robots + feeds. **ה-lastmod שלא נשא מידע, והבאג שרק
      שליפת הקובץ הבנוי חשפה.** מחצית ה-goal כבר הייתה קיימת ולא נכתבה מחדש;
      מה שהמדידה מצאה בה: ארבע הכניסות הסטטיות נשאו `lastModified: new Date()`.
      ‏lastmod שהוא תמיד "עכשיו" טוען שכל הדפים השתנו בכל שליפה, **וגוגל מתעלמת
      מ-lastmod לא מדויק לכל הקובץ ולא פר URL** - ארבעה תאריכים לא כנים עלו
      במחיר של המדויקים בכל מוצר. נגזרים עכשיו מה-`updated_at` הכי חדש;
      ‏`/contact` בלי lastmod בכלל. אומת: `2026-07-29T03:15:46Z` במקום זמן הבקשה.
      ‏**`g:price` הוא השדה שמשעה חשבונות Merchant**, ולכן הוא מגיע
      מ-`buildCouponOffer` ולא מ-`price_ils` (שעבור קופון הוא מחיר הסחורה **בבית
      העסק**; פרסומו היה מפרסם 200 על מה שהקופה מחייבת 80). נמדד: **15 פריטים
      עם `g:sale_price` אמיתי, בדיוק 15 הקופונים.**
      **הבאג ששליפת הפיד הבנוי מצאה:** ‏`g:image_link` היה נתיב יחסי -
      ‏**27 מתוך 46 המוצרים הפיזיים**, כמעט חצי הפיד, כל אחד דחיית פריט. כל שכבה
      מעליו נראתה נכונה: העמודה, ‏`isAllowedImageUrl` (ובצדק), וה-XML. עכשיו
      ‏**61/61 מוחלטים**.
      ‏`xml.ts` נבדק לבד: `&` יחיד ב"קפה & מאפה" הופך את **הקובץ** לבלתי נפרס,
      ולא גוגל ולא קורא RSS אומרים איזה מוצר עשה זאת. הבדיקות **פורסות** ולא
      משוות מחרוזות. שתי החרגות ב-Merchant נספרות ונרשמות בשם.
      ‏**1609/1609** (היו 1551), ‏tsc+lint על 715, ‏build 0, שני הפידים נמשכו
      מ-`pnpm start` נקי ונפרסו.

- [x] ✅ [50] Security hardening. **ה-webhook שענה 200 כשלא קרה כלום.**
      הסעיף על אימות בחתימה כבר תוקן בכתב הבקשה (ל-Cardcom אין חתימה), ולכן זו
      הקשחה של המנגנון הקיים - **והמדידה מצאה את התקלה שהמנגנון הסתיר.**
      ‏`payment_webhook_events.insert` שנכשל החזיר `{ok:true, replay:true}` ו-200.
      נכון להפרת UNIQUE, **שגוי לכל שגיאה אחרת**: ניתוק חיבור אמר ל-Cardcom
      שהקולבק התקבל, ולכן **לא היה retry**, ‏`GetLpResult` לא נקרא, ההזמנה נשארה
      פתוחה, **והשורה שה-DLQ משחזר היא בדיוק זו שלא נכתבה**. עכשיו `23505` הוא
      replay וכל השאר **503 עם התראה**, כי ה-retry של Cardcom הוא ההתאוששות.
      **סיבוב הסוד היה בלתי אפשרי ונראה אפשרי:** הסוד נצרב ל-IndicatorUrl בזמן
      יצירת דף התשלום, ולכן תמיד יש דפים פתוחים עם הערך הישן, והחלפת המשתנה
      היחיד מפילה את כולם - עם 200. נוסף
      ‏`CARDCOM_WEBHOOK_SECRET_PREVIOUS`, אותה צורה של `VOUCHER_QR_SECRET_PREVIOUS`.
      שני הסודות נבדקים **בלי short circuit**, אחרת זמן התגובה מחזיר את מה
      ש-`timingSafeEqual` מוציא.
      וסיבוב שנעשה בצד אחד בלבד **היה בלתי נראה**: 200 שקט, וכל ההזמנות
      המשולמות נשארות פתוחות. עכשיו התראה, **אך רק כשהגוף נפרס כקולבק** (התראה
      פר סורק היא התראה שאיש לא קורא).
      **למסלול לא הייתה ולו בדיקה אחת.** ‏17 עכשיו, ‏**4 נכשלות מול הקוד הישן**.
      ‏**רייט-לימיט:** ל-redeem לא היה, ל-lookup לידו כן - מה שהפך את התקרה
      לדקורטיבית, כי התוקף ישתמש במסלול שלא מוגבל, **והוא זה ששורף את השובר**.
      ‏120/שעה, fail-open.
      **עוגיית האורח נכתבה פעמיים ולאף עותק לא היה `secure`**, ואף אחד לא נראה
      שגוי כי כל אחד תאם את השני. בנאי אחד; ‏`secure` מותנה ב-TLS אמיתי ולא
      ב-NODE_ENV (‏`next start` כאן הוא production, ו-WebKit ב-E2E רץ מול
      ‏`http://localhost`).
      ‏**‏`img-src`** היה שלושה מארחים ידניים ליד `REMOTE_IMAGE_PATTERNS` בן
      שישה, והחסרים כללו את `picsum.photos` - **‏[18] בדיוק**. נגזר עכשיו
      מהמערך, עם שער. **לא בוצע, במדידה:** ‏CSP nonce - הוא מחייב רינדור דינמי
      וכל הקו [15]-[46] הביא את האתר לסטטי.
      ‏**1551/1551** (היו 1518), ‏tsc+lint נקיים על 702, ‏build 0.

- [x] ✅ [49] Wallet passes. **שני החוסמים שנרשמו לא חסמו, והם קבעו את הצורה.**
      אין כאן תעודת Apple Pass Type ID ואין service account של Google, ולכן
      "מוגדר" הוא **שאלה פר פלטפורמה**, בדיוק כמו Resend/Sentry/Meili/QStash
      כאן. פלטפורמה לא מוגדרת **לא מציגה כפתור**, ו-`/api/wallet/apple/[id]`
      מחזיר **404 ולא 500**: כפתור ארנק שנופל גרוע מאין כפתור, כי הלקוח לא יכול
      להבדיל בינו לבין דף קופון שבור.
      **הכלל היחיד שאינו קוסמטי:** הברקוד הוא `qr_payload` בייט-בבייט. הסריקה
      מאמתת HMAC מעל `KEV1.<body>.<mac>`, ולכן פאס עם הקוד הקצר היה נסרק כשובר
      לא מוכר **עם הלקוח בקופה**. נבדק בשני הכיוונים.
      ‏`.pkpass` נכתב ולא יובא: `zip.ts` store-only, ‏~90 שורות. הסיכון הוא
      ארכיון שעקבי עם עצמו ואף אחד לא פותח, ולכן **ארבע ראיות נפרדות**: round
      trip, ‏**`0xCBF43926`** (טבלת CRC שגויה-אך-עקבית עוברת round trip),
      היסטי ה-central directory, ו-**`unzip -t`**.
      ‏`pkpass.ts` **גוזר את המניפסט מהרשומות** (קובץ לא מכוסה = דחייה שקטה
      ב-iOS). הבדיקה **מייצרת תעודה משלה**, מאמתת את החתימה ידנית, ובודקת שהיא
      **מפסיקה לאמת על בייט אחד**. חותמות זמן קפואות → אותו פאס פעמיים
      בייט-בבייט, כי Wallet מחליף לפי serial number.
      **‏push: פקיעה לא צריכה שרת באף פלטפורמה** (`expirationDate` ו-
      ‏`validTimeInterval.end` נגזרים מ-`expires_at`, והלקוחות מורידים לבד).
      **מימוש כן, ורק לגוגל.** ה-push נעשה `await` (מופע serverless קופא ברגע
      התשובה), לא זורק, ומדווח **`skipped` ולא `ok`** כשלא מוגדר.
      אובייקטי גוגל על **קוד השובר ולא ה-UUID**: הקוד הוא המזהה היחיד שיש
      למסלול המימוש.
      **לא נבנה, בשמו:** ‏Apple pass web service (`voided` נצרב בהורדה), ולכן
      שובר שמומש ולא פג נשאר לא-מאופר ב-iOS. ה-QR לא מזיק: חד-פעמיות מוכרעת
      ב-DB. **74 בדיקות חדשות, 1518/1518** (היו 1444), ‏tsc ו-lint נקיים על 699
      קבצים, build יוצא 0. תלות אחת: `node-forge`.

- [x] ✅ [48] Refund flow. **כל refund בפרודקשן נכשל אחרי שהכסף כבר זז,
      וההצהרה "אין צורך במיגרציה" הייתה שגויה.** ‏`information_schema` על
      ‏`public.payments` מחזיר 18 עמודות ו-`refund_of_payment_id` אינה ביניהן:
      ‏026 מצהירה עליה בתוך `CREATE TABLE IF NOT EXISTS`, הטבלה כבר הייתה
      קיימת, ולכן **כל המשפט דולג ואף אחת מעמודותיו לא נוספה**. ‏`IF NOT EXISTS`
      על טבלה אינו `IF NOT EXISTS` על עמודותיה. ‏42703 מפיל את **כל** המשפט,
      ולכן ה-INSERT נכשל **אחרי** ש-Cardcom זיכה את הכרטיס: הלקוח קיבל את כספו,
      אף שורה לא רשמה זאת, ההזמנה נשארה `paid`, ולחיצה שנייה הייתה מזכה שוב.
      ה-SELECT היה שבור באותה צורה, ולכן רוב הזיכויים החזירו `NOT_FOUND` קודם.
      ‏**מיגרציה 106** דרך MCP, אומתה לפני ואחרי (‏18 ← 19 עמודות, ‏FK, אינדקס
      חלקי, ‏`supplier_debit` ב-CHECK) ובנוסף מקצה לקצה ב-DO block שגולגל לאחור:
      שורת זיכוי נכנסת, ‏`supplier_debit` מתקבל, **kind לא מוכר עדיין נדחה**,
      ואחרי הגלגול אפס שורות חדשות.
      שלוש העמודות: הסכום דרך `resolvePaymentMoneySchema` (‏pre-059, ‏`amount_ils`),
      ‏`paid_at` **היא עמודה של `orders`** וב-`payments` המקבילה `succeeded_at`,
      ו-`refund_of_payment_id` היא המיגרציה.
      ‏**`CancelOnly` מוכרע ולא מוצהר:** יום **קלנדרי** ב-Asia/Jerusalem ולא 24
      שעות, כי החיתוך הוא מנת השידור בסוף היום - חיוב ב-23:50 מפסיק להיות
      בר-ביטול עשרים דקות אחר כך. עסקה שבוטלה לא הגיעה לסליקה ולכן אין עליה דמי
      ביטול, ואף פעם לא בזיכוי חלקי.
      ‏**`supplier_debit` נותן גוף למשפט הוותיק במכונת המצבים**: עד עכשיו שורה
      פיזית מוחזרת עברה ל-`refunded` והספק שמר את הנתח, כלומר הפלטפורמה שילמה
      אותו פעמיים. אין טבלת payouts בפרודקשן, ולכן זה kind ב-`settlement_events`,
      חיובי, כי ה-CHECK של 094 שולל שליליים בכוונה.
      שובר שמומש/פג ← **`MANUAL_RESOLUTION`** בעברית, לפני שהכסף זז. זיכוי אפס
      נדחה לפני הספק. ‏**31 בדיקות חדשות, ‏15 מתוך 18 בסוויטת ה-action אומתו
      נכשלות מול הקוד הישן.** ‏**1444/1444** (היו 1413), ‏tsc ו-lint נקיים.

- [x] ✅ [47] השורה היחידה ש-goal 10 השאיר פתוחה: **structured logging עם
      request-id**. לפני: ‏**34 קריאות `console.*` ב-24 קבצים**, שמונה מוסכמות
      קידומת, ואפס דרך לקשור שתיים מהן. צ'קאאוט שנכשל בחציו כותב מה-webhook,
      ממייל השובר ומרשם הסילוק, בשלושה מודולים שלא מכירים זה את זה, משורשר עם כל
      בקשה אחרת שהתהליך מגיש; הקיבוץ היה ניחוש על חותמות זמן. עכשיו: אובייקט
      ‏JSON אחד לשורה, מנוקה ב-`scrub.ts` (אותו סקראבר של Sentry, ולכן השניים לא
      יכולים להיפרד), עם `request_id`. ‏`src/proxy.ts` טובע אותו פעם אחת - או
      שומר מזהה נכנס תקין, כדי שמסלול שהתחיל ב-load balancer יישאר מסלול אחד -
      מעביר במעלה הזרם ומחזיר ללקוח. ‏**15 מסלולי API** דרך `withRequestLog`,
      וחמש פעולות מסלול-הכסף דרך `withActionContext`.
      **שתי הנחות שנכתבו ואז נמדדו, ושתיהן היו מגיעות לפרודקשן בשקט:**
      ‏**(א)** מודול `'use server'` **כן** נסרק על ידי באנדלר הלקוח. האחסון נכתב
      תחילה במודול שכולם מייבאים, בנימוק שפעולה לעולם לא מגיעה לדפדפן;
      ‏Turbopack הפריך בשם: ‏`./src/server/actions/auth.ts` ...
      ‏`does not support external modules (request: node:async_hooks)` תחת
      ‏`Client Component Browser`. ‏`node:async_hooks` מרוכז עכשיו
      ב-`request-store.ts` בלבד, והשאר עובר דרך ידית על globalThis - וזה גם
      הנכון בין באנדל השרת לבאנדל ה-edge, שהם שני מופעים של אותו מקור.
      ‏**(ב)** ‏`enterWith`, השורה היחידה שפעולה יכלה להשתמש בה, **לא עובדת.**
      ארבע שורות מדידה: ‏`await helper that enters : MISSING` - זו הגרסה שנכתבה,
      והיא החזירה מזהה נכון וקשרה אותו **לכלום**, כי סטור שנכנס בתוך עוזר
      ‏awaited לא מגיע לקורא. ומהצד השני `leaked into the top level: two`: היא
      בורחת כלפי מעלה אל מחוץ לבקשה. **שורת לוג עם מזהה של מישהו אחר גרועה
      משורה בלי מזהה** - קורלציה חסרה נראית חסרה, שגויה לא. ‏`run` לא יכולה אף
      אחת מהשתיים, ו-`log-coverage` נופל על כל `enterWith(` ב-src.
      נתפס בדרך: ‏`import` **מעל** דירקטיבת `'use server'` מוציא את המודול מגרף
      השרת לגמרי, אותה משפחת תקלה ש-[10] מדד על ייצוא const.
      **נמדד אחרי, על בנייה נקייה:** דף המוצר עדיין `x-nextjs-prerender` +
      ‏`s-maxage=3600` ב-**1.9ms** והבית ב-3.9ms, כלומר המסלול הסטטי של [46]
      שלם והעתקת הכותרת בפרוקסי לא עולה דבר מדיד. ‏401 ו-503 אומתו חיים נושאים
      את המזהה; ‏2xx בכוונה לא כותב כלום; מזהה נכנס עוין
      (`<script>alert(1)</script>`) מוחלף ולא מוחזר.
      **מה שלא נעשה, בשמו ולא ברמז:** ‏69 מתוך 74 ה-Server Functions עדיין בלי
      מזהה, ‏`app/error.tsx` נשאר על console כי הוא רץ בדפדפן, ושורת לוג של
      פעולה מקצה לקצה **לא נלכדה כאן** - ספי ה-E2E של מסלול הכסף מדלגים בלי
      אישורים, והעמוד היחיד שנושא את פעולת ה-checkout מאחורי שער ההזדהות.
      ‏**1409/1409** Vitest (היו 1361), ‏**191/3/0** ב-Playwright, בדיוק הבסיס
      של [46], ‏tsc ו-biome נקיים על 681 קבצים.

- [x] ✅ [45] הראשון משני הממצאים ש-[44] פתח: **ששת הבאקטים הציבוריים הפסיקו
      להיות ניתנים לרישום** (מיגרציה `105`). כולם ציבוריים **וגם** נשאו
      מדיניות SELECT ל-`public` שכל תוכנה `bucket_id = '<שם>'`, ולכן
      ‏`POST /object/list/product-images` החזיר **10 אובייקטים עם שם וגודל**
      מול המפתח הפרסומי בלבד. אותה צורה כמו 104: סמוי ולא חי, כי העשרה הם
      תמונות מוצר ציבוריות ממילא. **התיקון נמדד לפני שנכתב**: עם המדיניות
      מוסרת, ‏`/object/public/...` נשאר **200 עם אותם 15114 בייטים** (באקט
      ציבורי לא מתייעץ עם RLS במסלול הזה) בזמן ש-`list` החזיר `[]`.
      ‏**403 שכמעט נספר כרגרסיה**: `/render/image/public/...` נופל, אבל הגוף
      הוא `FeatureNotEnabled` (מגבלת תוכנית) ולמסלול אפס אזכורים בריפו.
      ה-SELECT לא נמחק אלא **צומצם לפרדיקט של מדיניות ה-DELETE של כל באקט**,
      כי נתיב המחיקה של Storage קורא את השורה לפני שהוא מסיר אותה.
      ‏6/6 מחזירים `[]` ל-anon, והיועץ נקי מ-`public_bucket_allows_listing`.
      **נשאר של אופיר:** `auth_leaked_password_protection` הוא מתג ב-Auth
      ולא DDL, ואין לו כלי ב-MCP.

- [x] ✅ [44] שלוש המיגרציות שהמתינו הוחלו, וכל אחת אומתה מול הפרודקשן לפני
      ואחרי. ‏**103** (‏7 views עוקפי-RLS): נמדד שכולם `security_invoker`
      **unset** ושישה נשאו `arwdDxtm` ל-`anon`, ואחרי ההחלה שבעה מתוך שבעה
      ‏`on` עם ‏`postgres | service_role` בלבד. **תשעה views קיימים ולא
      שבעה**, ושני הנוספים נבדקו ולא נוספו: הם כבר invoker, ותחת
      ‏`set local role anon` החזירו **0 שורות** מול 61 מוצרים.
      ‏**104**: המדיניות הכפולה הוסרה, וספירת anon נשארה 61.
      ‏**102**: אומת ב-DO block שגולגל לאחור. הזמנת קופון הפיקה
      ‏`voucher_issued` עם השובר הקפוא ו-`supplier_sale`, **ובלי**
      ‏`order_paid`; אחרי הגלגול אפס שורות בכל שלוש הטבלאות. מפתח ה-dedupe
      זהה ל-Idempotency-Key של Resend, ולכן אין מייל כפול.
      **המלכודת שנבדקה:** ‏`CREATE OR REPLACE FUNCTION` של 102 **לא** החזיר
      ל-PUBLIC את ה-EXECUTE ש-103 שלל דקה קודם. אילו כן, 103 היה נפתח בשקט.
      יועץ Supabase: אפס `security_definer_view`. **שני ממצאים חדשים נרשמו
      ולא טופלו**: רישום קבצים ב-6 באקטים ציבוריים, והגנת סיסמאות דלופות
      מכובה.

- [x] ✅ [43] ‏goal 20 Final. פירוט מלא למעלה בשורת goal 20 ובסעיף [43]
      ב-STATE.md. השערים כפי שנמדדו: ‏tsc נקי, ‏`biome ci` נקי על כל 668
      הקבצים, ‏**1355/1355** Vitest, בנייה נקייה מ-`.next` ריק,
      ‏**187 passed / 5 skipped / 2 failed** ב-Playwright (שתי הנפילות עברו
      ‏24/24 בבידוד ועברו בפרויקט השני באותה ריצה, כלומר התנגשות DB ולא
      רגרסיה), ‏compare **11.5%** בית ו-**9.09%** קטגוריה, ‏Lighthouse נייד
      ‏91/86/86, ו-**k6 L1 נופל** ב-1120/884/751ms מול 800.

- [x] ✅ [42] דף המוצר מאחורי `use cache`: ‏`ProductPageBody` קרא ארבע טבלאות
      דרך הלקוח שקורא עוגיות, וקריאת עוגייה מבטלת קאשינג לסגמנט. הועבר ל-
      ‏`src/lib/product-detail.ts` עם `cacheLife('hours')` + `cacheTag` על
      ‏`createPublicClient`, ‏`generateStaticParams` מוגבל ל-200 slugs, ושלוש
      הקריאות הבלתי תלויות ב-`Promise.all`. ‏158 עמודים סטטיים במקום 94.
      ‏`buildCouponOffer` קרא שעון ולכן הפיל את הבנייה ב-
      ‏`next-prerender-current-time`; הוא הועבר לתוך ה-`use cache`, שם זה גם
      חוקי וגם כן יותר. **‏[43] תיקן את מסקנת המעבר של [42]** (ראה שם).

- [x] ✅ [41] ‏goal 19 Load. ‏`load/` מממש את ששת התרחישים של סעיף 5 ולא
      תרחיש אחד: ‏L1 גלישה, ‏L2 checkout, ‏L3 הצפת webhooks, ‏L4 תור מימוש,
      ‏L5 ‏connection pool, ‏L6 סורק חיפוש. טבלת הספים 5.3 היא `lib/thresholds.js`,
      ו-k6 יוצא 99 כשסף נחצה, ולכן כל קובץ הוא שער ולא דוח.
      **השערים אינם הערות.** ‏5.4 אומר "לעולם לא מול פרודקשן", ולכן
      `assertNotProduction()` מפיל את הריצה ב-init לפני שבקשה אחת יוצאת
      מהמכונה, **ואין דגל שפותח אותו**. שלוש הריצות אומתו נופלות: בלי
      `LOAD_ALLOW_WRITES`, מול הדומיין החי, ומול הדומיין החי **עם** הדגל.
      ה-URL-ים נלקחים מה-sitemap של היעד עצמו עם רצפה של 10 מוצרים, כי רשימת
      slugs קשיחה היא איך שבדיקת עומס חוזרת ירוקה אחרי שמדדה 404.
      **‏L6 עובר**: ‏p95 750ms/677ms, אפס רייט-לימיט (הרצה מתחת לתקרה של
      ‏[36] במכוון, ולכן האפס הוא טענה). **‏L1 נופל על דף המוצר**, וזה
      הממצא: ‏**1.2-1.9s חם**. לא cold cache, נמדד באותו slug ארבע פעמים
      ברצף. הבית **6ms** והקטלוג **18ms** סטטיים לגמרי, כלומר לא המכונה.
      דף המוצר נושא `x-nextjs-postponed: 1`, ‏6 ‏`await` סדרתיים ל-Supabase,
      וסיבוב אחד ל-DB מכאן הוא ~600ms: ‏1.8s הוא הוואטרפול. **התיקון כבר
      כתוב בעץ כשורה שנדחתה במכוון**, ‏`product/[slug]/page.tsx:83`:
      ‏`generateStaticParams` + ‏`use cache` על לקוח חסר-עוגייה, וה-לקוח הזה
      כבר נבנה ב-[22](ב).
      **כפילות שנוצרה בסשן מקביל:** ‏`k6/checkout-read-path.js` נכתב במקביל
      ומכסה את חצי-הקריאה של L2. שתי הספריות צריכות להתאחד תחת `load/`.

## החלטות שהתקבלו אוטומטית: מה נשאר מ-[9]

שתי ההחלטות שהיו פתוחות הוכרעו, בלי לשאול, לפי ההוראה הקבועה.

**`main`:** מוזג ב-`-s ours`. הקומיט היחיד שלו הוא 153,834 שורות של
`refs/`, `playwright-report/` ו-`supabase/.temp/`, אפס קוד. עכשיו הוא אב
קדמון בגרף ולא יורד ממנו קובץ אחד.

**7 branches של `feat/*`:** שלושה מוזגו, ארבעה לא, וכל אחד עם סיבה
נמדדת ולא עם הערכה.

| branch | הכרעה | למה |
|---|---|---|
| `feat/ci-foundation` | ✅ מוזג | שער per-diff נגד ערכים קשיחים, ובדיקות השמירה של מנוע העמלות |
| `feat/observability` | ✅ מוזג | Sentry, ולידציית env בעליה, התראת כסף לטלפון |
| `feat/wp-migration` | ✅ מוזג | מפת ה-301/410 של כתובות ה-WordPress. בלעדיה כל האתר המאונדקס נופל ל-404 ביום המעבר |
| `feat/checkout-cardcom` | ❌ לא | מביא `escrow.ts`, בניגוד לכלל הקבוע. מתנגש ב-11 קבצי ליבת תשלומים ש-GOAL 3 בנה מחדש |
| `feat/visual-polish` | ❌ לא | 44 קבצי UI מ-28.07 שידרסו עיצוב שנמדד מול האתר החי (compare מתחת ל-11%, Electro 18/18) |
| `feat/growth-core` | ✅ מוזג ב-[10] | הבאג תוקן ונחסם בבדיקה. ראה [10] |
| `feat/search-core` | ✅ מוזג ב-[11] | ההנחה שחסמה אותו נבדקה ולא החזיקה. ראה [11] |

## מצב התור

**‏03.08: תור goals 9-20 סגור.** ‏11 יעדים בוצעו ואחד, goal 15 (Wallet),
חסום בראיה ולא בהערכה: הארנק בשקלים numeric, וההמרה לאגורות **היא**
‏`PENDING-money-integer-fix.sql` שאסור להריץ, בעוד 52 קבצי אפליקציה קוראים
את השמות הישנים כשקלים. שינוי צד-קוד לפני הסכימה מכפיל כל מחיר פי 100.

‏**מה שנשאר פתוח אינו קוד.** שלוש המיגרציות שהמתינו (‏102, 103, 104) **הוחלו
ואומתו ב-[44] ב-03.08**, ודליפת ה-PII סגורה. נשארו חמישה חוסמי GO/NO-GO
שארבעה מהם קונפיגורציה ואחד נתונים, ושני ממצאי יועץ ש-[44] מדד: רישום קבצים
בשישה באקטים ציבוריים (**נסגר ב-[45]**), והגנת סיסמאות דלופות שמכובה
(מתג ב-Auth, אין לו כלי ב-MCP, של אופיר).
‏**‏k6 L1 על דף המוצר נסגר ב-[46]**, ולא היה צריך מכונה אחרת: השורה שנרשמה
כ"מדידה מחוץ למחשב הזה" הייתה שאילתה אחת לא מקאששת, ‏`p95 16.81ms` מול 800.

‏**הכלל שנוסף ב-[46], והוא הכללה של הכלל של [43]:** מדידה יחידה שיושבת קרוב
לסף אינה תוצאה, **וגם מדידה שנקראת נכון יכולה להיות מיוחסת לגורם הלא נכון.**
‏STATE.md רשם רצפה של 238ms וקרא לה "החור הדינמי של PPR". המספר היה נכון,
השם היה שגוי, וההפרדה בין השניים היא בדיוק `%{time_starttransfer}` מול
‏`%{time_total}`: ‏4ms מול 270ms על אותה בקשה. **ליד שמנוסח כארכיטקטורה ולא
כמדידה שולח לעבודה הלא נכונה.**
‏**ומסקנה שנייה, יקרה יותר:** זירוז חושף מרוצים שהיו סמויים. שלושה דברים
נשברו כאן **לא בגלל שהקוד שלהם השתנה** אלא בגלל שהדף הפסיק לחכות לבסיס
הנתונים - קליפת RSC, ‏effect של `pathname` שרץ ב-mount, ו-prefetch שהחזיר
‏404 שקליפת PPR הסתירה. שער E2E מלא הוא מה שתפס את שלושתם, ולא הקריאה.

‏**הקו של [15]-[43] נגמר באותה מסקנה שבה התחיל, ופעמיים באותו סבב:** כל שלב
מצא שקודמו צדק בכיוון וטעה בהיקף. ‏[43] מצא זאת גם על [35] (שורת inline
אחת הרימה 31 תמונות מהנעיצה, וזה נשמע כמו ניקוי אזהרה) וגם על [42] (מעבר
שער שהיה הגרלה אחת בתוך רצועת הרעש). **הכלל שנגזר**: מדידה יחידה שיושבת
קרוב לסף אינה תוצאה, ושמירה שדוגמת פריט אחד אינה שמירה.

**21 מתוך 21 סגורים. [22] נפתח עם שלושת מה ש-[21] השאיר מדוד.** ‏[1]-[12] אומתו מחדש בסשן נקי
ב-01.08 בלי לקבל את התיעוד כנתון: `tsc` נקי, **1235/1235** Vitest, build
נקי, **62/62** Playwright מול build טרי על פורט פנוי. אפס רגרסיה מול
`e067e0b`.

מ-[15] והלאה כל שלב נפתח ממה שקדם לו השאיר פתוח, וכל אחד מצא שקודמו צדק
בכיוון וטעה בהיקף. ‏[19] הוא הקצה של הקו הזה: הוא יצא לחפש מדרגת 288
ומצא שהאופטימייזר לא הקטין שום קובץ AVIF **מעולם**, ושכל מדידת בייטים
בסבבים [17] ו-[18] הייתה על קובץ מקור שהוגש כמו שהוא.

**המועמד הבא נמדד ולא נוחש, ואינו אלמנט**: ‏[20] סגר את שאלת אלמנט ה-LCP
והוכיח שהיא לא מה שמחזיק את הבית ב-80. מה שנשאר הוא **הגרף**, מדוד באותה
ריצת Lighthouse: ‏`render-blocking-insight` **850ms על 4 קובצי CSS**,
‏`unused-javascript` **78KiB**, `server-response-time` **433-620ms**, ו-
‏`bf-cache` שנופל על שתי סיבות. שלוש שורות נוספות פתוחות ב-STATE תחת [20]:
הסליידר שלוקח את כתר ה-LCP ב-6.5-9.5s בזכות 2.3% שטח, ‏`DefaultProductCard`
שלא נמדד מעולם, ו-4 מסכי קופון/הזמנה שעדיין מגישים `<img>` גולמי.

**כלל שנגזר מ-[20], תקף לכל הקוד:** לא משרשרים template literals ב-`+`.
הבנייה דורסת את הזנב של כל אופרנד ומוחקת אופרנד שאין בו השמה, בלי שגיאה
ובלי לוג.

מכיוון שהתור נסגר, [13] בדק את השער הבא של הפרויקט, `GO-LIVE.md`. אין שם
עבודת קוד פתוחה. **החוסם היחיד שנשאר הוא נתונים: 11 מתוך 11 הספקים בלי
כתובת ובלי לוגו**, ואת זה אי אפשר להסיק או להמציא. ההשלכה שלא הייתה
מתועדת: שער הפרסום רץ על כל שמירה של מוצר פעיל, ולכן **אי אפשר היום לערוך
אף אחד מ-61 המוצרים הפעילים באדמין** עד שהספק יושלם. המסך שמסנן בדיוק את
11 השורות: `/admin/suppliers?status=incomplete`.

שאר מה שנשאר הוא הגדרה ולא קוד, ומרוכז ב-`STATE.md` תחת "Blocking Issues".
הפריט שהכי כדאי לשים לב אליו שם הוא `VOUCHER_QR_SECRET`: בלעדיו אי אפשר
להנפיק או לאמת QR של שובר בפרודקשן. הוא כבר נאכף ב-boot
(`src/lib/env.ts:61`), כלומר פריסה בלעדיו נופלת בעלייה ולא אצל הלקוח.

## לוג התקדמות

| שלב | סטטוס | commit | הערות |
|---|---|---|---|
| [52] OG + share | ✅ | `c5b49f6` | **קישור השיתוף שציטט את המחיר הלא נכון, והכרטיס שנבנה והגיע לכלום.** כפתור הוואטסאפ שרשר `shekels(price)`, ועבור קופון `price` הוא `products.price_ils` - מה שהסחורה עולה **בבית העסק** - ולכן מי ששיתף קופון של ₪80 שלח לחבר **"₪200"**, והחבר נחת בדף שמצטט ₪80. **פעם שלישית שהכלל הזה נדרש כאן** (Merchant ו-JSON-LD כבר נושאים אותו), והכותרת של `coupon-offer.ts` מתעדת שהחזית סטתה פעמיים קודם. `lib/share/message.ts` הוא עכשיו המקום היחיד, וקופון שלא תומחר **אינו מצטט מספר בכלל**. **הסיכון שנרשם מראש אומת כאמיתי:** ל-Satori אין פונטי מערכת, וכרטיס עברי בלי face מפורש **מצייר ריק והבנייה נשארת ירוקה**; Heebo הוטמע כ-TTF (Satori אינה קוראת woff2, הצורה היחידה ש-`next/font` משאירה), 44KB למשקל + OFL, ושער שבודק **כותרת sfnt** כי קובץ באפס בייטים או מצביע LFS עובר בדיקת קיום ונופל בטלפון. **הממצא שהיה נשלח ונראה גמור:** עם `openGraph.images` ב-`generateMetadata`, הדף המוגש נשא את תמונת המוצר ו-`opengraph-image.tsx` **מעולם לא נוצל** - מוסכמת הקובץ ממלאת את השדה **רק אם המטא-דאטה לא תפסה אותו** - ולכן הכרטיס נבנה, הופיע ברשימת הנתיבים, החזיר 200 בשליפה ישירה, **והגיע לכלום**. כל מה שהכרטיס אומר מוכרע ב-`lib/og/product-card.ts` ונבדק שם, כי OG הוא המשטח היחיד שמי ששולח לא רואה; הכותרת נחתכת בתווים כי ל-Satori אין text-overflow. **שני תיקונים שה-PNG-ים חשפו:** מוצר עם `is_coupon_enabled` בלי `coupon_price_ils` (**4 מתוך 61**) צייר לוח לבן ריק, ופסיק בכרטיס האתר נחת בצד הלא נכון (אין bidi ב-Satori). לדף הבית **לא היה כרטיס OG בכלל** בזמן שהוא מצהיר `summary_large_image` = מלבן אפור ריק. **פייסבוק:** ה-sharer מקבל כתובת ובלבד (`quote` הוסר ב-2017), ולכן כרטיס ה-OG הוא **התוכן** של השיתוף; הסמל SVG פנימי כי ל-`lucide-react@1` אין ייצוא `Facebook`. שער הטוקנים הקיים תפס hex גולמי בשלושת הקבצים החדשים ובצדק. **1637/1637**, tsc+lint על 724, build 0, שני הכרטיסים **נבדקו כפיקסלים** |
| [51] Sitemap + feeds | ✅ | `4cd3592` | **ה-lastmod שלא נשא שום מידע, והבאג שרק שליפת הקובץ הבנוי חשפה.** מחצית ה-goal כבר קיימת ולא נכתבה מחדש; המדידה מצאה שארבע הכניסות הסטטיות נשאו `lastModified: new Date()` - **lastmod שהוא תמיד "עכשיו" טוען שכל הדפים השתנו בכל שליפה, וגוגל מתעלמת מ-lastmod לא מדויק לכל הקובץ ולא פר URL**, כלומר ארבעה תאריכים לא כנים עלו במחיר של המדויקים בכל מוצר. נגזרים עכשיו מה-`updated_at` הכי חדש בקטלוג, ו-`/contact` בלי lastmod (משתנה כשהקוד משתנה, ואין לזה כאן סיגנל); אומת על בנייה נקייה: `2026-07-29T03:15:46Z` במקום זמן הבקשה. **`g:price` הוא השדה שמשעה חשבונות Merchant** (גוגל סורקת את דף הנחיתה ומשווה), ולכן הוא מגיע מ-`buildCouponOffer` - אותה פונקציה של דף המוצר וה-JSON-LD - **ולעולם לא מ-`price_ils`**, שעבור קופון הוא מחיר הסחורה בבית העסק; נמדד: **15 פריטים עם `g:sale_price` אמיתי = בדיוק 15 הקופונים הפעילים**. **הבאג:** `g:image_link` היה `/images/products/...`, שזה **27 מתוך 46 המוצרים הפיזיים** - כמעט חצי הפיד, כל אחד דחיית פריט על תמונה שאי אפשר למשוך - **וכל שכבה מעליו נראתה נכונה** (העמודה מחזיקה את הערך, `isAllowedImageUrl` מקבלת אותו ובצדק, ה-XML תקין). רק הבייטים המוגשים הראו את זה; עכשיו **61/61 מוחלטים**. `xml.ts` נפרד ונבדק לבד כי הבריחה היא כל הסיכון: `&` יחיד ב"קפה & מאפה" הופך את **הקובץ** לבלתי נפרס ולא את הפריט, ואיש לא אומר איזה מוצר; תווי בקרה נמחקים ולא נבררחים (`&#x0;` אינו חוקי גם הוא), `]]>` מפוצל לשתי מקטעי CDATA, והבדיקות **פורסות** את התוצאה. שתי החרגות ב-Merchant (בלי מחיר, בלי תמונה) **נספרות ונרשמות בשם ה-slug**, כי פיד שמתכווץ בשקט זהה לקטלוג שהתכווץ בשקט. `lastBuildDate` = חותמת הפריט החדש ולא השעון; `guid isPermaLink` = כתובת המוצר, ולכן עריכת מחיר לא מתריעה מחדש למנויים. גילוי דרך `alternates.types` (ל-robots.txt אין שדה לפיד); Merchant **במכוון לא מפורסם**. `siteUrl()` אוחד משלושה עותקים לאחד. נתפס בדרך: תיאור ה-RSS היה שני template literals ב-`+`, **[20] בדיוק**. **1609/1609**, tsc+lint על 715, build 0 |
| [50] Security hardening | ✅ | `7ff90c2` | **ה-webhook שענה 200 כשלא קרה כלום.** `payment_webhook_events.insert` שנכשל החזיר `{ok:true, replay:true}` ו-200: נכון להפרת UNIQUE, **שגוי לכל שגיאה אחרת**. ניתוק חיבור אמר ל-Cardcom שהקולבק התקבל ← **אין retry**, `GetLpResult` לא נקרא, ההזמנה נשארה פתוחה, **והשורה שה-DLQ משחזר היא בדיוק השורה שלא נכתבה, ולכן שום דבר לא ידע**. עכשיו `23505` = replay, כל השאר **503 + התראה**, כי ה-retry של Cardcom **הוא** ההתאוששות. **סיבוב הסוד היה בלתי אפשרי ונראה אפשרי:** `?s=` נצרב ל-IndicatorUrl ביצירת דף התשלום, ולכן תמיד יש דפים פתוחים בדפדפנים עם הערך הישן והחלפת המשתנה מפילה את כולם - **בדרך שלמעלה, עם 200**. `CARDCOM_WEBHOOK_SECRET_PREVIOUS` (אותה צורה של `VOUCHER_QR_SECRET_PREVIOUS`), ושניהם נבדקים **בלי short circuit** כי יציאה מוקדמת מחזירה לזמן התגובה את מה ש-`timingSafeEqual` מוציא ממנו. וסיבוב חד-צדדי **היה בלתי נראה**: 200 שקט וכל ההזמנות המשולמות פתוחות; עכשיו התראה **רק כשהגוף נפרס כקולבק** - התראה פר סורק היא התראה שאיש לא קורא. **למסלול הכסף הזה לא הייתה ולו בדיקה אחת: 17 עכשיו, 4 מהן אומתו נכשלות מול הקוד הישן.** **רייט-לימיט:** ל-redeem לא היה בזמן של-lookup לידו יש, מה שהפך את תקרת ה-lookup ל**דקורטיבית** (התוקף בוחר את הלא-מוגבל, **והוא זה ששורף את השובר**) - 120/שעה, fail-open. **עוגיית האורח נכתבה בשני מקומות ולאף אחד לא היה `secure`**, ואף אחד לא נראה שגוי כי כל אחד תאם את השני; בנאי אחד, ו-`secure` מותנה ב-TLS אמיתי ולא ב-NODE_ENV (`next start` כאן הוא production, ו-WebKit ב-E2E רץ מול http). **`img-src`** היה 3 מארחים ידניים ליד `REMOTE_IMAGE_PATTERNS` בן 6, והחסרים כללו `picsum.photos` - **[18] בדיוק, 45 שורות קטלוג שציירו תמונה שבורה עם הפרה בקונסול וזהו**; נגזר עכשיו מהמערך עם שער שנופל על מארח שביעי. **לא בוצע במדידה:** CSP nonce - מחייב רינדור דינמי, וכל הקו [15]-[46] הביא את האתר ל-`x-nextjs-prerender`. **1551/1551**, tsc+lint על 702, build 0 |
| [49] Wallet passes | ✅ | `f852a3c` | **שני החוסמים שנרשמו לא חסמו - הם קבעו את הצורה.** אין תעודת Apple ואין service account של Google, ולכן "מוגדר" הוא שאלה **פר פלטפורמה**, כמו Resend/Sentry/Meili/QStash כאן: פלטפורמה לא מוגדרת **לא מציגה כפתור**, ו-`/api/wallet/apple/[id]` מחזיר **404 ולא 500** (כפתור שנופל גרוע מאין כפתור - הלקוח לא מבדיל בינו לבין דף קופון שבור). **הכלל היחיד שאינו קוסמטי:** הברקוד הוא `qr_payload` בייט-בבייט; הסריקה מאמתת HMAC מעל `KEV1.<body>.<mac>`, ולכן פאס עם הקוד הקצר נסרק כשובר לא מוכר **עם הלקוח בקופה**. ‏`.pkpass` נכתב ולא יובא: `zip.ts` store-only ~90 שורות, והסיכון הוא ארכיון עקבי-עם-עצמו שאיש לא פותח, ולכן **ארבע ראיות נפרדות** - round trip, **`0xCBF43926`** (טבלת CRC שגויה-אך-עקבית עוברת round trip), היסטי central directory (`readZip` הולך על local headers, כל unzip אמיתי לא), ו-**`unzip -t`**. `pkpass.ts` **גוזר את המניפסט מהרשומות** כדי שקובץ לא יוכל להישאר לא מכוסה (דחייה שקטה ב-iOS); הבדיקה **מייצרת תעודה משלה** (מפתח פיקסצ'ר ב-repo הוא מפתח), מאמתת ידנית, ובודקת שהאימות **נופל על בייט אחד**. חותמות זמן וזמן חתימה **קפואים** → אותו פאס פעמיים בייט-בבייט, כי Wallet מחליף לפי serial number. **push נמדד מול הפורמטים:** **פקיעה לא צריכה שרת באף פלטפורמה** (`expirationDate` ו-`validTimeInterval.end` נגזרים מ-`expires_at`), ולכן הסריקה הלילית לא דוחפת כלום; **מימוש כן, ורק לגוגל**, כי אובייקט שמור שומר את ה-`state` עד שה-API אומר אחרת. ה-push **נעשה await** (מופע serverless קופא ברגע התשובה), לא זורק, ומדווח **`skipped` ולא `ok`** כשלא מוגדר. אובייקטי גוגל על **קוד השובר ולא UUID**: הקוד הוא המזהה היחיד שיש למימוש. **לא נבנה, בשמו:** Apple pass web service - שובר שמומש ולא פג נשאר לא-מאופר ב-iOS; ה-QR לא מזיק כי חד-פעמיות מוכרעת ב-DB. **74 בדיקות, 1518/1518**, tsc+lint נקיים על 699, build 0 |
| [48] Refund flow | ✅ | `5d26f0a` | **כל refund בפרודקשן נכשל אחרי שהכסף כבר זז.** ‏`information_schema` על `payments`: **18 עמודות, בלי `refund_of_payment_id`**. ‏026 מצהירה עליה בתוך `CREATE TABLE IF NOT EXISTS` והטבלה כבר הייתה קיימת, ולכן כל המשפט דולג - **`IF NOT EXISTS` על טבלה אינו `IF NOT EXISTS` על עמודותיה, וההבדל שקט**. ‏42703 מפיל את כל המשפט, ולכן ה-INSERT נכשל **אחרי** ש-Cardcom זיכה: כסף חזר ללקוח, אף שורה לא רשמה זאת, ההזמנה נשארה `paid`, ולחיצה שנייה הייתה מזכה שוב. ה-SELECT שבור באותה צורה (`amount_agorot`, `paid_at`) ולכן רוב הזיכויים החזירו `NOT_FOUND` על **כל** הזמנה עוד קודם. **מיגרציה 106** דרך MCP, אומתה לפני/אחרי (18←19 עמודות, FK, אינדקס חלקי, `supplier_debit` ב-CHECK) **ובנוסף ב-DO block שגולגל לאחור מול הפרודקשן**: שורת זיכוי נכנסת, `supplier_debit` מתקבל, **kind לא מוכר עדיין נדחה**, ואחרי הגלגול 2 payments ו-0 events. שלוש העמודות: סכום דרך `resolvePaymentMoneySchema` (pre-059 = `amount_ils`), **`paid_at` היא עמודה של `orders`** (ב-`payments`: `succeeded_at`), ו-`refund_of_payment_id` = המיגרציה. **`CancelOnly` מוכרע:** יום **קלנדרי** ב-Asia/Jerusalem, לא 24 שעות, כי החיתוך הוא מנת השידור - חיוב ב-23:50 מפסיק להיות בר-ביטול עשרים דקות אחר כך; עסקה שלא הגיעה לסליקה לא נושאת דמי ביטול; אף פעם לא בזיכוי חלקי. **`supplier_debit`** נותן גוף למשפט של מכונת המצבים: עד עכשיו שורה פיזית מוחזרת עלתה לפלטפורמה את נתח הספק **פעמיים**. אין טבלת payouts בפרודקשן ← kind ב-`settlement_events`, **חיובי**, כי ה-CHECK של 094 שולל שליליים בכוונה והסימן שייך לעמודה המוגבלת. שובר מומש/פג ← `MANUAL_RESOLUTION` בעברית **לפני** שהכסף זז; זיכוי אפס נדחה לפני הספק (`amount_ils` נושאת `CHECK (> 0)`); ה-catch של הפרסיסטנס מרים התראה. **31 בדיקות חדשות, 15/18 בסוויטת ה-action אומתו נכשלות מול הקוד הישן.** **1444/1444** (היו 1413), tsc ו-lint נקיים על 682 קבצים. **נרשם ולא טופל:** `types/database.ts` נבדל מהפרודקשן ב-~1200 שורות (שייך ל-[57]), ו-`payments.token_id` חסרה מאותה סיבה של 026 |
| [47] structured logging + request-id | ✅ | `c6db2ba` | השורה היחידה ש-goal 10 השאיר. **34 קריאות `console.*` ב-24 קבצים ← אובייקט JSON אחד לשורה** עם `request_id`, מנוקה ב-`scrub.ts` כדי שהלוגר וה-Sentry לא יוכלו להיפרד. `src/proxy.ts` טובע/משמר/מחזיר; 15 מסלולי API דרך `withRequestLog`; 5 פעולות מסלול-כסף דרך `withActionContext`. **שתי הנחות שהבנייה והמדידה הפריכו, שתיהן היו מגיעות לפרודקשן בשקט: (א)** מודול `'use server'` **כן** נסרק על ידי באנדלר הלקוח - `Client Component Browser: ./src/server/actions/auth.ts` עם `does not support external modules (request: node:async_hooks)`; האחסון רוכז ב-`request-store.ts` והשאר עובר דרך ידית globalThis, שהיא גם הנכונה בין באנדל השרת ל-edge. **(ב)** `enterWith` **לא עובדת**: `await helper that enters : MISSING` (הגרסה שנכתבה - מזהה נכון, קשור לכלום) ומנגד `leaked into the top level: two` (בורחת מחוץ לבקשה). **מזהה שגוי גרוע ממזהה חסר**, ולכן callback בלבד, ושער שנופל על כל `enterWith(`. בדרך: `import` מעל `'use server'` מוציא מודול מגרף השרת, כמו ה-const של [10]. **אחרי:** דף המוצר `x-nextjs-prerender` + `s-maxage=3600` ב-**1.9ms**, בית 3.9ms - המסלול הסטטי של [46] שלם; 401/503 אומתו חיים עם המזהה, 2xx שקט במכוון (`/api/a` בכל צפייה), מזהה עוין מוחלף. **לא נעשה, בשמו:** 69/74 פעולות עדיין בלי מזהה, `error.tsx` נשאר console (רץ בדפדפן), ושורת פעולה חיה לא נלכדה - ספי הכסף מדלגים בלי אישורים. **1409/1409** (היו 1361), **191/3/0** |
| [46] הרצפה של 238ms | ✅ | `aef2044` | **היא לא הייתה PPR, היא הייתה שאילתה אחת.** ‏TTFB 4ms בשני הדפים; התשובה המלאה עלתה 268-289ms (מוצר) ו-273-327ms (קטגוריה) מול סיבוב keep-alive חם ל-Supabase של **266-313ms**. שני שורדים מ-[42]: `RelatedProducts` דרך `createClient()` רמה אחת מתחת למה ש-[42] קאשש, ו-`generateMetadata` של הקטגוריה שירתה שאילתה משלה בזמן ש-`getCategoryBySlug` כבר `use cache` על אותן שתי עמודות. אחרי: **1.8-6.9ms** ו-**6.5-14ms**, ודף המוצר **סטטי לגמרי** (`s-maxage=3600` במקום `private, no-store`). **k6 L1 עבר לראשונה מאז [41]**: product p95 **1120/884/751 ← 16.81ms** מול 800, שלוש ריצות נקיות; ריצה רביעית חצתה תחת תחרות על המכונה משתי ריצות compare, ונרשמה. `generateStaticParams` ל-12 הקטגוריות: פגיעה קרה **1.769s ← 0.285-0.671s**. **שלוש שבירות שהזירוז חשף ותוקנו:** (א) ניווט צד-לקוח למוצר מת על "Connection closed" - ה-`<Suspense>` השורשי גרם ל-RSC להחזיר את קליפת ה-fallback, **31852B זהים לכל slug**, בזמן שבקשת מסמך החזירה 94338B תקינים; (ב) הוספה לעגלה הפסיקה לפתוח את המיני-קארט, כי `MiniCartDropdown` סגר מ-effect על `pathname` שרץ גם ב-mount ו-`usePathname` מתייצב אחרי hydration - בלתי מזיק כל עוד הדף עלה בסיבוב DB, קטלני ב-2ms; (ג) שער ה-404 של הפוטר האדים **בצדק**: **8 מתוך 32** slugs של דילי הבית אינם קיימים, כלומר רבע מהגריד ירה 404 בכל צפייה, סמוי כל עוד ה-PDP היה קליפת PPR. שמירה: `catalogue-render-path.test.ts` על כל גרף הייבוא, אומת נכשל כשמחזירים את הבאג. **191/3/0** (בסיס [43]: 187/5/2), **1361/1361**, compare **11.5% ← 9.73%** |
| [45] באקטים ציבוריים בלי רישום | ✅ | `5f0fd7d` | מיגרציה `105`. ששת הבאקטים ציבוריים **וגם** נשאו מדיניות SELECT ל-`public` שכל תוכנה `bucket_id`, ולכן `POST /object/list/product-images` החזיר **10 אובייקטים עם שם וגודל** מול המפתח הפרסומי בלבד. סמוי ולא חי, כמו 104. **התיקון נמדד לפני שנכתב**: בלי המדיניות, `/object/public/...` נשאר **200 עם אותם 15114 בייטים** ו-`list` החזיר `[]`, כי באקט ציבורי לא מתייעץ עם RLS במסלול ההגשה, ו-`getPublicUrl()` (`upload.ts:54`) בונה בדיוק אותו. **403 שכמעט נספר כרגרסיה**: `/render/image/public/...` נופל על `FeatureNotEnabled`, מגבלת תוכנית ולא RLS, ואפס אזכורים בריפו. ה-SELECT **צומצם ולא נמחק**, לפרדיקט של מדיניות ה-DELETE של כל באקט, כי נתיב המחיקה קורא את השורה לפני ההסרה; `vendor-logos` קיבל `has_role('vendor')` בלי `is_admin()` כי זה מה שכתוב בכתיבה שלו, מראה ולא שיפור. ‏6/6 מחזירים `[]` ל-anon, ‏4/4 כתובות ציבוריות 200, היועץ נקי מהלינט |
| [44] שלוש המיגרציות הממתינות | ✅ | `63ce1ca` | ‏102, ‏103 ו-104 הוחלו דרך MCP `apply_migration` בלבד, וכל טענה בראש קובץ נמדדה מול ה-DB החי לפני ההחלה במקום להיקרא ממנו. ‏**103**: שבעת ה-views היו `security_invoker` **unset** ושישה נשאו CRUD מלא ל-`anon` **ו**ל-`authenticated`; אחרי ההחלה שבעה מתוך שבעה `on` ו-ACL של `postgres | service_role` בלבד, ואף קורא לא נשבר כי כל ארבעת אתרי הקריאה הם `createAdminClient()`. **בפרודקשן תשעה `v_*` ולא שבעה**, והשניים שלא נכללו נשללו במדידה ולא בהנחה: כבר invoker, ותחת `set local role anon` **0 שורות משניהם** מול 61 מוצרים באותה שאילתה. ‏**104**: שתי מדיניות SELECT ציבוריות נבדקות ב-OR, ולכן `deleted_at IS NULL` מעולם לא חל; נשארה מדיניות אחת, וספירת anon נשארה 61. ‏**102**: ‏DO block מול הפרודקשן שגולגל לאחור החזיר `voucher_issued` עם `vouchers=1` ו-`supplier_sale`, **בלי** `order_paid`, ואחריו 0 outbox / 0 שוברים / 0 הזמנות. ‏dedupe זהה לבייט ל-Idempotency-Key ב-`voucher-email.ts:114`, וה-drain מוסר אותו ל-Resend, ולכן אין מייל כפול. **המלכודת שנבדקה במפורש:** ‏`CREATE OR REPLACE FUNCTION` ב-102 **לא** החזיר ל-PUBLIC את ה-EXECUTE ש-103 שלל, אחרת 103 היה נפתח בשקט. יועץ Supabase אחרי: **אפס `security_definer_view`**, ומה שנשאר הוא בדיוק קבוצת "לא נשלל בכוונה" של 103. **שני ממצאים חדשים נרשמו ולא טופלו**: `public_bucket_allows_listing` על 6 באקטים, ו-`auth_leaked_password_protection` מכובה. ‏tsc נקי, ‏**1355/1355** |
| [43] goal 20 Final | ✅ | `64f4629`, `d5ea8d3` | **השער שהיה אדום נסגר, והחשוד היה הלא נכון.** ‏STATE ייחס את 23% של `home` ל-[40] ופטר אותו; ‏[43] שלל קודם את נקודת הייחוס (חי מול חי באותו סשן = **1.85%**) ואז מצא את הגורם: ‏`style={{width:'auto',height:'auto'}}` מ-[35], שנוסף כדי להשתיק אזהרת **dev-only** של next/image. **סגנון inline מנצח מחלקה**, ולכן `.p_con__image { height: 245px }` חדל לחול ו-**31 מתוך 31** התמונות צוירו באספקט המקור, ‏124px עד 361px. **‏24.16% ← 11.5%**, בתוך הרצועה של [20]/[24]; ‏`category` 9.09% ללא תזוזה; ‏Lighthouse נייד 91/86/86 עם CLS 0.004, כלומר לא נקנה בציון. **שמירת [17] פספסה כי היא דוגמת כרטיס אחד** ובודקת `> 100px`; החדשה מודדת את כולם מול הנעיצה. בנוסף `pnpm lint` **44 ← 0** (כל "biome נקי" קודם נמדד ב-`lint:changed` על ה-diff), ובתוכו **שני `biome-ignore` שנימוקם גלש לשורה שנייה ולכן לא דיכאו כלום** ב-JSON-LD של הבית והמוצר, ו-`eslint-disable` בפרויקט שהלינטר שלו biome. ‏`@next/bundle-analyzer` נבנה ומעולם לא הוחל, והחלתו לא הייתה עוזרת (webpack, אינרטי תחת Turbopack). **‏[42] תוקן בהיקפו**: שלוש ריצות L1 רצופות על אותה בנייה הן **1120/884/751ms** מול 800, כלומר 657ms היה הגרלה. ‏1355/1355, ‏**187/5/2** (שתי הנפילות עברו 24/24 בבידוד ועברו בפרויקט השני באותה ריצה) |
| [42] product page behind use cache | ✅ | `bb8c865` | ‏`ProductPageBody` קרא ארבע טבלאות דרך הלקוח שקורא עוגיות, וקריאת עוגייה מבטלת קאשינג לסגמנט. הועבר ל-`src/lib/product-detail.ts` עם `use cache` + `cacheLife('hours')` + `cacheTag(CATALOGUE_TAG)` על `createPublicClient`, ‏`generateStaticParams` מוגבל ל-200 slugs, ושלוש קריאות בלתי תלויות ב-`Promise.all`. ‏158 עמודים סטטיים במקום 94. ‏`buildCouponOffer` קרא שעון והפיל את הבנייה ב-`next-prerender-current-time`; הועבר פנימה. **מסקנת המעבר תוקנה ב-[43]** |
| [41] goal 19 Load | ✅ | הקומיט הזה (הקבצים נסחפו ל-`42aaa89`) | ששת התרחישים של סעיף 5, לא אחד. הספים של 5.3 הם קוד (`lib/thresholds.js`) ו-k6 יוצא 99 כשסף נחצה, ולכן זה שער. **שני השערים אומתו נופלים בשלוש ריצות**: בלי `LOAD_ALLOW_WRITES`, מול הדומיין החי, ומול הדומיין החי עם הדגל דלוק. **‏L6 עובר** (750/677ms, אפס 429 מתחת לתקרה של [36]). **‏L1 נופל, וזה הממצא:** דף המוצר **1.2-1.9s חם** מול תקציב 800ms. נבדק שאינו cold cache (אותו slug 4 פעמים: 1.96/1.75/1.87/1.80s) ושאינו המכונה (בית **6ms**, ‏`/products` **18ms**, שניהם סטטיים). ‏`x-nextjs-postponed: 1` + ‏6 `await` סדרתיים + ~600ms לסיבוב DB = ואטרפול. התיקון כתוב בעץ ב-`page.tsx:83` כשורה שנדחתה במכוון, והלקוח שהוא דורש נבנה ב-[22](ב). **הקבצים נכנסו בטעות ל-`42aaa89` של הסשן המקביל** (`git add -A`), ואותו סשן כתב במקביל `k6/checkout-read-path.js` שמכפיל את חצי-הקריאה של L2 |
| סגירת התור 02.08 | ✅ | `09e7924` | [1]-[9] אומתו כאבות של HEAD; [4] אומת מול הפרודקשן (RLS, סטטוסים, SELECT בלבד); מיגרציית האגורות ב-`PENDING-money-integer-fix.sql` ממתינה לאישור אופיר דרך MCP; שתי דריסות snapshot של הקבצים האלה שוחזרו (06:17, 10:52), הדורס חייב git pull לפני כתיבה |
| [21] cacheComponents, app-wide | ✅ | `7d2bf1d` | הליד האחרון של [21], ו**הגורם לא היה 64 עמודים אלא שורה אחת לכל route group**: כל layout עשה `await createClient()` ו-`getCart()` בראש הקומפוננטה. דף הבית **אינו קורא שום נתון** והיה דינמי אך ורק בגללו. ‏`--debug-prerender` מכבה early-exit ולכן נותן את כל השגיאות בבנייה אחת: **78/63 ← 29/26 ← 12/16 ← 0**. שתי מדרגות הן עריכה אחת לאזור שלם: `children` כ-slot של pass-through מאחורי גבול אחד מכסה את **34 עמודי האדמין**, ו-`AnalyticsProvider` ב-**root layout** עם `usePathname` הוציא כל נתיב `[id]`/`[slug]` באפליקציה מהשלד. ‏`/cart` יצא פשוט יותר: הוא קרא את העגלה פעם שנייה כדי לכתוב אותה ל-store שכבר מתמלא. ‏TTFB **385 ← 4ms**, ‏FCP **564 ← 84ms**, ‏SI **5.1-7.2 ← 1.1-1.6s**, ציון **69/77/77 ← 91/78/83**. **הכותרת `no-store` לא זזה ולא יכלה** (‏`x-nextjs-postponed`), וזה החלק ש-[21] ניסח שגוי. שינוי מכוון: slug מת הוא 200 + `noindex` ולא 404, מאומת ב-HTML. שלושה מספרי fallback שחישבתי משמות מחלקות היו שגויים ונמדדו (**349←401.5**, **57←53**, **53←49.5**). שערים ללא תזוזה. ‏1254/1254, **74/74** |
| [21] catalogue thumb on a phone | ⏳ | `309f1ac` | סריקה של כל נתיב ציבורי ב-412px אחרי התיקון של `(main)`, על ההנחה שפריסה שנמדדה מול דסקטופ תישבר פעמיים. ‏`--cat-thumb-max` (186.03px, קופסת התוכן של הכרטיס **בדסקטופ**) שימש כ-`max-width` שטוח בכל רוחב, ולכן בטלפון התמונה ציירה 186 בתוך קופסה של 117-143: שתי תמונות נפגשות בלי מרווח, תג ההנחה על השכן, ו**מסמך 4px רחב מהמסך** ב-3 דפים. ‏`min(..., 100%)` ו-`sizes` שהולך אחרי אותה צורה (`50vw - 63px`, נפגשים ב-497px). ‏`/products` ‏**365.2 ← 193.9KB** ב-412/dpr1.75. הדסקטופ ללא שינוי עד הבייט, שערים 32.59% ו-9.07% כמו לפני, ‏CLS 0. **סשן מקביל החזיר את שני הקבצים בין הבדיקה ל-commit** והוחלו מחדש. ‏1254/1254, **75/75** |
| [21] (main) group + image allowlist | ⏳ | `24c686c` | נכנסתי לשורה האחרונה של [19] (ארבעה מסכים עם `<img>` גולמי) ו**שניים מהם היו נכונים**: הם QR כ-data URL, שם תג גולמי הוא המימוש היחיד. השניים האחרים הובילו לשני ממצאים גדולים יותר. (א) **`(main)/layout.tsx` פרש 3 עמודות בכל רוחב בלי breakpoint**, ולכן בטלפון 412px העמודה האמצעית, שהיא הדף, נפתרה ל-**2px**: `/coupons`, `/coupons/[id]` ושני מסכי הניוזלטר בלתי שמישים בנייד. הדסקטופ זהה לפיקסל. (ב) **`next/image` זורק על מארח לא מורשה, כלומר 500 על הדף**, וארבעה מקומות שכותבים כתובת תמונה לא בדקו, כולל `products.images` שהיה בלי ולידציה בכלל. רשימה אחת ב-`src/lib/images/remote-hosts.ts` שממנה `next.config` בונה את `remotePatterns`. נבדק מול הפרודקשן: 76 ערכים, כולם מותרים, אף שורה לא ננעלת. שתי תמונות הקופון אומתו מקצה לקצה עם שורה שנכתבה בפרודקשן ונמחקה. **1254/1254, 73/73** |
| [21] DefaultProductCard sizes | ⏳ | `2e247e2` | הליד מ-[19] שלא נמדד מעולם. ‏`sizes` נקב את **עמודת הגריד** ולא את התמונה: בלי מרווח העמוד, בלי ה-gap ובלי ה-padding. נמדד ב-14 רוחבי מסך, שלושה ענפים בהתאמה לינארית עם שארית 0. דף המוצר: **236.3 ← 117.0KB** ב-412/dpr2. לא רגרסיית איכות: המועמד הוא 1.03 מהקופסה במקום 1.38, ו-diff מול הבנייה הקודמת 0% בנייד ו-0.107% בדסקטופ בלי אף פיקסל מעל 64. **הבדיקה הראשונה שכתבתי עברה מול הבאג**: הגריד מתחת לקפל, `currentSrc` ריק, `Number(null)` הוא 0. ‏1246/1246, **70/70** |
| [21] dead links in the chrome | ⏳ | `f603ab6` | הפוטר וה-masthead מקשרים לשישה נתיבים שהגיעו עם ה-markup מוורדפרס. ‏next עושה prefetch כשהפוטר נכנס למסך, ולכן **כל צפייה בעמוד ירתה 6 רינדורי שרת מלאים** שחוזרים 404. שניים מהם לא היו עמודים חסרים אלא נתיב שגוי: `/profile` ← `/account`, `/orders` ← `/account/orders`. לארבעה אין עמוד ו-prefetch כבוי אצלם עם `built: false`. נמדד עם probe שגולל לתחתית: **6 ← 0**. הגלילה היא הבדיקה, ובלעדיה זה בלתי נראה. ‏1246/1246, **69/69** |
| [21] CSS chunks + Sentry defines | ⏳ | `adb5811` | ‏4 קובצי CSS חוסמי רינדור לאחד (‏454ms + 3x304ms כדי למסור 8.6KB), **אותם בייטים בדיוק**: 93374 מול 93371. בטוח רק כי שלושתם BEM ממודר לעמוד, וההערה ב-root layout אומרת את זה למי שיוסיף שם קובץ רביעי. ‏`webpack.treeshake` של Sentry הוא DefinePlugin של webpack ולכן **אינרטי תחת Turbopack**; ‏`compiler.define` מגיע ל-node_modules ומוריד **1802668 ← 1743875** (הצ'אנק הגדול 436747 ← 381140), ושלושת דגלי ה-rrweb נמדדו על **אפס** והושמטו. ‏`inlineCss` נוסה ובוטל במדידה: המסמך 267631 ← 542125 כי הסגנונות נפלטים גם ב-RSC payload. ‏4 זוגות A/B **משולבים** (החלפת בנייה בין כל ריצה): ‏FCP 1.7 ← 1.1s, חסימה 870 ← 250ms, ‏JS מת 78 ← 62KiB, **הציון לא נפרד** כי LCP דו-שיאי בשתי הבניות. ‏7 עמודים x 2 viewports מול הבנייה הישנה: 13/14 זהים פיקסל-לפיקסל. ‏1246/1246, **68/68** |
| [1] Cart | ✅ | קיים מ-goal קודם, אומת מחדש | tsc נקי, 932/932 Vitest, compare cart 3.31% (מצב ריק, מפתח service_role חסר למדידת עגלה מלאה) |
| [2] Checkout UI | ✅ | `49e327f`, `2e4b202`, `883a56e` | שלושה שלבים + שלב סיום, טוקנים נאכפים ב-`checkout-tokens.test.ts`, Electro נמדד 18/18 cart ו-11/12 checkout |
| [3] Cardcom | ✅ | `21a348e`, `e184f42` | רובו היה בנוי; DLQ תוקן ונבנה, ה-enum של finalize.ts:312 קיים ב-DB החי ולא נדרש DDL, Cardcom לא חותם על callbacks ולכן re-verify מול GetLpResult הוא האימות |
| [4] Coupon redemption | ✅ | `db33a4c`, `78c752c` | תוקנו ה-QR שלושה מסכים קידדו לא נכון והרשימה שלא הובילה אליו. **הליבה לא הייתה תקינה**: `issueVoucher` כתב `platform_bp`, עמודה שאין בפרויקט המתארח (059 מעולם לא הוחלה שם), ולכן כל INSERT נפל על 42703 ו**אף שובר לא יכול היה להיווצר** - הטבלה ריקה. תוקן בעזרת probe. אומת מול ה-DB החי ב-DO block עם rollback: הנפקה מתקבלת, סריקה מצליחה, סריקה חוזרת נדחית, וכל 10 תרחישי redeem_voucher נכונים. tsc נקי, 1013/1013 |
| [5] אזור אישי | ✅ | `76a631f` | כל הדפים היו קיימים. הארנק הוצג **אפס לכל לקוח**: `balance_agorot` לא קיים בפרודקשן ושני קוראים נקבו בו, כולל הקופה שמחליטה כמה קרדיט אפשר להפעיל. עכשיו probe יחיד לארבעת הקוראים. `fn_wallet_transfer` מוענק ל-service_role בלבד, אין דרך לקוח להזיז כסף. tsc נקי, 1019/1019, build עובר |
| [6] Notifications | ✅ | `9a0ed6e` | מיגרציה 095 הוחלה: `notification_outbox` + טריגר על `orders.paid_at` (אישור ללקוח + התראה לכל ספק) וטריגר על `vouchers.status` (קופון נסרק). שני הטריגרים בולעים שגיאות ולכן לא יכולים להפיל חיוב או מימוש. ניקוז ב-`/api/cron/notifications` דרך Resend, backoff ו-dead אחרי 5. **בלי Edge Function**: `pg_net` לא מותקן, וה-outbox הוא מה שנותן את העמידות. אומת בפרודקשן ב-DO block עם rollback. tsc נקי, 1038/1038, build עובר |
| [7] SEO+Performance | ✅ | `1ed4f18`, `019ac3e` | sitemap ו-robots כבר היו, וכך גם `lang="he" dir="rtl"`. **JSON-LD לא היה בכלל** ונבנה: Product+Offer ו-BreadcrumbList במוצר, Organization+WebSite בבית. המחיר נגזר מ-`CouponOffer`, אותו אובייקט שמנוע העמלות מחייב לפיו. נוסף canonical ו-OpenGraph. Lighthouse על ה-build, desktop: בית **90/93/96/100**, מוצר **97/97/96/92**. הביצועים עלו מ-86 אחרי שהמדידה הראתה שכרטיסי הדילים מושכים כל תמונה בגודל המקור |
| [8] E2E Playwright | ✅ | `18a48f7` | **58/58**, פעמיים: worker אחד, ואז 2 workers מול `pnpm start` טרי. היה 48/10. החוסם לא היה המפתח החסר אלא **מי שדרש אותו**: העגלה רצה על `createAdminClient()` כדי לקרוא קטלוג ציבורי ולגעת בשורת עגלה אחת, ולשניהם כבר יש מדיניות. הועברה ל-anon, כולל `Cookie: session_id` שעליו המדיניות של `carts` בנויה. אומת מול פרודקשן לפני שנכתבה שורה, והשורות נמחקו. בנוסף 4 ספקים שהיו שגויים ולא ביש-מזל |
| [8] E2E Playwright (סבב קודם) | ⚠️ | `1d09184` | הסוויטה קיימת ומכסה את הנדרש (purchase-flow, coupon-scan, cart). תוקנו 4 ספקים שעדיין דרשו את שער ה-checkout שהוסר בכוונה ב-GOAL 2. **48 עוברים, 10 נופלים מסיבה אחת שאינה הקוד**: ה-`SUPABASE_SECRET_KEY` ב-`.env.local` מחזיר 401, וכל כתיבת עגלת אורח עוברת דרך admin client. שוחזר ישירות מול ה-REST API. חסום עד שיוחלף מפתח. **01.08:** הורץ מחדש מול ה-build אחרי 3 המיזוגים, שוב 48/10 בדיוק, כלומר אפס רגרסיה. כל 10 הנופלים דורשים עגלת אורח מלאה. חיפוש מפתח תקין ב-4 דרכים (MCP, `.env.local`, `.env.test`, CLI/Vercel) העלה חרס: `.env.test` מחזיק מפתח של ref אחר לגמרי וגם פג |
| [11] search-core | ✅ | `2870ae7` | **ההנחה שחסמה אותו הייתה שגויה, ונבדקה ישירות.** `enqueueSearchIndexJob` קורא `QSTASH_TOKEN` ובהיעדרו מריץ inline ומחזיר `{transport:'inline'}` - אותה צורה בדיוק שבה Resend, Sentry ו-Meilisearch כבר עובדים כאן. QStash קונה retry, backoff ו-DLQ לפרודקשן; הוא לא תלות. גם ה-indexer מדלג בשקט בלי Meilisearch, ו-`/search` ממשיך לענות מה-ILIKE. מיגרציה 069 היא 34 שורות idempotent של טבלה תפעולית אחת בלי שום תלות, והוחלה דרך MCP: 7 עמודות, RLS דלוק, 0 policies בכוונה. מה שבאמת חסר הוא **הגדרה ולא קוד**: `SEARCH_WEBHOOK_SECRET` ו-Database Webhook על `public.products`. tsc נקי, 1220 בדיקות (היו 1175), build נקי |
| [12] cart reaper | ✅ | `452b8b0` | `expires_at` קיים מההתחלה ואף אחד לא מחק לפיו. הממצא האמיתי: `abandoned_cart_nudges.cart_id` היה `ON DELETE CASCADE`, ולכן reaper היה מוחק בשקט את ראיית ההחזרה של עגלה שנשלחה עליה תזכורת וחזרה ושילמה - בדיוק הדוח שתוקן ב-[10]. שונה ל-SET NULL. אומת בפרודקשן ושורות הבדיקה נמחקו. נתיב המחיקה היחיד באפליקציה, וה-cron הראשון עם בדיקות. tsc נקי, 1175 בדיקות, build נקי |
| [10] growth-core | ✅ | `bb7bc5f` | הבאג שהחזיק את ה-branch תוקן, ונחסם ב-`cart-units.test.ts` שקורא את אתר הקריאה עצמו (אומת שהוא נכשל כשמחזירים את הכפל). נמצאו שלושה נוספים: `newsletter.ts` הוא `'use server'` וייצא קבוע, מה שמאפס את **כל** הייצוא בקובץ ומפיל את ה-build של שני דפי הניוזלטר; `fn_attribute_cart_recovery` חי בפרודקשן ומעולם לא נקרא; `cart_value_agorot` מעולם לא נכתב. שני האחרונים הפכו את דוח ההחזרה באדמין לאפס קבוע. אומת בפרודקשן: קריאה ראשונה משייכת 1, חוזרת 0, הדוח מראה 100.0%. שורות הבדיקה נמחקו. tsc נקי, 1167 בדיקות (היו 1142), build נקי |
| [9] Integration pass | ✅ | `06bfc33` ואחורה | שתי ההחלטות הוכרעו: `main` מוזג ב-`-s ours` (אפס קבצים ירדו ממנו), ו-3 מתוך 7 `feat/*` מוזגו (ci-foundation, observability, wp-migration). 4 לא, כל אחד עם ראיה נמדדת. 1132 בדיקות, tsc נקי, build נקי |
| [20] LCP element, and a build bug | ✅ | `7db0b88` + הקומיט הזה | הבאנר הוא ה-LCP, והוא חיכה ל-hydration: **3712-3880ms ← בדיוק ב-FCP** (‏slow 4G אמיתי + cpu/4, ‏3 ריצות לכל צד). ‏**Lighthouse לא זז מעבר לרעש** (‏80/81/80 ← 78/81/82) ולא היה יכול: ה-LCP הנצפה בטרייס שלו היה 615ms כבר לפני, ו-5.1s הוא סימולציית Lantern על גרף שמול localhost מכיל את כל הדף. הממצא הגדול הוא בכלל בבנייה: **שרשור `` `a${x}b` + `c` `` מקופל בדריסת הזנב של כל אופרנד**, שלושה קטעים נעלמו מהסניפט, ומה שהוגש הוא regex לא סגור שזורק SyntaxError אצל כל מבקר עם 200 ובלי לוג. שני המצבים אומתו ב-`.next/server/chunks`. שמירה: הסניפט מורץ באמת מול document מזויף על טבלת הערכים של `needsConsentDecision`, ובדיקת E2E משווה את **הבייטים המוגשים** לקבוע. בנוסף `<noscript>` שמסתיר את הבאנר בלי JS, אחרי שה-E2E תפס שהוא היה מוצג עם שני כפתורים מתים. ‏1246/1246, **67/67**, שער הבית 11.26-11.62% |
| [19] The optimizer that never optimized | ✅ | `8503bb4` | ‏pnpm נתן ל-next עותק sharp משלו (‏`^0.34.5`, ‏libvips 8.17.3) בזמן שהאפליקציה פותרת את ה-0.35.3 שהיא מצהירה. ‏0.34.5 זורק `source: bad seek` על ה-AVIF של הפרויקט, ו-`optimizeImage` **בולע את החריגה ומחזיר את קובץ המקור בלי לוג**: 200, תמונה תקינה, **זהה בייט-בייט בכל `w`**. ‏32 כרטיסים ששלחו מקור 2048px לצייר 157px, וההסבר לכך ש-[17] ו-[18] מדדו אותם בייטים בלי קשר ל-`sizes`. הוצמד sharp אחד ב-`pnpm-workspace.yaml`: אותה כתובת ב-w=288 היא **93264 ← 4176 בייטים**. ‏`.next/cache/images` דורש ניקוי חד-פעמי - 13MB של תשובות pass-through המשיכו להיות מוגשות אחרי התיקון. ‏`sizes` תוקן משני הכיוונים (נמדד ב-18 צמדי רוחב/dpr: מתחת ל-1024 הכרטיס הרחב הוא בדיוק `50vw - 48px`), ונוספה מדרגת 288. בית בנייד **1330.4 ← 403.3KB**, ‏Lighthouse **78 ← 80**, ‏SI **3.3 ← 1.5s**, שערים ללא תזוזה. בדרך: ‏`pnpm install` היה יוצא 1 על placeholder מילולי ב-`allowBuilds`. ‏1235/1235, **62/62** |
| [18] Catalog optimizer + hero sizes | ✅ | `5fe6563`, `b6f17ff` | שלושת דפי הקטלוג לא הגיעו לאופטימייזר מעולם: `/products` ‏613.5KB שמתוכם 604.2KB גולמיים, לצייר קופסאות 186px. הממצא הגדול יותר הוא ש-**45 שורות תמונה picsum נחסמו ב-CSP** (`img-src` בלי picsum) וצוירו שבורות בשלושת הדפים; הוכח ב-probe עם הודעת ההפרה בקונסול, ולא הוסק. ‏`/products` ‏**613.5 ← 365.3KB**, ‏16 תמונות מצוירות ← 25. הירו: ‏4 שקופיות עם 100vw שמציירות 193px קבוע (נמדד ב-6 רוחבים), **326.7 ← 106.7KB**; הסטילס 27.7 ← 15.9KB אחרי שהתברר ש**ההערה מ-[16] הפוכה** - ב-next 16.2.4 דווקא `sizes` בלי vw מקבל את כל הרמפה, ו-42vw הוא שחתך את מדרגת 256. **התיקון החמיר CLS** (קטגוריה 0.197 ← 0.414) כי תמונה חסומה לא מזיזה כלום; קופסת התמונה מקבעת עכשיו את 186.03px שהשלד כבר הבטיח, וה-Lighthouse הנייד עלה **78 ← 85** ו-**60 ← 82**. שערים: home 11.56 ← 11.16%, category ו-products עלו **בתוך קופסאות התמונה בלבד** (גובה הדף זהה, נמדד בשני המצבים על אותה בניה). ‏2 שמירות E2E אומתו נכשלות בלי התיקון. ‏1235/1235, **61/61** |
| [17] Deal card images | ✅ | `b345bd8` | ‏`fill` על תמונה שהקופסה שלה לוקחת ממנה את הגובה: `position:absolute` inline מנצח את המחלקה, התמונה יוצאת מהזרימה, ו-`.p_con__image-wrap` מתמוטט ל-0. נמדד על הבניה הרצה: **239x0 עם naturalWidth 459** בכל 32 הכרטיסים. גובה הדף 3504 ← 5464 (החי 5492), השער **22.44% ← 11.56%**, תמונות בגובה 0: 36 מתוך 51 ← 5. רצפת הרעש נמדדה בנפרד בהשוואת האתר החי לעצמו ב-20 שניות הפרש: **2.62%**, וכולה הסליידר שלו. האופטימייזר נשמר, כי מה שהוא צריך זה `sizes` ולא `fill`. שמור ב-E2E ולא ב-Vitest כי ל-jsdom אין מנוע פריסה, ואומת שנכשל כשמחזירים `fill`. ‏1235/1235, ‏**59/59** |
| [16] Hero art direction | ✅ | `893e4a4` | ‏`<picture>` עם `<source media>`: הנפשה בדסקטופ, סטילס (frame 0) בנייד. ‏775.8KB ← 27.7KB, בית 72 ← 75, ‏LCP 10.1 ← 7.7s. **הבניה הראשונה שברה את הדסקטופ:** ‏`getImageProps` עם `unoptimized` מחזיר `srcSet: undefined`, ו-`<source>` בלי srcset **כן מתאים ל-media** ופשוט לא נפתר, אז ב-1440 הדפדפן בחר בו והשקופית רינדרה 1x1 בלי שגיאה. הוחזר בנוסף preload עם `media` שהקומפוננטה לא יכולה לפלוט. שער הפיקסלים 22.44% מול **22.34% באותה בניה בלי השינוי** - הפער קדם לדיף וצריך בדיקה נפרדת. הופרד במדידה: כל פס מחוץ לקופסת הירו 0%, ובתוכה רק פריים ההנפשה (לולאה 17.4s, ‏5s על פריים 0, ‏compare מצלם ב-6s). ‏6 בדיקות על ההצמדה, אומתו נכשלות בלי המיפוי. ‏1235/1235, ‏58/58 |
| [15] Lighthouse mobile | ✅ | הקומיט הזה | לא נמדד מעולם: [7] מדד דסקטופ בלבד. מול build נקי ו-`pnpm start` על 3211, כי שני `next-server` ישנים החזיקו את 3000. בית 72 / מוצר 88 / קטגוריה 85, SEO 100 בשלושתם. **דף הבית גרוע מה-baseline**: LCP 10.1s מול 9.2s רשום. הסיבה היחידה היא קובץ ירו מונפש של 777KB שיוצא גולמי בלי `/_next/image` (השאר 56-97KB דרך האופטימייזר), והוא `unoptimized` בכוונה כי `next/image` הורג הנפשה. TBT 80ms, CLS 0. ממצא נלווה: `sitemap.ts` בנוי על `createAdminClient()` ולכן מקומית מחזיר 3 כתובות בלי שגיאה |
| [14] WP import dry run | ✅ | `fd3f6d9` | ‏`WP_IMPORT_ALLOW_WRITES` לא הוגדר, אפס כתיבות. הבסיס מ-29.07 שוחזר בדיוק לפני נגיעה. מוצר ה-Dokan הוא `publish`, ולכן סינון הסטטוס מעולם לא תפס אותו: הוחרג לפי slug, ו-`count_parity_products` לימד להחסיר אותו כדי לא להחליף ירוק שקרי באדום שקרי. **הממצא**: שורה קיימת מיפתה את `/` ל-`/product/חיתולי-פמפרס-העתק`, כי `pathOf` מוריד query string ו-wp_id 6810 מקושר כ-`/?post_type=product&p=6810`. תוקן ב-`permalinkPath()`. שער `redirect_coverage` עבר קודם 76/76 על תת-קבוצה, עכשיו נופל 98/103 נכון. ‏52 בדיקות wp-import (היו 43), שתיים מהן אומתו שנכשלות בלי התיקון. ‏1229/1229 Vitest, tsc נקי, biome נקי |
| [13] ביקורת GO-LIVE | ✅ | `0e16179` | **שתי שורות במסמך לא היו נכונות.** ‏`CHECKOUT_ENABLED` נרשם כחוסם פתוח ("משתנה חסר מפעיל צ'קאאוט") אבל `src/lib/payments/env.ts:37-40` כבר נופל סגור בפרודקשן על כל דבר שאינו `'true'` מדויק, עם 6 בדיקות שנועלות את שני הכיוונים; ומספר ה-worktrees היה 34 ולא 7. נסגרו כולם: 27 נקיים וממוזגים הוסרו, 5 החזיקו עבודה לא מקומיטת ש**קומיטה ונדחפה ל-`origin`** לפני ההסרה (הגדולה שבהן סוויטת seed בת 11 קבצים), ו-2 branches שלא מוזגו בכוונה נשארו שלמים. נמדד מול הפרודקשן: 11/11 ספקים בלי כתובת ולוגו, 6/11 בלי טלפון, 61 מוצרים פעילים, 30 עם `picsum` (היו 45). הממצא שלא היה מתועד: שער הפרסום רץ על כל שמירה של מוצר פעיל, ולכן עריכת מוצר באדמין חסומה היום לכל 61. השער תקין, הנתונים חסרים, ולא נגעתי בו |
| [9] Integration pass (סבב ראשון) | ⚠️ | `5ae8298` ואחורה | **22 branches מוזגו**: כל `arch/*` וגם `feat/checkout-complete`, כולם תיעוד בלבד. 93 מסמכי ארכיטקטורה על ה-branch, אפס שורות קוד השתנו, 1055 בדיקות ירוקות. **8 לא מוזגו במכוון**: 7 branches של `feat/*` נושאים 11 עד 44 קבצי קוד מ-27 עד 30.07, ו-`main` שהקומיט היחיד שלו הוא צ'קפוינט של 153,834 שורות artifacts. שניהם דורשים החלטה של אופיר, לא מיזוג עיוור |
